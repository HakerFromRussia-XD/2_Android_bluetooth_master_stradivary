# План архитектуры V3 и UBI4

Зафиксирован: 2026-09-09. Это основной действующий план.
История выполненных изменений: [перенос Slider](v3_speed_settings_migration.md)
и [исправление синхронизации V3](v3_sync_initialization_fix.md).

## 1. Постоянные решения

- Сохраняем Gradle-модули `app`, `shared`, `bluetooth`, `delegateadapter`.
- Сначала завершаем V3 (STANDARD_V3 и INDY3), затем UBI4.
- У V3 состав виджетов задан приложением, у UBI4 приходит от устройства/CRC-кеша.
  Эти способы формирования экрана остаются разными.
- `BLEParser` и `BLEParserV3`, включая существующую генерацию виджетов,
  остаются без переработки. Общую инфраструктуру не копируем для каждого экрана.
- Не редактируем `iosApp`, `shared/src/iosMain`,
  `old_electronic_by_Misha`, `new_electronic_by_Rodeon`.
- Сохраняем XML, Compose, OpenGL, внешний вид, порядок виджетов, локализацию,
  протоколы, очередь, ключи настроек, базу и сохранённые данные.
- Уточнение пользователя 2026-09-11: UI и способы отображения остаются прежними.
  Архитектурный перенос не добавляет блоки ошибок, индикаторы, кнопки, сообщения
  или новые повторы операций. Уже имеющиеся флаги ошибок в UiState сохраняются;
  новое отображение для них в текущий рефакторинг не входит.
- MVVM: ViewModel принадлежит конкретному экрану. MVI: действие → обработка →
  неизменяемый UiState → отображение. StateFlow не заменяет постоянное хранилище.
- Правила и сценарии — domain; BLE/БД/сеть — data; подготовка UI и его состояние —
  presentation. Зависимости: `presentation → domain ← data`.
- Конструкторы и небольшие фабрики; внедрение Hilt/Koin, новые Gradle-модули,
  обновление библиотек и универсальный MVI-фреймворк в план не входят.
- Используем существующие названия Slider, ToggleSlider, Spinner, Switch, Plot,
  Gestures и ключи параметров. Не вводим Legacy/Migrated в названия.
- Один запрос — небольшой законченный шаг текущего этапа. Размер шага можно
  увеличить по просьбе пользователя, сохраняя границы и проверки.
- Исправление `BindingGroupV3Test` отложено пользователем. Не меняем привязки
  и парсеры ради запуска этого теста; ограничение явно учитываем в результатах.

## 2. Единица работы — законченный экран

Стратегия: доводим до полной цепочки один экран, затем переходим к следующему.
Внутри экрана переносим типы виджетов по очереди, когда этого требует его состав.
Готовые обработчики переиспользуем на других экранах.

Первый экран — `SpecialSettingsFragment` (display=2).
Он содержит разделы настроек протеза и приложения. Текущий маршрут:
`page_4 → showSpecialScreen() → SpecialSettingsFragment`.
Затем `SensorsFragment` (display=1), `ServiceFragment` (display=4),
далее остальные экраны и сценарии V3 по порядку раздела 7.

Законченный экран получает из своего UiState:
- состав, порядок и устойчивые идентификаторы элементов;
- их значения и доступность;
- состояние загрузки/синхронизации и значимые ошибки, относящиеся к этому экрану.

Граница SpecialSettings уточнена при завершении A6: загрузка/операции профилей,
autoLogin и ошибки настроек принадлежат экрану. Общий SyncProgressDialog и его
синхронизация принадлежат Activity; их состояние не дублируем в экранной ViewModel
без отдельного потребителя. Готовность параметров приходит через repository.

Fragment отображает состояние и передаёт действия. Он не читает
`UiState.listWidgets`, не вызывает `DataFactory.prepareData()` и не собирает
BLE-пакеты. Адаптеры не обращаются к ViewModel, репозиториям или глобальным stores.
Служебные одноразовые эффекты вводим только для реальных сценариев навигации,
диалогов и сообщений; правила доставки определяем явно.

Не считаем экран завершённым только потому, что обработчики его Slider перенесены.

## 3. Оценка уже выполненной работы

Сохранить и развивать:
- `V3DeviceSettingsRepository` и его реализацию: чтение, наблюдение, запись через
  существующие stores, кодеки, сохранение профиля и очередь.
- `SetSliderValueUseCaseV3` и `V3SliderSettingsRules`: ограничения семи параметров
  проверяются в domain, UI использует те же диапазоны.
- Раздельные состояния и отложенные записи Slider; сохранение второго EMG-поля
  при изменении первого; отмену ожидающих записей при остановке представления.
- Пассивный `SliderDelegateAdapterV3`: `renderSliders(states)` и callback действий.
- Существующие проверки пакетов, кеша, профилей, границ и lifecycle-действий.

На 2026-09-09 Android-сборка и 68 app-тестов прошли
(timestamp 12:30:28 UTC; лог `/tmp/ubi4-slider-fragment-domain-20260909.log`).
Эти проверки не покрывали подключение Slider к фактически открываемому Fragment;
ошибка выбора AdvancedFragment обнаружена после них. Исправление — A1.1 ниже.
Реальные действия Slider, сохранение профиля и переходы lifecycle на V3/INDY3
ещё не проверены. Проверенная ранее синхронизация обычного V3 этого не заменяет.

### Судьба bindV3SliderSettings и BaseWidgetsFragment

`bindV3SliderSettings` сейчас архитектурно допустим как временное соединение
слоёв: Fragment владеет ViewModel и подпиской через
`viewLifecycleOwner.repeatOnLifecycle(STARTED)`, адаптер отображает состояние.
Откатывать эту работу не нужно.

Но базовый Fragment не становится постоянным владельцем поведения всех экранов.
Фиксируем порядок его сокращения:
1. В A2 подписка и действия SpecialSettings перенесены в `SpecialSettingsFragment`.
   Его владелец — `V3SpecialSettingsViewModel`, фабрика —
   `V3SpecialSettingsViewModelFactory`, состояние — `V3SpecialSettingsUiState`.
   Пакет — `presentation.specialsettings`; имена V3AdvancedSettings из кода удалены.
2. Общая логика выделена в `presentation.sliders/V3SliderSettingsController`
   с явно переданным viewModelScope. Sensors с B1 использует собственную
   `V3SensorsViewModel`, Service с B2.1 — `V3ServiceViewModel`. Advanced пока
   использует нейтральную `V3SliderSettingsViewModel` через прежний binder.
   Остальные действия Service завершаются в B2; domain/data не копируются.
3. Пока экран не переведён, его рабочий путь через `bindV3SliderSettings`
   сохраняется. После перевода последнего потребителя этот метод и временная
   настройка `v3SliderParameterKeys` удаляются из BaseWidgetsFragment.
   AdvancedFragment тоже сохраняет прежнюю привязку, пока существует его отдельный
   вход showAdvancedScreen; его судьбу проверяем перед удалением последнего binder.
4. Остальные обязанности BaseWidgetsFragment сокращаются при переносе их
   конкретных экранов. Большого единовременного переписывания базового класса нет.

Это заранее запланированное завершение перехода, а не смена стратегии по ходу.
Новые ToggleSlider/Spinner-подписки в BaseWidgetsFragment не добавляем.
При отделении SpecialSettings сначала сохраняем работоспособность Slider на Sensors/Service;
не расширяем общую ViewModel до модели поведения всех экранов приложения.

## 4. Границы shared, DataFactory и domain

`shared` — модуль переиспользуемого кода, а не отдельный архитектурный слой.
В нём могут находиться data, domain и общая presentation-логика.

Существующая цепочка состава ещё не перенесённых экранов и UBI4:
`BLEParser/BLEParserV3 → UiState.listWidgets → DataFactory.prepareData(display)
→ List<Any> с SliderItemV3/другими Item → Fragment → адаптеры`.

После A3 состав SpecialSettings V3 проходит через
`DataFactoryV3SpecialSettingsWidgetsSource → V3SpecialSettingsWidgetMapper
→ V3SpecialSettingsViewModel → UiState.widgets → Fragment`.
Преобразователь создаёт совместимые Items для прежних адаптеров на UI-границе.
Domain не зависит от DataFactory и UI-структур.

Цепочка значений настроек:
`входящий пакет → существующий кодек/store → Repository → ViewModel → UiState`.
Действие идёт через `Fragment → ViewModel → UseCase → Repository → текущая очередь`.
Это пути данных/вызовов, а не обратные зависимости domain от реализации репозитория.

DataFactory фильтрует по display, сортирует, выбирает подписи и создаёт UI Items.
Эти обязанности относятся к подготовке представления.
Папка `shared/.../data` также содержит Room, сеть, парсеры, состояния и структуры;
массовый перенос всей папки в один слой не выполняется.

| Обязанность | Целевое место |
| --- | --- |
| Параметры устройства, ограничения и зависимости настроек, UseCase и repository-контракты | Domain соответствующей версии |
| Парсеры, кодеки, BLE-пакеты, БД, сеть, преобразование транспортных данных | Data; существующие парсеры остаются на месте |
| Состав и порядок UI, подписи, преобразование в UI Items | Presentation, общая часть может быть в shared |
| UiState, действия и черновики экрана | Presentation/ViewModel в app |
| Fragment, XML/Compose, адаптеры и анимации | Android UI в app |

Не протаскиваем `Any`, `SliderParameterWidgetSStruct`, Room Entity,
Android View/Context и байты BLE в новые domain-контракты.
Domain не занимается локализацией, сортировкой RecyclerView или выбором XML.

### Совместимость с iOS

iOS напрямую вызывает DataFactory.prepareData() и использует существующие Items
и widget-структуры. Эти публичные API сохраняем.

Сначала отделяем Android-потребление через узкую границу совместимости:
существующая подготовка списка → преобразование в типизированные UI-модели →
состояние экрана. Разбор старых `Any`/widget-структур сосредоточен в одном
presentation-преобразователе; Fragment и ViewModel не разбирают их сами.
Domain получает только понятия устройства и правила, не старые UI Items.

DataFactory остаётся источником совместимости, пока новые цепочки не покрыты
проверками. На этапе KMM его внутренние обязанности можно выделять с делегированием
из прежнего API; его имена, сигнатуры и наблюдаемое поведение для iOS сохраняются.
Старые публичные модели нельзя удалить только потому, что Android ими уже не пользуется.

### Физическое размещение

На текущих Android-этапах сохраняем `app/.../versions/v3/{presentation,domain,data}`.
Пустые будущие пакеты заранее не создаём.

На отдельном этапе KMM платформенно независимые domain-модели, правила и UseCase
переезжают в `shared/commonMain/.../versions/v3/domain` с сохранением package/API.
Android ViewModel, Fragment и платформенные реализации остаются в app;
репозитории/адаптеры размещаются по их реальным платформенным зависимостям.
Это единственный запланированный перенос уже выделенного чистого domain в другой
существующий модуль; одновременно его поведение не перерабатывается.

UBI4 затем использует такую же структуру слоёв для своей версии. Специфические
сценарии V3/UBI4 разделены; общие алгоритмы и инфраструктура переиспользуются.

## 5. Правила состояния и операций

- Наблюдение, первая отрисовка и повторная подписка не отправляют команды.
- UiState содержит состояние экрана и черновики; подтверждённое устройством
  значение не приравнивается автоматически к локально выбранному.
- Сохраняем текущую оптимистичную запись. Подтверждения, retries и rollback
  не добавляем под видом рефакторинга.
- Business-правила выполняет domain. Таймеры жестов UI, отображение черновика
  и анимации остаются в presentation. UseCase соответствует содержательному
  сценарию, а не каждому callback или полю.
- Побочные действия выполняются вне `MutableStateFlow.update` и чистых reducers.
- Состояние устройства читается через repository-контракт; сборка UI относится
  к presentation. UseCase-посредник ради одного перенаправления не обязателен.
- Для экранной подписки используем lifecycle представления. При выходе из STARTED
  ожидающие записи отменяются; при возврате читается актуальное состояние.
- Fragment.hide() без смены lifecycle не равен STOP. Отдельно проверяем текущую
  навигацию; её семантику не меняем незаметно вместе с архитектурой.
- Создание конкретных зависимостей — точка сборки/фабрика; UI и ViewModel
  не вызывают `MainActivityUBI4.main` для работы с устройством.

## 6. Первый экран: SpecialSettingsFragment

Ниже — состав раздела настроек протеза из текущих генераторов BLEParserV3, display=2.
Имена, порядок, подписи и различия профилей берём из существующего источника,
второй независимый каталог хардкода не создаём.

| Тип / ключ | STANDARD_V3 | INDY3 |
| --- | --- | --- |
| Slider: P_KEY_EMG_MAX_GAIN_VALUE | Да, 0..250 | Да, 0..250 |
| Slider: P_KEY_FORCE_SETTINGS | Да, 0..100 | Да, 0..100 |
| Slider: P_KEY_SPEED_SETTINGS | Да, 0..100 | Да, 0..100 |
| ToggleSlider: P_KEY_EMG_CHANGE_GESTURE | Да | Нет |
| ToggleSlider: P_KEY_EMG_MOVEMENT_LOCK | Да | Да |
| ToggleSlider: P_KEY_SCREEN_TIMEOUT | Да | Нет |
| Spinner: P_KEY_HAND_CONTROL_MODE | Да | Да |
| Spinner: P_KEY_GESTURE_CHANGE_MODE | Да | Нет |
| Spinner: P_KEY_SETTINGS_PROFILE | За выключенным SETTINGS_PROFILE_WIDGET_ENABLED | Да |

У ToggleSlider исходные границы 10..100, множитель отображения 0.1 и секунды.
Это ещё не описание формата записи: кодирование флага/времени, блокировки,
связанные значения и профильное сохранение прослеживаем перед его переносом.
SETTINGS_PROFILE — особый сценарий локального/синхронизируемого профиля;
не считаем его обычной записью Spinner по внешнему сходству.

Вторая вкладка берётся из DataFactory.mobileWidgets(): сейчас это Switch
«Автоматический вход», значение — SET_MODE_SMART_CONNECTION в SharedPreferences.
Выбор вкладки сохраняется в LAST_ACTIVE_SETTINGS_FILTER. Эти настройки не являются
BLE-параметрами V3. Их рабочий путь сохраняем при A2–A5, состояние включаем в A6.
У SpecialSettingsFragment нет pull-to-refresh; обработчик AdvancedFragment
не переносим в этот экран.

Текущая проверенная отправная точка: три Slider на этом экране уже используют
domain/Repository, а состав списка и остальные элементы — прежнюю цепочку.

## 7. Зафиксированный порядок этапов

### Этап A. Завершить SpecialSettingsFragment

- [x] A0. Зафиксировать стратегию, текущее состояние и состав экрана по двум профилям (этот документ).
- [x] A1. Проследить updateFlow/DataFactory, загрузку, профиль и обработчики остальных
  элементов SpecialSettings; определить контракт полного UiState и карту преобразования.
  Зафиксировать проверки состава/порядка/подписей STANDARD_V3 и INDY3.
  Результат: [контракт состояния SpecialSettings V3](v3_special_settings_state_contract.md).
  Исправлена карта навигации и добавлена вкладка приложения; реализация полного
  состояния и проверки поведения остаются в A2–A6.
- [x] A1.1. Исправить пропущенную привязку трёх Slider в SpecialSettingsFragment
  через существующий binder; проверить сборкой и app-тестами. Не объявлять этим
  завершённым A2 и не перерабатывать остальные обработчики.
  Добавлены ключи скорости, силы и максимальной чувствительности; guard V3 в Base
  сохраняется. `:app:assembleDebug` и 68 app-тестов прошли, 0 ошибок/пропусков
  (timestamp 2026-09-09 13:14:38–39 UTC;
  лог `/tmp/ubi4-special-settings-slider-20260909.log`). Маршрут/привязка сверены
  по коду; существующие unit-тесты не проверяют Android view. Действия на телефоне
  и переключение вкладок с ожидающей записью не проверены.
- [x] A2. Закрепить владение состоянием SpecialSettings за SpecialSettingsFragment и его ViewModel.
  Сохранить Slider на Sensors/Service через переиспользуемую presentation-логику.
  Убрать для SpecialSettings зависимость от временного bindV3SliderSettings.
  Если для этого нужно разделить текущую общую ViewModel, минимальные экранные
  ViewModel Sensors/Service с уже работающими Slider выделяются в этом же
  техническом шаге; остальное поведение этих экранов остаётся этапом B.
  Учесть выбор вкладки приложения: скрытие настроек протеза не вызывает STOP
  Fragment, поэтому отмену ожидающих записей и восстановление значений проверить
  отдельно. В A1.1 добавляется только ранее пропущенная привязка.
  Выполнено: отдельные V3SpecialSettingsAction/UiState/ViewModel/Factory,
  общие V3SliderAction/SettingsController и совместимая Slider ViewModel.
  SpecialSettings больше не задаёт v3SliderParameterKeys и не использует базовый
  binder. При STOP и выборе APPLICATION ожидающие записи отменяются; возврат
  к PROSTHESIS читает актуальные значения без отправки. Выбор вкладки уже входит
  в UiState, чтение/сохранение предпочтения пока остаётся в Fragment до A6.
  Проверка: assembleDebug и 77 app-тестов в 14 suites, 0 ошибок/пропусков;
  9 новых проверок экранной модели. Timestamp 2026-09-09 13:30:31–32 UTC,
  лог `/tmp/ubi4-special-settings-a2-20260909.log`. Проверены отмена, возврат,
  блокировка, независимость экранов и прежние тесты всех семи Slider.
  Android UI и BLE-действия на телефоне не проверялись; состав списка и остальные
  обработчики ещё используют прежний путь, экран не объявляется завершённым.
- [x] A3. Отделить получение и преобразование состава SpecialSettings через границу совместимости.
  Подключить список к состоянию экрана, сохраняя прежнее поведение ещё не перенесённых
  обработчиков. DataFactory и iOS API не менять. Отсутствие повторной инициализации
  при подписке — обязательная проверка.
  Выполнено: presentation-источник вокруг DataFactory/updateFlow и типизированные
  описания Slider, ToggleSlider, Spinner, SettingsProfile и мобильного Switch.
  UiState содержит deviceProfile и widgets выбранной вкладки. Первое чтение и
  возврат экрана получают снимок без ожидания события; одинаковый состав не
  сбрасывает черновик/таймер Slider и не пересоздаёт список при изменении значения.
  Изменение адреса/типа устройства или набора Slider отменяет ожидающие записи.
  При занятом RecyclerView откладывается отрисовка актуального состояния;
  callback удаляется при уничтожении view. Путь UBI4 остаётся прежним.
  Проверка: assembleDebug и 91 app-тест в 17 suites, 0 ошибок/пропусков;
  12 новых проверок A3. Timestamp 2026-09-09 14:01:12–13 UTC,
  лог `/tmp/ubi4-special-settings-a3-20260909.log`. Реальные неизменённые генераторы
  V3/INDY3 и ресурсы RU/EN проверены по составу, порядку, подписям, вариантам,
  диапазонам и метаданным, определяющим ID/привязки существующих адаптеров.
  Проверены обе вкладки, replay, отсутствие BLE-вызовов при наблюдении,
  поздний список/значения, смена устройства и ParameterStore.clear().
  Android view/действия на телефоне не проверялись. Обработчики ToggleSlider,
  Spinner и AUTO_LOGIN остаются прежними до соответствующих A4–A6.
- [x] A4. Перенести ToggleSlider этого экрана: сначала один проверенный сценарий,
  затем остальные применимые параметры обоих профилей.
  A4.1 выполнен: «Блокировка движения с ЕМГ» (`P_KEY_EMG_MOVEMENT_LOCK`) в
  SpecialSettings для STANDARD_V3 и INDY3. Время и флаг входят в toggleSliders
  экранного UiState; действия проходят через ViewModel и domain UseCase.
  V3ToggleSliderSettingsRepository — отдельный узкий контракт; его реализует
  существующий V3DeviceSettingsRepositoryImpl, экземпляр общий с обычными Slider.
  EditToggleSliderUseCaseV3 изменяет время либо включённость, сохраняя второе поле;
  SendToggleSliderValueUseCaseV3 отправляет актуальное локальное значение.
  Domain проверяет пользовательские 10..100 (1.0..10.0 сек) и доступность.
  Data сохраняет прежние bit7/0..6, store → профиль → сериализованный кеш.
  Это локальное сохранение происходит сразу; отправка через прежнюю очередь —
  спустя 300 мс после последнего изменения. Таймер принадлежит экранной ViewModel.
  STOP, APPLICATION, исчезновение виджета, смена адреса/типа устройства и блокировка
  отменяют отправку; уже сохранённое локальное значение не откатывается.
  Base добавляет только конфигурацию адаптера, render и callback; новых подписок
  ToggleSlider и их таймеров в Base нет. На остальных экранах и для остальных
  ключей остаётся прежний обработчик. Связи view/анимации освобождаются при
  повторной привязке и переработке строки RecyclerView.
  Проверка: assembleDebug и 111 app-тестов в 18 suites, 0 ошибок/пропусков;
  20 новых интеграционных проверок A4.1. Timestamp 2026-09-09 14:25:18–19 UTC,
  лог `/tmp/ubi4-special-settings-a4-20260909.log`. Проверены оба типа устройства,
  кеш/store/clear, drag без записи, немедленное сохранение и отложенный пакет
  `00 0F 39 <packed> <CRC>`, границы, сохранение входящих 0/9/100/127 при
  переключении флага, отмена/повторный attach и прямые ограничения domain.
  Android view/действия на телефоне не проверялись. Парсеры, shared/iOS,
  Gradle-модули и ветки by_Misha/by_Rodeon не менялись.
  A4.2 выполнен: «Переключение жестов сенсорами» (`P_KEY_EMG_CHANGE_GESTURE`)
  и «Время работы экрана» (`P_KEY_SCREEN_TIMEOUT`) в STANDARD_V3 подключены
  через те же UiState, UseCase, repository и адаптер. Новые классы не добавлялись:
  расширены допустимые ключи domain и набор ToggleSlider экранной ViewModel.
  Команды сверены с registry и генератором: `00 0F 15 <packed> <CRC>` для
  переключения жестов, `00 10 08 <packed> <CRC>` для времени экрана.
  Формат флага/времени, 10..100, шаг 0.1 сек, немедленное локальное сохранение
  и задержка отправки 300 мс сохранены. Таймер каждого ключа независим.
  INDY3 по своему составу активирует только EMG_MOVEMENT_LOCK; действия для
  отсутствующих виджетов отклоняются, смена типа устройства отменяет ожидающие команды.
  Проверка A4.2: assembleDebug и 118 app-тестов в 18 suites, 0 ошибок/пропусков;
  7 новых проверок, всего 27 проверок ToggleSlider. Timestamp 2026-09-09 14:40:24 UTC,
  лог `/tmp/ubi4-special-settings-a4-2-20260909.log`. Проверены свои команды,
  кеш и ключи профильного сохранения обоих параметров, drag/флаг/границы,
  независимые таймеры трёх виджетов, удаление одного, общая отмена и переход в INDY3.
  Android view/действия на телефоне не проверялись. В A4.2 production-изменения
  ограничены двумя файлами domain/presentation; UBI4, парсеры и shared/iOS не менялись.
- [x] A5. Перенести обычные Spinner SpecialSettings; сценарий SETTINGS_PROFILE выполнить
  отдельным шагом после проверки его сохранения/применения и различий профилей.
  A5.1 выполнен: «Режим работы протеза» (`P_KEY_HAND_CONTROL_MODE`) для STANDARD_V3
  и INDY3. `spinners` в экранном UiState содержит отображаемый индекс и доступность;
  варианты остаются в описании виджета. `SpinnerValueSelected` проходит через
  ViewModel → SetSpinnerValueUseCaseV3 → V3SpinnerSettingsRepository. Узкий контракт
  реализует тот же V3DeviceSettingsRepositoryImpl; domain разрешает пять режимов 0–4.
  Пользовательский выбор сразу обновляет store, сохранение профиля и сериализованный
  кеш, затем отправляет прежнюю команду 0F/3B через очередь. Повторный явный выбор
  текущего режима сохраняет прежнее поведение отправки. При render, входящем значении,
  возврате на экран и обновлении состава записи нет. Default/clamp применяются только
  к отображению, исходное значение не переписывается. На время программного выбора
  синхронный listener PowerSpinner 1.2.7 отключается и восстанавливается в finally.
  Адаптер освобождает обработчики при recycle/rebind/destroy; позднее обновление списка
  профилей не меняет уже переиспользованную строку. Остальные Spinner сохраняют прежний путь.
  Проверка A5.1: assembleDebug и 136 app-тестов в 20 suites, 0 ошибок/пропусков;
  XML 2026-09-10 08:07:24 UTC, лог `/tmp/ubi4-special-settings-a5-1-20260910.log`.
  Добавлено 18 проверок: пять команд, оба профиля, кеш/store/clear, блокировки,
  смена устройства/вкладки, отсутствующие/невалидные значения и варианты;
  реальный DefaultSpinnerAdapter проверен на JVM с заглушками View и перерисовки.
  Android view/действия на телефоне не проверялись. Shared, парсеры и iOS не менялись.
  A5.2 выполнен: «Действие при смене жеста» (`P_KEY_GESTURE_CHANGE_MODE`) для
  STANDARD_V3 подключено через те же UiState/Action/UseCase/repository и адаптер.
  Production-изменения этого шага — ключ в ViewModel и domain-диапазон 0–1;
  порядок вариантов «Без действия» / «Перейти в открытое положение» и команда
  0F/2D сохранены. В INDY3 виджета нет: его состояние не допускает выбор,
  «Режим работы протеза» продолжает работать. Значения, кеш и профильные ключи
  двух Spinner независимы. Обычные Spinner SpecialSettings завершены;
  На этапе A5.2 A5 оставался открытым до сценария SETTINGS_PROFILE (A5.3).
  Проверка A5.2: assembleDebug и 146 app-тестов в 20 suites, 0 ошибок/пропусков;
  XML 2026-09-10 08:20:00–02 UTC, лог `/tmp/ubi4-special-settings-a5-2-20260910.log`.
  Добавлено 10 проверок команды/сохранения, независимости двух ключей, восстановления,
  блокировок и domain-границ. Проверка существующих генераторов RU/EN дополнена
  порядком двух вариантов и отсутствием виджета в INDY3. Android view/телефон
  не проверялись; shared, парсеры и iOS не менялись.
  A5.3.1 выполнен: список «Профили настроек» и активный ID входят в
  `settingsProfiles` экранного UiState. V3SettingsProfilesRepositoryImpl читает
  существующий shared repository по зафиксированному serial; GetSettingsProfilesUseCaseV3
  сохраняет сортировку по ID, максимум 3 и профиль по умолчанию. UI локализует
  стандартные имена; индекс не заменяет ID. Чтение не применяет профиль и не
  отправляет BLE; после принятого результата data синхронизирует прежний локальный
  индекс в store/кеше. Поздние ответы после смены устройства, нового запроса,
  STOP и очистки ViewModel не обновляют состояние/кеш. Без виджета запросов нет.
  На этапе A5.3.1 выбор/создание/переименование выполняли прежние обработчики адаптера.
  Их callback и завершение существующего серверного импорта инвалидируют список
  через Android V3SettingsProfilesUpdates. Bind/входящие store-значения больше
  не определяют список профилей SpecialSettings. Render не вызывает выбор;
  scope оставшихся операций восстанавливается после destroy/rebind.
  isLoading/loadFailed описывают только чтение; отображение общей ошибки остаётся A6.
  A5.3 и весь экран ещё не завершены.
  Проверка A5.3.1: assembleDebug и 164 app-теста в 23 suites, 0 ошибок/пропусков;
  XML 2026-09-10 08:36:47–49 UTC, лог `/tmp/ubi4-special-settings-a5-3-1-20260910.log`.
  Добавлены 13 проверок состояния/жизненного цикла/правил, 2 проверки чтения и
  локального кеша, 2 проверки инвалидации импорта и проверка программного выбора
  реального SettingsProfileSpinnerAdapterV3 на JVM с заглушками View/перерисовки.
  Android view/устройство и реальные операции профилей вручную не проверялись.
  Shared, парсеры, iOS и существующие Gradle-модули не менялись.
  A5.3.2 выполнен: выбор существующего профиля передаётся по ID через
  SettingsProfileSelected → V3SpecialSettingsViewModel → SelectSettingsProfileUseCaseV3
  → V3SettingsProfilesRepository. UseCase проверяет ID и наличие профиля;
  явный повтор активного профиля по-прежнему применяет сохранённые значения.
  На этапе A5.3.2 isSelecting/selectionFailed отражали выполнение/ошибку выбора
  (с A5.3.3 заменены на operation/failedOperation). На время выбора
  все перенесённые параметры экрана недоступны; таймеры Slider/ToggleSlider отменены.
  В shared SettingsProfileManager добавлен awaitPendingWrites(serial): ожидаются
  уже зарегистрированные BLE- и мобильные сохранения этого serial до смены
  активного профиля. Отмена ожидания не отменяет сохранения. Сигнатуры прежних
  методов сохранены, парсеры и iOS-файлы не менялись.
  Data проверяет текущие serial/MAC/доступность и cancellation перед изменением
  профиля и применением; синхронизирует локальный индекс, вызывает прежний applier,
  переданный при создании repository, и инвалидирует список. STOP, смена раздела,
  устройства/serial и блокировка отменяют выбор. Уже выполненное изменение БД
  не откатывается: после ошибки/возврата читается фактический локальный профиль.
  Это не ACK устройства и не общая транзакция с импортом сервера/другими экранами.
  На этапе A5.3.2 создание и переименование оставались на прежнем пути.
  Проверка A5.3.2: assembleDebug и 187 app-тестов в 24 suites, 0 ошибок/пропусков;
  XML 2026-09-11 08:56:39–42 UTC, лог `/tmp/ubi4-special-settings-a5-3-2-20260911.log`.
  Добавлено 23 проверки: ID/повторный выбор, блокировки, отмена таймеров, ошибка/повтор,
  реальный KMM-менеджер с задержанными сохранениями, другой serial, отмена ожидания,
  поздний ответ БД и сохранение прежнего набора значений для applier.
  Android view/устройство и iOS-сборка не проверялись. Прежняя проблема запуска
  shared-тестов остаётся отложенной; изменённый менеджер проверен app-тестами.
  A5.3.3 выполнен: «+» передаёт SettingsProfileCreateRequested через Fragment
  в ViewModel → CreateSettingsProfileUseCaseV3 → V3SettingsProfilesRepository.createProfile.
  UseCase проверяет serial и предел 3; data повторно читает состав после ожидания
  сохранений и проверяет устройство перед созданием. Общий путь выбора/создания
  отменяет таймеры параметров экрана, блокирует повторные действия, обновляет
  локальный индекс, вызывает прежний applier и инвалидирует список. UiState содержит
  operation/failedOperation (SELECT или CREATE); отмена и ошибка не вызывают автоповтор.
  В shared сохранены снимок BLE-значений и копирование BLE/мобильных значений активного
  профиля. Исправлен выбор свободного ID: импорт [1, 3] → новый 2 вместо недопустимого 4.
  Парсеры, iOS-файлы, UBI4 UI и Gradle-модули не менялись. Видимость сохранена:
  STANDARD_V3 скрывает виджет (SETTINGS_PROFILE_WIDGET_ENABLED=false), INDY3 показывает.
  Пользователь подтвердил обычный V3: отсутствие виджета соответствует этому флагу.
  Проверка A5.3.3: assembleDebug и 215 app-тестов в 25 suites, 0 ошибок/пропусков;
  XML 2026-09-11 09:13:13–16 UTC, лог `/tmp/ubi4-special-settings-a5-3-3-20260911.log`.
  Проверены копирование реальным KMM repository, пропуск в ID после импорта,
  предел, ожидание сохранений, двойное нажатие, блокировки, отмена и ошибки.
  Устройство/iOS-сборка не проверялись; shared-тесты остаются отложенными.
  На этапе A5.3.3 далее оставалось переименование.
  A5.3.4 выполнен: адаптер передаёт SettingsProfileRenameRequested(profileId),
  ViewModel открывает nameEditor в UiState, SpecialSettingsFragment показывает
  существующий Compose-диалог. Ввод текста остаётся локальным состоянием диалога;
  повторный render не пересоздаёт его. requestId защищает от старых save/dismiss.
  STOP, смена устройства/serial/раздела, блокировка и перечитывание профилей закрывают
  диалог. SettingsProfileNameSubmitted → RenameSettingsProfileUseCaseV3 проверяет
  serial/ID/наличие профиля и V3SettingsProfileNameRules: trim, 1–30 символов.
  Data сохраняет имя через прежний KMM repository и инвалидирует список, без
  смены активного профиля, применения значений и BLE-записи. RENAME отражается
  в operation/failedOperation; повторные профильные действия блокируются.
  Таймеры параметров при переименовании не отменяются: активный профиль не меняется.
  Ошибка/отмена не запускает автоповтор, итоговое имя перечитывается из БД.
  Проверка A5.3.4: assembleDebug и 252 app-теста в 25 suites, 0 ошибок/пропусков;
  XML 2026-09-11 09:25:26–31 UTC, лог `/tmp/ubi4-special-settings-a5-3-4-20260911.log`.
  Проверены ID/имя, сохранение значений реальным KMM repository, отсутствие apply,
  stale callback, lifecycle, ошибки/повтор и сохранение запланированных Slider-записей.
  Shared, парсеры, iOS-файлы и видимость виджетов в этом шаге не менялись.
  A5 завершён в коде; Android view/телефон и iOS-сборка не проверялись.
  Общая презентация ошибок и завершение экрана остаются A6.
- [x] A6. Включить доступность, загрузку и значимые ошибки в состояние SpecialSettings.
  Завершить чтение/сохранение выбранной вкладки и состояние «Автоматический вход»
  через контракт настроек приложения, сохраняя ключи, локальное сохранение и
  отсутствие BLE-записи. Выбранная вкладка уже присутствует в UiState с A2.
  Удалить его заменённые прямые подписки/обработчики; проверить полный экран.
  A6.1 выполнен: «Автоматический вход» подключён к autoLogin в UiState и
  AutoLoginChanged → ViewModel → SetAutoLoginEnabledUseCaseV3 → V3AppSettingsRepository.
  Android data использует прежние APP_PREFERENCES/SET_MODE_SMART_CONNECTION,
  default=false и apply(). Чтение, пользовательская запись и внешние изменения
  наблюдаются без Activity в data/domain. Listener SharedPreferences живёт только
  во время активной подписки; STOP/уход из V3/clear ViewModel освобождают его.
  Сохраняется фактический путь мобильного SwitchItem: только настройка приложения,
  без BLE и без добавления записи в профиль. BLE-блокировка не отключает эту настройку.
  AutoLoginDelegateAdapterV3 отображает типизированный Switch из экранного списка
  в прежнем XML; render/rebind не сохраняют значение. UBI4 и остальные Switcher-адаптеры
  остаются на прежнем пути. Источник подписей/состава DataFactory не менялся.
  Ошибки чтения/записи отражены в readFailed/saveFailed; общая презентация ошибок
  остаётся следующим шагам A6. Повторное действие с тем же значением не пишет preferences.
  Проверка A6.1: assembleDebug и 266 app-тестов в 28 suites, 0 ошибок/пропусков;
  XML 2026-09-11 10:42:53–59 UTC, лог `/tmp/ubi4-special-settings-a6-1-20260911.log`.
  Проверены оба профиля V3, работа при BLE-блокировке, внешние изменения/возврат,
  listener lifecycle, ошибки/повтор, отсутствие записи при render и старых callback.
  Shared, парсеры, iOS, XML/Compose и Gradle-модули в этом шаге не менялись.
  На телефоне не проверено.
  A6.2 выполнен: выбранная вкладка восстанавливается ViewModel через
  V3AppSettingsRepository до первого снимка widgets и при ViewAttached.
  SettingsSectionSelected → SetSpecialSettingsSectionUseCaseV3 → repository
  сохраняет прежний LAST_ACTIVE_SETTINGS_FILTER: false — PROSTHESIS, true — APPLICATION.
  V3SpecialSettingsSection находится в domain.appsettings; Android preferences — в data.
  Повторный выбор не пишет то же значение и не обновляет состав/черновики без необходимости.
  Fragment только передаёт действие и отображает selectedSection, включая совместимый
  activeSettingsFragmentFilterFlow (1/2). Прямое чтение/сохранение осталось только в UBI4.
  При ошибке начального чтения показывается PROSTHESIS без перезаписи предпочтения;
  при ошибке позднего чтения или сохранения остаётся текущая вкладка. Ошибки отражены
  в settingsSectionReadFailed/settingsSectionSaveFailed; отображение остаётся A6.3.
  Проверка A6.2: assembleDebug и 276 app-тестов в 28 suites, 0 ошибок/пропусков;
  XML 2026-09-11 10:53:43–49 UTC, лог `/tmp/ubi4-special-settings-a6-2-20260911.log`.
  Проверены восстановление обеих вкладок, возврат/внешнее изменение, ошибки/повтор,
  игнорирование callback при STOP/NOT_V3 и независимость от autoLogin/BLE-блокировки.
  Shared, парсеры, iOS, XML/Compose и Gradle-модули в A6.2 не менялись.
  На телефоне не проверено. Далее A6.3 — загрузка и значимые ошибки экрана.

  A6.3 завершён в коде 2026-09-11 с сохранением прежнего UI. У Spinner профилей
  удалён второй путь доступности и наблюдения: режим settingsProfilesFromState
  не читает preferences и не подписывается на ParameterStoreV3/глобальный BLE-допуск.
  Показ, выбор, «+» и редактирование используют экранный state/callback; прежние
  ветки Spinner остальных экранов сохраняются. Recycle/rebind/destroy освобождают
  обработчики и listener закрытия popup. Slider также освобождает строки при recycle;
  запоздалые callback проверяются по экземпляру привязки, а не по равенству её полей.
  SpecialSettings отсоединяет RecyclerView от адаптера при destroyView.
  Политика существующих анимаций проходит source → UiState.animationsEnabled → UI;
  Base сохраняет прежний источник для остальных экранов и UBI4.
  Загрузка/ошибки профилей и autoLogin уже были в UiState. Новых сообщений, кнопок,
  индикаторов, повторов операций и копии Activity-синхронизации не добавлено.
  Debug-сборка и 282 app-теста в 30 suites прошли, 0 ошибок/пропусков;
  XML 2026-09-11 11:37:40–45 UTC, лог `/tmp/ubi4-before-sensors-20260911.log`.
  Release-сборка прошла: `/tmp/ubi4-before-sensors-release-20260911.log`.
  Парсеры, shared, iOS, XML/Compose, Gradle-модули и SensorsFragment не изменены.
  Проверка телефона: Samsung SM-A065F, реальный обычный V3 (подтверждено пользователем).
  Release APK с совпадающей подписью установлен через install -r, данные сохранены.
  После запуска открылся рабочий экран, зависания синхронизации не наблюдалось.
  Последняя вкладка APPLICATION и включённый autoLogin восстановились; после ухода
  в фон/возврата к прежнему MainActivity состояние сохранилось. Отдельный запуск
  SplashScreen из adb открывал сканер поверх MainActivity; возврат Back восстановил
  существующий экран. Это не проверка поведения обычного нажатия иконки лаунчера.
  Обе вкладки сравнены до/после: у протеза совпали 121 узел UI по тексту, значениям,
  enabled/clickable/checked и bounds; у мобильной вкладки совпали элементы и autoLogin.
  Оба обычных Spinner раскрываются с прежними вариантами, выбор не изменялся.
  Телефон возвращён на PROSTHESIS, параметры протеза и переключатели не менялись.
  Снимки проверки: `/tmp/v3-before-sensors-*.xml`, `/tmp/v3-before-sensors-ui-comparison.txt`.
  Проверка записей реальным протезом/физическим INDY3 остаётся ручной; INDY3 и
  профильные операции покрыты app-тестами, скрытый виджет обычного V3 не включали.


A1–A3 могут требовать отдельных небольших запросов. Во время технического A3
допускается сохранение прежних обработчиков для ещё не перенесённых типов;
весь экран не объявляется готовым до A6. Не переносим ToggleSlider на других
экранах вместо завершения текущего этапа.

### Этап B. Завершить SensorsFragment, затем ServiceFragment

- [x] B1. Sensors: состояние всего экрана, сохранение двух Slider, Plot/пороги
  и кнопки открытия/закрытия; отдельные шаги для потока графика и команд.
  - [x] B1.1. Сверить состав STANDARD_V3/INDY3 и подключить V3SensorsViewModel:
    список виджетов и оба Slider чувствительности через UiState/Action,
    существующие V3SliderSettingsController, SetSliderValueUseCaseV3 и репозиторий.
    Подключение через bindV3SliderSettings для Sensors удалено. Состав пока
    поступает через DataFactoryV3SensorsWidgetsSource, без изменения shared.
  - [x] B1.2. Plot: поток значений и пороги через screen state и domain/data;
    формат графика, жесты порогов и существующие команды сохраняются.
    Реализованы V3SensorsPlotRepository, EditPlotThresholdUseCaseV3,
    SetPlotThresholdsUseCaseV3 и V3SensorsPlotController внутри экранной ViewModel.
    PlotDelegateAdapterV3 получает V3PlotUiState и передаёт V3PlotAction;
    в нём нет BLE, store, Activity, coroutine-подписок или таймера данных.
  - [x] B1.3. Кнопки открытия/закрытия и граница обновления Sensors:
    SensorsButtonsDelegateAdapterV3 передаёт действия экранной ViewModel,
    StartProsthesisMovementUseCaseV3/StopProsthesisMovementUseCaseV3 и
    RefreshSensorsUseCaseV3 используют V3SensorsCommandsRepository.
    DOWN/UP/CANCEL, прежние пакеты и окно синхронизации сохранены;
    принятые нажатия завершаются STOP при блокировке и уходе с экрана.
    ButtonsDelegateAdapterV3 для калибровки Service пока остаётся прежним.
  - [x] B1.4. Проверены оставшиеся обращения Sensors/делегатов к глобальным
    данным и BLE. Исправлена обработка поздних действий после очистки ViewModel.
    [Карта границ и остаточных связей](v3_sensors_architecture_audit.md) фиксирует
    мосты до C4/D и отдельный путь UBI4. B1 завершён в этих границах.
- [ ] B2. Service: состояние всего экрана, Slider положений пальцев там, где они
  присутствуют, Spinner, ввод имени/серийного номера, калибровка и существующий
  вход в BLE Log; сохранить роли и различия профилей.
  - [x] B2.1. Собственная V3ServiceViewModel, UiState/Action, типизированный состав
    и подключение уже готовых Slider. Проверены реальные 9 строк STANDARD_V3 и
    6 строк INDY3 в ru/en; у INDY3 нет Slider положений пальцев и стороны руки.
    [Контракт состояния](v3_service_state_contract.md). Вторая Slider ViewModel
    через Base для Service больше не создаётся.
  - [x] B2.2. Spinner «Режим работы ЕМГ» и «Сторона руки» через состояние Service
    и готовую цепочку domain/data. UiState хранит только присутствующие ключи;
    у INDY3 выбора стороны нет. Диапазоны 0–3/0–1 проверяет domain, пакеты и
    порядок записи сохранены. 385 app-тестов, debug/release успешны.
  - [x] B2.3. Особый сценарий Spinner «Роль»: выбранная роль, PIN и права доступа
    через состояние Service и domain/data с прежним XML-диалогом. В адаптере
    удалены проверка PIN и сохранение роли; у обоих профилей прежние две роли.
    411 app-тестов, debug/release успешны.
  - [x] B2.4. TextInput «Имя протеза» и «Серийный номер»: ввод и запись через
    состояние Service и domain/data с прежними ограничениями и командами.
    Адаптер только отображает ввод и передаёт действия. 435 app-тестов,
    debug/release успешны; правила имени, TEXT-серийный номер и SET → GET сохранены.
  - [ ] B2.5. Калибровка протеза: действие экрана через domain/data с прежней
    командой, доступностью и отображением. Затем проверить остаточные связи
    Service и существующий вход в журнал BLE перед закрытием B2.
- [ ] B3. Проверить удаление bindV3SliderSettings и v3SliderParameterKeys после
  последнего потребителя; если разделение было завершено в A2, повторного переноса нет.
  Общая бизнес-логика и data-реализации не копируются между экранами.

### Этап C. Остальные сценарии V3

- [ ] C1. Gestures: список, выбор/редактирование, группы и доступные действия.
  Отложенный вопрос BindingGroup рассматривается отдельно, если действительно
  нужен изменяемому сценарию; не исправляется автоматически.
- [ ] C2. Остальные используемые V3-сценарии аккаунта/профилей:
  границы серверных и локальных данных, отдельные экранные состояния.
  SpecialSettings завершается в A, повторного переноса в C нет.
- [ ] C3. SprGesture/SprTraining и обучение: разнести UI, длительные операции,
  данные и управление устройством по сценариям.
- [ ] C4. Навигация и оставшаяся координация Activity/BaseWidgetsFragment;
  для V3 выделить обязанности очереди, подключения и синхронизации BLEController
  за контрактами, сохраняя проверенный порядок и протокол.
- [ ] C5. Прошивка и 3D — последними отдельными этапами. Сначала карта состояний,
  восстановления/отмены и границ, затем небольшие изменения с их проверками.

Перед каждым C-этапом уточняем его фактический состав по коду. Это детализация
выбранного сценария, а не смена порядка или переход к другой версии.

### Этап D. Привести общую KMM-часть V3 к целевым границам

- [ ] D1. Перенести выделенный платформенно независимый domain в shared без
  изменения package/API и поведения; app подключает его существующей зависимостью.
- [ ] D2. Изолировать stores/Room/network и входящие данные за контрактами;
  выделить подготовку общих UI-описаний из смешанных обязанностей DataFactory.
  Старые iOS API делегируют либо сохраняют совместимый путь.
- [ ] D3. Проверить Android, Kotlin/Native и используемые iOS-контракты без правок
  iOS-кода. Общие парсеры и генераторы не изменяются. Удалять можно лишь то,
  что действительно не используется ни одной платформой.

Совместимость обязательна; отсутствие возможности проверить конкретную платформу
фиксируется как незакрытая проверка, а не как успешный результат.
Долг публичных старых API можно изолировать, но их полное удаление вне этого плана.

### Этап E. Завершить V3, затем перейти к UBI4

- [ ] E1. Проверить целые экраны, соединение/повторное подключение, сохранённые
  настройки и lifecycle на STANDARD_V3 и INDY3. Составить остаточный список проблем.
- [ ] E2. После V3 переносить UBI4 по экранам в том же порядке:
  SpecialSettings → Sensors → Service → остальные применимые сценарии.
  Динамические описания и восстановление CRC-кеша сохраняются.
- [ ] E3. Финальная проверка разделения версий, KMM-совместимости и удаление
  заменённых Android-путей. Оставшиеся совместимые фасады документируются.

## 8. Завершение небольшого шага

В отчёте указываем номер этапа, что изменилось, какие прежние пути заменены,
результат подходящей Android-сборки/тестов и что осталось проверить на устройстве.
Обычный набор: `:app:assembleDebug` и app-тесты затронутой области.
Тесты проверяют поведение и границы: пакеты, сохранение, состав экрана, lifecycle;
не добавляем тесты ради переименований или повторения реализации.

Отдельно проверяем:
- неизменность состава/порядка/подписей/значений для соответствующего профиля;
- отсутствие команд из чтения, render и повторного открытия;
- сохранение соседних полей общей команды, блокировки, отмену отложенных записей;
- сохранение кеша и профиля, отсутствие изменений защищённых областей.

Проверка на устройстве проводится при наличии доступа; непроверенный UI/BLE
не объявляем готовым к выпуску. Unit-тесты не подтверждают реальные действия BLE.
Недоступность устройства не запрещает продолжать согласованную работу с кодом.

## 9. Управление изменениями плана

Уточнение 2026-09-09 после проверки навигации с пользователем: первым экраном
ошибочно был назван AdvancedFragment. Фактический экран — SpecialSettingsFragment.
Удержание accountBtn переключает видимость page_secret, который открывает
ServiceFragment. Восстанавливаем привязку Slider к рабочему экрану в A1.1 и
учитываем обе вкладки SpecialSettings в A2–A6. Это исправление исходной карты;
порядок V3 → UBI4, слои и постепенное завершение экранов сохраняются.

Эта версия заменяет прежний общий порядок «сначала все типы виджетов».
Готовая работа со Slider сохраняется и становится частью этапов экранов.

Без отдельного согласования не меняем:
- порядок версий, архитектурные границы и платформенные ограничения;
- единицу завершения «целый экран»;
- правила владения состоянием, источник бизнес-ограничений;
- протоколы и публичные API KMM/iOS.

Находки по коду уточняют задачи внутри текущего этапа. Если находка требует
изменения одного из этих решений, сначала описываем причину, варианты и влияние,
обновляем план после согласования. Не переписываем соседнюю часть проекта молча.

Перед очередным запросом сверяемся с этим файлом и выбираем ближайший незакрытый
пункт. Исторические документы и прежние сообщения не задают конкурирующий порядок.

## Следующий шаг

**B2.5: «Калибровка протеза» в Service V3.**
Подключить действие к экранной ViewModel и domain/data, сохранив прежние команду,
доступность и UI. После этого проверить остаточные связи Service; весь B2
не объявляется завершённым до этой проверки.

B2.1–B2.4 выполнены: экранная ViewModel управляет составом, Slider, обычными
Spinner, выбором роли с PIN и текстовыми полями имени/серийного номера.
[Контракт Service](v3_service_state_contract.md) фиксирует границы и
проверки: 435 app-тестов, debug/release успешны. Остальные обработчики пока
прежние, поэтому B2 ещё не закрыт. Domain/data и адаптеры переиспользованы.

### Проверка предыдущего шага B1.4

B1.4 завершён: [проверка границ Sensors](v3_sensors_architecture_audit.md).
Слайдеры, Plot/пороги, кнопки и обновление управляются состоянием экранной
ViewModel. Общая синхронизация/навигация, DataFactory и внутреннее устройство
shared остаются в C4/D. В B1.4 исправлена возможность отправки Slider из
запоздалого callback после очистки ViewModel; новая проверка сначала воспроизвела
ошибку. Подробности сборки и проверки приведены в карте границ.

### Проверка предыдущего шага B1.3

B1.3 выполнен: состояние кнопок находится в V3SensorsUiState.buttons.
Адаптер передаёт нажатие/отпускание с идентификатором жеста через Fragment в
ViewModel. Принятое нажатие вызывает прежнюю OPEN/CLOSE, отпускание или CANCEL —
STOP. Блокировка, уход с экрана, удаление кнопок и очистка ViewModel также
завершают принятые нажатия; устаревший callback не завершает более новый жест.
Repository проверяет текущий адрес перед постановкой команды в существующую
очередь. STOP прежнего устройства не направляется новому. Саму очередь и её
механизм подключения этот шаг не меняет; это остаётся границей этапа C4.

RefreshRequested проходит через ViewModel → RefreshSensorsUseCaseV3 →
V3SensorsCommandsRepositoryImpl. Сохранён прежний порядок: fullInitInProgress,
окно синхронизации Activity, refreshWidgetsV3BySwipe в BLEController. Этот
контроллер по-прежнему владеет завершением и существующими повторами; новые
повторы не добавлены. Пока fullInitInProgress=true, второй запуск отклоняется.
Событие состава завершает только локальную анимацию свайпа. Повторный показ
состояния не запускает команды. Ветка UBI4 использует прежний refreshWidgetsList.

Кнопки Sensors выделены в пассивный SensorsButtonsDelegateAdapterV3; он
подключён перед ButtonsDelegateAdapterV3 и выбирает только display=1.
Служебная калибровка display=4 остаётся в прежнем адаптере до B2.
Названия, XML, расположение и количество кнопок не менялись.

Проверка B1.3 (2026-09-14): 344 app-теста в 39 классах, без ошибок и пропусков
(22 новых); :app:assembleDebug и :app:assembleRelease успешны. Лог:
`/tmp/ubi4-sensors-buttons-final-20260914.log`, timestamps XML:
`2026-09-14T10:00:39`–`2026-09-14T10:00:46` UTC. Проверены оба профиля,
реальные байты OPEN/CLOSE/STOP и CRC, release при блокировке, lifecycle,
повторные/устаревшие события, смена устройства и повторный refresh.
Отложенные shared-тесты привязок не запускались. Парсеры, shared, iOS,
запрещённые каталоги, XML/Compose и настройки Gradle не изменялись.

На Samsung SM-A065F с реальным обычным V3 установлен release APK поверх
прежнего приложения с сохранением данных. Начальная синхронизация завершилась.
Свайп Sensors вызвал прежнее окно и повторную синхронизацию: это подтверждено
logcat (SyncProgressDialog и запрос BLE в 15:04:31 по времени телефона).
После обновления и после перехода в SpecialSettings и обратно все 60 узлов
списка Sensors совпали с исходной сборкой по текстам, значениям, доступности,
кликабельности и координатам: чувствительность 41/62, пороги 158/109.
Отчёт: `/tmp/sensors-b1-3-ui-comparison.txt`; снимки UI и logcat имеют префикс
`/tmp/sensors-b1-3-`. Кнопки OPEN/CLOSE и изменение параметров на реальном
устройстве не проверялись; их команды проверены тестами с подставной очередью.
Физического INDY3 в этой проверке не было.

### Проверка предыдущего шага B1.2

На B1.2 Plot подключён к V3SensorsUiState.plot. Путь входящих данных:
V3SensorsPlotRepositoryImpl → domain-контракт V3SensorsPlotRepository →
V3SensorsPlotController в V3SensorsViewModel → SensorsFragment → PlotDelegateAdapterV3.
Путь жестов: адаптер → V3PlotAction → экранная ViewModel → EditPlotThresholdUseCaseV3
для черновика; при ACTION_UP — SetPlotThresholdsUseCaseV3 → Repository.

Пороги представлены неизменяемой парой V3PlotThresholds. Изменение одного
сохраняет второе; границы 0–255 проверяет domain. DOWN/MOVE меняют черновик,
UP отправляет одну прежнюю команду с обоими порогами; CANCEL не отправляет.
Data сохраняет прежний порядок: BLE-очередь → ParameterStoreV3 → профиль →
сериализованный кеш. При блокировке управления показаны нули, при разблокировке
восстанавливается кеш. Повторное отображение и получение данных не вызывают запись.

График сохраняет две кривые V3/INDY3, шесть каналов в модели, шаг 25 мс,
сглаживание за три шага, прежнее окно точек, цвета и оси. Формирование кадров
находится в presentation; frame.sequence позволяет продолжать график при
неизменных показаниях и исключает повторную вставку кадра при обновлении порога.
При паузе перехода кадры не добавляются. При STOP/смене устройства/исчезновении
Plot подписки и таймер отменяются, черновик сбрасывается. Работа с Android View
выполняется на Main; recycle/rebind/destroy освобождают жесты и анимации.
Частые кадры не вызывают повторное отображение Slider или перестроение списка.

На момент завершения B1.2 Buttons и команда обновления оставались в прежних
адаптере/Base; их перенос выполнен в B1.3 выше. Состав виджетов пока приходит через
DataFactoryV3SensorsWidgetsSource — этот мост уйдёт на этапе работы с shared.
XML, Compose, UBI4, BLEParser/BLEParserV3, iOS и запрещённые каталоги не изменялись.

Проверка B1.2 (2026-09-14): 322 app-теста в 36 классах, без ошибок и пропусков
(21 новый сценарий); :app:assembleDebug и :app:assembleRelease успешны.
Лог: `/tmp/ubi4-sensors-plot-final-20260914.log`. XML timestamps:
`2026-09-14T09:26:59`–`2026-09-14T09:27:05` UTC. Проверены оба профиля,
жесты адаптера (UP/CANCEL, повторные и устаревшие callbacks), границы пары,
реальный кодек/пакет и порядок кеширования, входящие значения без записи,
пауза/сглаживание/постоянные показания и окно графика, STOP/смена устройства/
удаление Plot. Предыдущие проверки SpecialSettings и Slider также прошли.
Телефон не обнаружен через adb devices: APK не устанавливался, физические
пороги/график в этом шаге не проверялись. Проверка телефона от B1.1 ниже относится
только к предыдущей сборке. Отложенный shared-тест привязок не запускался.

### Проверка предыдущего шага B1.1

B1.1 выполнен: у Sensors собственная ViewModel, типизированный состав и состояние
двух Slider. Обычный V3 и INDY3 имеют одинаковые четыре строки: Plot,
чувствительность открытия, чувствительность закрытия, кнопки открытия/закрытия.
Внешний вид, XML и прежние обработчики Plot/Buttons сохранены.

Состав читается из DataFactory через presentation-адаптер; ViewModel не зависит
от Fragment, DataFactory, Android View или BLEController. Значения Slider
проходят через уже существующие domain/data. Повторный состав не сбрасывает
черновики; STOP lifecycle, смена устройства/набора Slider отменяют отложенные
записи. Флаг isRefreshIndicatorVisible относится только к прежней локальной
анимации свайпа: updateFlow завершает её, но не объявляет BLE готовым.
SyncProgressDialog и команда refreshWidgetsList пока остаются в Activity/Base.

Проверка B1.1: 301 app-тест в 32 классах, без ошибок и пропусков (19 новых);
:app:assembleDebug и :app:assembleRelease успешны. Лог:
`/tmp/ubi4-sensors-b1-1-verified-20260911.log`, timestamp XML:
`2026-09-11T12:31:30`–`2026-09-11T12:31:39` UTC.
Общий отложенный shared-тест привязок не запускался и не изменялся.

На подключённом Samsung SM-A065F с реальным обычным V3 установлен release APK
поверх прежней сборки с сохранением данных. Синхронизация завершилась,
Sensors открылся: все 60 узлов списка совпали до/после по текстам, значениям,
доступности, кликабельности и координатам. Переход в SpecialSettings и обратно
сохранил тот же Sensors (18/62, пороги 158/109); список SpecialSettings также
совпал. Отчёт: `/tmp/v3-sensors-b1-1-ui-comparison.txt`.
При этой проверке значения Slider/порогов и кнопки открытия/закрытия не трогали;
запись физических параметров и INDY3 на телефоне на этом шаге не проверялись.

## Исторический базис

Следующий раздел описывает начальную проверку до изменений, а не текущий статус.

### Исходная проверка 2026-09-08

До начала работы в рабочем дереве уже были изменения `app/build.gradle.kts`
(версия приложения `3.3.1827`), release-артефактов, служебных файлов `.gradle`
и `tools/__pycache__`. Они не относятся к этой миграции и сохранены.

Выполнено без `clean`, без release-сборки и без запуска приложения на устройстве:

```sh
./gradlew :app:assembleDebug \
  :app:testDebugUnitTest --tests 'com.bailout.stickk.ubi4.*' \
  :shared:testDebugUnitTest --tests 'com.bailout.stickk.ubi4.*' \
  --continue --console=plain
```

| Проверка | Результат |
| --- | --- |
| `:app:assembleDebug` | Успешно, создан `app/build/outputs/apk/debug/app-debug.apk` |
| `:app:testDebugUnitTest`, фильтр UBI4 | 33 теста в 9 классах; 0 ошибок, 0 падений, 0 пропусков |
| `:shared:compileDebugUnitTestKotlinAndroid` | Ошибка компиляции существующего теста |
| `:shared:testDebugUnitTest` | Не выполнен из-за ошибки компиляции |
| Итог объединённой команды | `BUILD FAILED`, exit code 1; продолжение позволило независимо завершить app-сборку и app-тесты |
| Инструментальные тесты, реальный BLE, iOS | Не запускались |

Причина ошибки:

```text
shared/src/androidUnitTest/kotlin/com/bailout/stickk/ubi4/data/parser/BindingGroupV3Test.kt:99:41
Unresolved reference 'BINDING_GROUP_FLOW'.
```

Тест ожидает `WidgetEmitTargetV3.BINDING_GROUP_FLOW`, но этот enum-элемент и
соответствующий маршрут закомментированы в `WidgetResponseRoutesV3.kt`.
Это исходное рассогласование теста и реализации, обнаруженное до рефакторинга.
Нельзя считать установленным, что достаточно переименовать константу.
Исправление отложено пользователем; действующий путь привязок в рамках
следующих шагов миграции скорости не меняем.

Отчёты свежего app-запуска: `app/build/test-results/testDebugUnitTest/TEST-*.xml`.
Отчёты `shared/build/test-results/testDebugUnitTest` с датой 2026-08-18 устарели
и не являются результатом этой проверки.

В составе подготовки код приложения, Gradle-конфигурация и тесты не редактировались.
Сравнение состояния Git до и после запуска не выявило новых изменений этих файлов
или защищённых областей; добавлен только этот документ.
