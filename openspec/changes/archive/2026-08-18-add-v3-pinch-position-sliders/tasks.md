## 1. Общий V3-контракт параметров

- [x] 1.1 Добавить в `ProsthesisModuleControlEnum` команды thumb `SET 0x44`/`GET 0x45` и finger `SET 0x46`/`GET 0x47`, а в доменном слое — два нейтральных глобальных ключа закрытого положения без привязки к жесту.
- [x] 1.2 Зарегистрировать оба master-параметра как независимые `SLIDER` metadata с scalar codec, нулевым offset и соответствующими SET-командами.
- [x] 1.3 Добавить общие русские и английские строки «Закрытое положение большого пальца» и «Закрытое положение указательного/среднего пальцев» без единицы измерения и упоминания `PINCH`/«Щёпоть».

## 2. Чтение, запись и состояние

- [x] 2.1 Расширить `WidgetCommandBridgeV3`: сопоставить каждую SET-команду с её GET-запросом и нормализовать GET metadata обратно в правильную SET-команду при записи.
- [x] 2.2 Добавить в `WidgetResponseRoutesV3` независимые маршруты ответов `0x45` и `0x47` через slider codec/store/update flow.
- [x] 2.3 Покрыть unit-тестами точные короткие BLE-пакеты с CRC для GET и SET значений `0`, промежуточного значения и `100`, разбор однобайтового payload и отсутствие перекрёстного обновления параметров.
- [x] 2.4 Закрепить тестами глобальную модель состояния: ключи параметров не содержат gesture id, а смена активного жеста не создаёт и не выбирает отдельные значения.

## 3. Служебные виджеты и внешний вид

- [x] 3.1 Добавить два `SliderParameterWidgetSStruct` только в `generatedHardcodeWidgets()` standard V3 на `display = 4` с диапазоном `0…100`, шагом `1` и без `unitLabel`, переиспользовав существующие Android/iOS V3 slider cells.
- [x] 3.2 Проверить порядок в «Служебных настройках» и корректный перенос утверждённых русских/английских заголовков на поддерживаемых размерах Android и iOS.
- [x] 3.3 Расширить parser/widget tests: standard V3 публикует в служебном наборе оба уникальных глобальных слайдера с правильными metadata и границами, а INDY3 и legacy не публикуют их.

## 4. Начальная синхронизация платформ

- [x] 4.1 Добавить GET `0x45` и `0x47` с ожидаемыми response keys в standard V3 init Android `BLEController` и учёт общего прогресса синхронизации.
- [x] 4.2 Добавить эквивалентные запросы и response keys в standard V3 init iOS `BleManagerKmm`.
- [x] 4.3 Добавить parity/regression tests состава init: обе платформы запрашивают параметры standard V3, а обе INDY3-последовательности остаются без `0x45`/`0x47` и с прежним числом поддерживаемых запросов.
- [ ] 4.4 Расширить V3 BLE-эмулятор/debug hooks ответами и хранением двух независимых значений, чтобы обе платформы можно было проверить без реального протеза.

## 5. Профили настроек и платформенное поведение

- [x] 5.1 Подтвердить unit-тестом round-trip обоих глобальных slider values через существующий `SettingsProfileRepository`: сохранение, переключение профиля и восстановление правильных metadata.
- [ ] 5.2 Добавить Android UI/adapter test для размещения в служебном разделе, утверждённых заголовков, начального значения, границ, шага, отсутствия `%` и отправки `0x44`/`0x46` только после стандартного commit/debounce.
- [ ] 5.3 Добавить эквивалентный iOS ViewModel/cell test для служебного раздела, отображения, обновления из общего store и формирования правильной SET-команды.
- [x] 5.4 Добавить regression test, подтверждающий, что переключение между жестами не меняет два значения UI/store и не создаёт gesture-specific metadata.

## 6. Проверка интеграции

- [x] 6.1 Запустить общие и Android unit-тесты, собрать Android debug-вариант и устранить найденные регрессии V3/INDY3.
- [ ] 6.2 Запустить целевые iOS unit/UI-тесты и simulator build, проверить одинаковое размещение в служебном разделе, заголовки, диапазоны и команды с Android.
- [ ] 6.3 На актуальном standard V3 либо согласованном BLE-эмуляторе проверить GET/SET для `0`, промежуточного значения и `100`, подтвердить однобайтовый payload по BLE-логу, сохранение значений при переключении нескольких жестов и повторное чтение после переподключения.
