# Первый сценарий: «Настройка скорости» V3

Дата анализа: 2026-09-08. Связанный [план миграции](ubi4_v3_clean_architecture_plan.md).

Этот файл хранит историю реализации и проверок Slider. Действующая стратегия,
порядок завершения экранов и следующий шаг определяются только связанным планом.

Текущее состояние: все 7 параметров обычного `SliderItemV3` подключены к MVVM/MVI:
скорость, сила, максимальная чувствительность датчиков, чувствительность открытия
и закрытия, закрытое положение большого пальца и указательного/среднего пальцев.
Общий `V3SliderSettingsController` хранит `sliders: Map<String, SliderUiStateV3>`
и отдельную отложенную запись для каждого ключа выбранного экрана. В SpecialSettings
связь с V3SpecialSettingsViewModel находится в самом Fragment; остальные
потребители используют V3SliderSettingsViewModel через BaseWidgetsFragment.
Диапазоны определяет V3SliderSettingsRules в domain.
Ниже сохранены исходная карта и история шагов;
актуальное расширение описано в последнем разделе.

Уточнение 2026-09-09: настройки из нижнего меню открываются в
`SpecialSettingsFragment`, а не в `AdvancedFragment`. Пропущенная привязка трёх
Slider на рабочем экране исправлена в A1.1: скорость, сила, максимальная
чувствительность. Android-сборка и 68 app-тестов прошли (13:14:38–39 UTC,
`/tmp/ubi4-special-settings-slider-20260909.log`); фактические действия на телефоне
не проверены. После этого выполнен A2: отдельный владелец SpecialSettings,
нейтральные имена общей Slider-логики и отмена ожидающей записи при выборе вкладки
приложения. Android-сборка и 77 app-тестов прошли (13:30:31–32 UTC,
`/tmp/ubi4-special-settings-a2-20260909.log`). В A3 типизированный состав двух
вкладок и deviceProfile подключены к экранному состоянию через presentation-источник
DataFactory и преобразователь; UBI4 сохраняет прежний путь. Сборка и 91 app-тест
прошли (14:01:12–13 UTC, `/tmp/ubi4-special-settings-a3-20260909.log`).
Полное состояние и оставшиеся обработчики — A4–A6;
исторические названия классов далее описывают реализацию на момент тех шагов.

A4.1: в SpecialSettings подключён ToggleSlider «Блокировка движения с ЕМГ»
для V3/INDY3 через экранное состояние, domain UseCase и существующую реализацию
repository. Его локальное сохранение остаётся немедленным, отправка — через
300 мс; таймер отменяется при уходе/блокировке. Сборка и 111 app-тестов прошли
(2026-09-09 14:25:18–19 UTC, `/tmp/ubi4-special-settings-a4-20260909.log`).
В A4.2 через те же классы подключены «Переключение жестов сенсорами» и
«Время работы экрана». Сборка и 118 app-тестов прошли (2026-09-09 14:40:24 UTC,
`/tmp/ubi4-special-settings-a4-2-20260909.log`). Проверены команды дополнительных
параметров, независимость трёх таймеров и ограничение состава INDY3.
ToggleSlider SpecialSettings завершены; весь экран ещё нет. Далее — обычные Spinner в A5.

Первоначально выполнен анализ исходного кода без изменений приложения.
Ниже сохранена карта исходного поведения; результат последующего выделения
репозитория и подключения MVVM описан в разделах шагов 3 и 4. Команды реальному устройству
в рамках этих шагов не отправлялись.

## Область

Выбран `P_KEY_SPEED_SETTINGS`: один числовой слайдер «Настройка скорости»
в `AdvancedFragment` (`display = 2`). Он задаётся приложением как для
стандартного V3, так и для INDY3. В отличие от усиления пары EMG-каналов и
toggle-slider, этот сценарий работает с одним целым значением.

Существующие имена участка: `SliderDelegateAdapterV3` → `SliderItemV3` →
`SliderParameterWidgetSStruct`, ключ параметра — `P_KEY_SPEED_SETTINGS`.
В плане и новом коде сохраняем терминологию Slider; скорость обозначает
конкретный параметр этого виджета, а не новый тип виджета.

Динамическая UBI4, остальные V3-параметры, XML и генерация виджетов внутри
`BLEParserV3` сохраняются. Извлечение касается только Android-логики вокруг
параметра скорости, а не всего `SliderDelegateAdapterV3` за один шаг.

## Метаданные и отображение

Источники:

- `shared/src/commonMain/kotlin/com/bailout/stickk/ubi4/data/parser/BLEParserV3.kt`: `generatedHardcodeWidgets()`, `generatedHardcodeWidgetsINDY3()`, `parseWidgets()`.
- `shared/src/commonMain/kotlin/com/bailout/stickk/ubi4/persistence/preference/PreferenceKeysUbi4.kt`: `ParameterInfoRegistry`, `ProsthesisModuleControlEnum`.
- `shared/src/commonMain/kotlin/com/bailout/stickk/ubi4/data/widget/endStructures/SliderParameterWidgetSStruct.kt`.
- `app/src/main/java/com/bailout/stickk/ubi4/adapters/widgetDelegateAdaptersV3/SliderDelegateAdapterV3.kt`: `onBind()`.

| Свойство | Текущее значение |
| --- | --- |
| Ключ | `P_KEY_SPEED_SETTINGS` |
| Группа команд | `PROSTHESIS_MODULE_CONTROL = 0x0F` |
| SET / GET | `PWCE_SET_SPEED_SETTINGS = 0x3D` / `PWCE_GET_SPEED_SETTINGS = 0x3E` |
| ParameterInfo | `(parameterID=0x0F, dataCode=0x3D, deviceAddress=1, dataOffsets=0)` |
| Codec / виджет | `SLIDER` / `SLIDER` |
| Поле сериализованного значения | `sliderValue` |
| Экран | `AdvancedFragment`, `display = 2` |
| Диапазон UI | `0..100`, шаг `1` |

Диапазон получается из текущего поведения: генератор создаёт базовую структуру,
парсер оборачивает её в `SliderParameterWidgetSStruct` с `minProgress = maxProgress = 0`,
а адаптер трактует равные границы как диапазон до 100. Это диапазон UI по коду;
отдельная спецификация допустимых аппаратных значений здесь не проверялась.

## Чтение и обновление

1. `BLEController.initRequestsV3()` выбирает список запросов по профилю.
   И `buildV3InitRequests()`, и `buildINDY3InitRequests()` включают GET скорости.
2. `BLECommandsV3.request(0x3E)` формирует пакет `00 0F 3E 00 CRC`.
   Используется существующее вычисление CRC, без новой реализации.
3. Входящие данные поступают через `BLEController.parseReceivedDataV3()` в
   `BLEParserV3.parseReceivedData()`.
4. `WidgetResponseRoutesV3` сопоставляет ответ `0x0F/0x3E` с `P_KEY_SPEED_SETTINGS`
   и `SLIDER_FLOW`.
5. `ParameterCodecRegistryV3.decodeFromPayload(SLIDER, ...)` читает `payload.u8(1)`
   и создаёт `ParameterTypedValueV3.Slider`.
6. `BLEParserV3.handleWidgetRoute()` записывает значение в `ParameterStoreV3`,
   сериализует его в `ParameterProvider`, публикует соответствующее событие.
7. Android-слайдер фактически подписан на `ParameterStoreV3.updates` и фильтрует
   обновления по ключу. При первоначальном показе он читает текущее значение;
   если его нет в store, пробует декодировать строку из `ParameterProvider`.

`DataFactory.prepareData(2)` готовит элементы списка, а `AdvancedFragment`
передаёт их существующему составному адаптеру. Генерация структуры и живые
значения параметра — разные источники; при миграции это различие сохраняется.

## Действия пользователя и запись

- При перетаскивании SeekBar меняется подпись; SET отправляется после отпускания.
- Кнопки `+`/`−` меняют значение на 1 с ограничением по диапазону. Отправка
  откладывается на 300 мс после последнего нажатия для этого виджета.
- Доступность ввода определяется `UiState.v3WidgetsInteractionEnabled`.
- При уничтожении представления адаптер отменяет таймеры и подписки.

Текущий `SliderDelegateAdapterV3.sendProgress()` для скорости выполняет:

1. Поиск метаданных и кодирование `SetInt` через существующий codec.
2. Создание SET-пакета `00 0F 3D VALUE CRC` через `BLECommandsV3.sendCommand()`.
3. Оптимистическую запись нового значения в `ParameterStoreV3`.
4. Вызов `SettingsProfileManager.saveBleValue()` — сохранение запускается асинхронно.
5. Сериализацию значения в `ParameterProvider`.
6. Постановку пакета в текущую очередь через `MainActivityUBI4.bleCommandWithQueue()`,
   характеристика `SERIALPORTCHAR_UUID`, тип `WRITE`.

Профиль сохраняется существующим `SettingsProfileRepository` через Room DAO
с привязкой к серийному номеру и активному профилю. Если текущий serial пустой,
manager пропускает сохранение. Завершение записи в БД не ожидается перед BLE SET.

## Что сейчас означает подтверждение

- `ParameterStoreV3` обновляется и локально при SET, и парсером при ответе.
  Само изменение этого store не доказывает ответ устройства.
- В `sendProgress()` нет отдельного GET скорости после каждого SET и нет
  ожидания совпадающего значения readback; callback очереди передаётся пустым.
- Callback отправки/GATT и завершение очереди не равны подтверждению применения
  значения. В частности, `writeData()` может завершиться и при отказе dispatch.
- Входящий ответ обновляет отображаемое значение по существующему пути.
  Фактическое поведение устройства после SET в этой проверке не измерялось.

Первый рефакторинг сохраняет оптимистическое обновление и существующий порядок
операций. Он не добавляет новые GET, таймауты, повторные отправки, rollback или
надпись «подтверждено устройством». Такие изменения требуют отдельного решения.

## MVVM с потоком состояния в стиле MVI

Базовый пакет новых классов: `app/.../ubi4/versions/v3/`.
Скорость переносится как первая часть экрана дополнительных настроек V3
(`AdvancedFragment`, `display = 2`). Отдельная ViewModel на слайдер не создаётся.

- `domain/settings/V3DeviceSettingsRepository`: контракт чтения и изменения
  настроек устройства V3. Сначала содержит только нужные операции скорости,
  без Activity, BLE-пакетов, Room и типов parser/widget-структур. Связанные
  настройки добавляются по мере миграции, независимо от их UI-виджета.
- `data/settings/V3DeviceSettingsRepositoryImpl`: Android-адаптер к текущим
  store, codec, профилю и предоставленному порту отправки в существующую очередь.
- `presentation/advancedsettings/V3AdvancedSettingsViewModel`: принимает действия
  и данные репозитория для перенесённой части дополнительных настроек V3.
- `presentation/advancedsettings/V3AdvancedSettingsUiState`: неизменяемое
  состояние этой части экрана, доступное через StateFlow; сначала только скорость.
- `presentation/advancedsettings/V3AdvancedSettingsAction`: действия экрана
  для существующего типа Slider: изменение значения, завершение перетаскивания,
  нажатия `+`/`−` с идентификацией параметра. Для первого сценария используем
  существующий ключ `P_KEY_SPEED_SETTINGS`; отдельную Speed-терминологию не вводим.
- `presentation/advancedsettings/V3AdvancedSettingsViewModelFactory`: создаёт
  ViewModel через Android lifecycle. ViewModel не хранит Activity, View, binding
  или собственную копию BLE-сессии.

После проверки первого сценария переносим небольшие группы параметров с общим
поведением, расширяя соответствующие модели экрана и контракт настроек.
Отдельные репозитории и ViewModel для каждого виджета не планируются.

Контракт, data-реализация и классы presentation уже добавлены для первого Slider.
`domain/settings/usecase/SetSliderValueUseCaseV3` проверяет допустимый диапазон
и актуальную блокировку перед записью через репозиторий.

Поток записи: `UI Action → ViewModel → UseCase → domain-контракт → существующий механизм`;
данные репозитория и локальные действия формируют `UiState → render`.
Render/подписка не запускают отправку команды. Временный черновик и отображаемое
значение не подменяют сохранённые данные устройства. Подтверждённость значения
не выводится из store, который смешивает локальные и входящие обновления.

MVI задаёт порядок обработки событий. Хранилищем состояния экрана в памяти
будет StateFlow, долговременные данные продолжат храниться существующим способом.

## Критерии будущего переноса

1. Стандартный V3 и INDY3 сохраняют виджет скорости; UBI4 и другие параметры идут прежним путём.
2. Начальное и последующие значения корректно отображаются без отправки SET из render.
3. Сохраняются диапазон UI `0..100`, шаг 1 и форматирование.
4. Перетаскивание отправляет SET после отпускания, `+`/`−` сохраняют задержку 300 мс.
5. Отправленные байты и порядок локального обновления/планирования сохранения/постановки в очередь совпадают с исходными.
6. Блокировка ввода, отмена отложенных действий и пересоздание экрана не создают лишних команд. Обнаруженные исходные расхождения поведения фиксируются отдельно, без скрытого изменения семантики.
7. Повторное отображение восстанавливает текущее состояние и не воспроизводит прошлые действия.
8. Источник значения не объявляется аппаратным подтверждением без соответствующих данных.

Для новых границ и ViewModel можно писать app unit-тесты с подставными зависимостями.
Известная ошибка компиляции shared-тестов отложена пользователем; эти тесты не
удаляем и не считаем пройденными. Проверка физического V3 остаётся отдельной
непроведённой проверкой.

## Шаг 3: выделение репозитория (2026-09-08)

Первый этап реализации: Android, `SliderDelegateAdapterV3`, только
`P_KEY_SPEED_SETTINGS`. Добавлены:

- `versions/v3/domain/settings/V3DeviceSettingsRepository`: `getSliderValue(key)`
  и `setSliderValue(key, value)`; контракт использует String/Int, без Android,
  Room, parser/widget-структур и BLE-пакетов.
- `versions/v3/data/settings/V3DeviceSettingsRepositoryImpl`: чтение store
  с прежним fallback в ParameterProvider; запись через существующий codec,
  store, планирование сохранения профиля, сериализацию и постановку в очередь.
  На этом этапе принимает только `P_KEY_SPEED_SETTINGS`; другие ключи отклоняются.
- Репозиторий передаётся в `SliderDelegateAdapterV3` через конструктор из
  `BaseWidgetsFragment`. Функция отправки использует прежнюю очередь,
  `SERIALPORTCHAR_UUID` и `WRITE`; внутри репозитория нет Activity и новой BLE-сессии.
- В data-реализацию также передаётся функция сохранения: по умолчанию существующий
  `SettingsProfileManager.saveBleValue`, в тестах — подставная функция без БД.

Отрисовка, подписка на `ParameterStoreV3.updates`, ограничения ввода, таймеры
300 мс и отмена при уничтожении представления пока остаются в адаптере.
Это промежуточный этап перед MVVM/MVI, а не завершённый перенос экрана.
Все остальные параметры идут по прежней ветке `SliderDelegateAdapterV3`.
Парсеры, shared, iOS, UBI4-адаптеры, XML/Compose и Gradle не изменены этим этапом.

Добавлены app unit-тесты `V3DeviceSettingsRepositoryImplTest`: приоритет store,
fallback в кеш, отсутствие значения, отсутствие отправок при чтении,
порядок локальных операций и единственный SET-пакет `00 0F 3D 2A A6` для значения 42,
отклонение неперенесённого ключа без побочных действий.

Проверка после переноса, 2026-09-08:

```sh
./gradlew :app:assembleDebug :app:testDebugUnitTest --tests 'com.bailout.stickk.ubi4.*' --console=plain
```

Результат: `BUILD SUCCESSFUL`, Android debug APK собран; 38 тестов в 10 классах,
включая 5 новых тестов репозитория, без ошибок и пропусков. Свежие XML-отчёты
`app/build/test-results/testDebugUnitTest/TEST-*.xml` имеют timestamp
`2026-09-08T13:32:18`. Лог запуска: `/tmp/ubi4-slider-repository-20260908.log`.
608 файлов из снимка защищённых областей совпадают по SHA-256.
Тесты shared, инструментальные проверки и физический BLE не запускались.

По уточнению пользователя реализация переименована в
`V3DeviceSettingsRepositoryImpl`, а её тест — в `V3DeviceSettingsRepositoryImplTest`.
Логика не менялась. После переименования `:app:assembleDebug` и повторный запуск
5 тестов этого класса завершились успешно; лог:
`/tmp/ubi4-repository-rename-20260908.log`.

## Шаг 4: MVVM и состояние Slider (2026-09-09)

Для `P_KEY_SPEED_SETTINGS` подключена `V3AdvancedSettingsViewModel` уровня
Fragment через `ViewModelProvider` и небольшую фабрику. Она создаётся только
при привязке переносимого Slider. Репозиторий/UseCase не удерживают Activity,
View или Fragment; функция отправки обращается к текущей существующей очереди.

`V3AdvancedSettingsUiState` содержит `SliderUiStateV3`: ключ, отображаемое
значение (nullable до получения данных), диапазон `0..100`, доступность ввода
и признак анимации изменения. Наружу ViewModel отдаёт read-only StateFlow.
Slider передаёт `SliderValueChanged`, `SliderChangeCommitted`,
`SliderStepClicked`. Все действия и отложенная запись обрабатываются на Main.
Шаги кнопок изменяют черновик; запись выполняется через 300 мс после последнего
нажатия. Перетаскивание записывает значение после отпускания.

Наблюдение значения и блокировки идёт через domain-контракт к существующим
`ParameterStoreV3` и `UiState.v3WidgetsInteractionEnabled`. Чтение и render
не отправляют команды. При повторном создании представления черновик
восстанавливается из текущих данных репозитория без воспроизведения действий.
UI-подписка отменяется в существующем onDestroy адаптера; наблюдение данных
в ViewModel живёт в viewModelScope. Остальные Slider используют прежний путь.

Явные изменения поведения для переносимого параметра:

- Блокировка ввода теперь отменяет ожидающую команду кнопок. Раньше отменялся
  другой таймер, а таймер конкретного виджета мог продолжить работу.
- Начало изменения значения перетаскиванием и отпускание отменяют ожидающую
  команду кнопок, чтобы не отправить вторую команду после отпускания.
- Закрытие представления отменяет ожидающую запись; повторный вход не отправляет
  несохранённый черновик. Очередь уже отправленных команд остаётся прежней.

Проверки: `:app:assembleDebug` и `:app:testDebugUnitTest` с фильтром
`com.bailout.stickk.ubi4.*` завершились `BUILD SUCCESSFUL`.
48 тестов без ошибок и пропусков, включая 9 тестов ViewModel/UseCase и 6 тестов
репозитория. Проверены чтение/наблюдение без отправок, таймер 300 мс, отпускание,
блокировка, границы, повторное открытие и отмена при очистке ViewModel.
Свежие XML имеют timestamp `2026-09-09T07:50:44–45`;
лог — `/tmp/ubi4-slider-mvvm-20260909.log`.
608 защищённых файлов совпадают с исходным снимком. Shared, парсеры, iOS,
XML/Compose и Gradle этим шагом не изменены. Реальные жесты UI и BLE на физическом
V3/INDY3 не проверены; это остаётся проверкой шага 5.

## Шаг 6: «Настройка силы» в общей ViewModel (2026-09-09)

Добавлен `P_KEY_FORCE_SETTINGS`: существующие `SliderItemV3`, `SliderDelegateAdapterV3`
и `SliderParameterWidgetSStruct`. Он создаётся в `generatedHardcodeWidgets()`
и `generatedHardcodeWidgetsINDY3()` на display=2. Как у скорости, равные исходные
границы 0/0 трактуются UI как `0..100`, шаг 1. Метаданные:
`ParameterInfo(0x0F, 0x3F, 1, 0)`, codec `SLIDER`, поле `sliderValue`.
GET `0x40` уже присутствует в init-запросах обоих профилей и в WidgetResponseRoutesV3;
парсер и генерация виджетов не изменялись.

Фабрика передаёт диапазоны двух параметров в одну `V3AdvancedSettingsViewModel`.
Состояния и задания отправки индексируются существующими ключами. Входящее значение
силы не сбрасывает черновик скорости; действия и таймеры одного Slider не отменяют
запись другого. Блокировка ввода и закрытие представления отменяют обе ожидающие записи.

Адаптер использует `sliderParameterKey`, `sliderViewModel`, `sliderStateJob`,
`bindSliderViewModel` и `renderSliderUiState`. Каждый элемент отображает своё
состояние. Остальные параметры продолжают использовать существующие обработчики.
Репозиторий принимает только ключи скорости и силы и сохраняет прежний порядок:
store → запуск сохранения профиля → сериализованный кеш → существующая очередь.
Для силы 42 тест фиксирует единственный пакет `00 0F 3F 2A 37`, правильный ключ
профиля и отсутствие изменения значения/кеша скорости.

Проверка: `:app:assembleDebug` и `:app:testDebugUnitTest` с фильтром
`com.bailout.stickk.ubi4.*` — `BUILD SUCCESSFUL`. 53 теста в 11 классах,
без ошибок и пропусков; среди них 12 тестов ViewModel/UseCase и 8 репозитория.
Свежие отчёты имеют timestamp `2026-09-09T11:27:20`;
лог — `/tmp/ubi4-slider-force-20260909.log`.
608 защищённых файлов совпали с исходным снимком. На физическом устройстве
этот этап не запускался; изменение скорости и силы и их сохранение на V3/INDY3
нужно проверить отдельно. Установка APK и отправка команд устройству не выполнялись.

## Шаг 6: «Максимальная чувствительность датчиков» (2026-09-09)

Добавлен `P_KEY_EMG_MAX_GAIN_VALUE` в существующие Repository, адаптер и конфигурацию
общей ViewModel. Виджет создаётся на display=2 для STANDARD_V3 и INDY3 с диапазоном
`0..250`, шагом 1. Метаданные: `ParameterInfo(0x12, 0x03, 1, 0)`, codec `SLIDER`,
поле `sliderValue`. GET `0x12/0x04` уже обрабатывается существующим маршрутом
и запрашивается при инициализации обоих профилей. Парсеры и init-запросы не менялись.

Новые классы не понадобились: действия, состояние, UseCase и отмена отложенных записей
используются для третьего ключа. Сохранён порядок store → сохранение профиля → кеш →
очередь BLE. Для значения 250 пакет остаётся `00 12 03 FA 5A`. Чувствительность
датчиков открытия и закрытия (`EMG_GAINS`) этим шагом не переносилась.

Проверки охватывают значения выше 100, границы 0/250, собственный ключ сохранения
и пакет записи, независимость от скорости и силы, отсутствие отправок при чтении,
отмену ожидающих записей при блокировке, закрытии представления и очистке ViewModel.
Изменение и сохранение параметра на физическом V3/INDY3 остаются непроверенными.

Проверка: `:app:assembleDebug` и `:app:testDebugUnitTest` с фильтром
`com.bailout.stickk.ubi4.*` — `BUILD SUCCESSFUL`. 56 тестов в 11 классах,
без ошибок и пропусков; среди них 14 тестов ViewModel/UseCase и 9 репозитория.
Timestamp отчётов: `2026-09-09T11:43:09`;
лог — `/tmp/ubi4-slider-max-gain-20260909.log`.
Shared, iOS, XML/Compose и конфигурация Gradle не изменены. APK на телефон
не устанавливался, команды физическому устройству не отправлялись.

## Шаг 6: все параметры обычного Slider (2026-09-09)

Завершён перенос оставшихся четырёх параметров. Все семь используют существующие
Repository → UseCase → ViewModel → UiState; новые классы для отдельных параметров
не создавались. Виджеты и их состав по профилям остаются прежними: чувствительность
открытия/закрытия есть у STANDARD_V3 и INDY3, положения пальцев — у STANDARD_V3.

| Параметр | Диапазон | SET / GET | Представление данных |
| --- | --- | --- | --- |
| `P_KEY_EMG_GAIN_OPEN_VALUE` | 0..100 | 0x12/0x01 / 0x12/0x02 | `EMG_GAINS`, offset 0, `openGain` |
| `P_KEY_EMG_GAIN_CLOSE_VALUE` | 0..100 | 0x12/0x01 / 0x12/0x02 | `EMG_GAINS`, offset 1, `closeGain` |
| `P_KEY_GLOBAL_THUMB_CLOSED_POSITION` | 0..100 | 0x0F/0x44 / 0x0F/0x45 | `SLIDER`, `sliderValue` |
| `P_KEY_GLOBAL_INDEX_MIDDLE_CLOSED_POSITION` | 0..100 | 0x0F/0x46 / 0x0F/0x47 | `SLIDER`, `sliderValue` |

Два EMG Slider используют одну запись в `ParameterStoreV3` и один сериализованный
кеш. Репозиторий читает нужное поле по offset; при записи кодек получает актуальную
пару из store (либо кеша) и сохраняет второе значение. Пакет строится прежним
`BLECommandsV3.sendGaines`. Сохраняются обе величины в typed value и прежний
`parameterInfo` изменённого поля при вызове сохранения профиля.
Существующее поведение кодека при отсутствии данных пары не изменялось.

Каждое поле наблюдается с `distinctUntilChanged`: запись открытия с неизменным
значением закрытия не сбрасывает ещё не отправленный черновик закрытия.
Раздельные таймеры ViewModel продолжают работать и для общей команды пары.
Репозиторий принимает метаданные вида `SLIDER`; остальные типы через этот контракт
не записываются. Диагностический лог `V3_FINGER_POSITION` перенесён в data-слой.

В `SliderDelegateAdapterV3` удалены прямые обращения к BLE, store, кешу и профилям,
старые подписки на глобальные данные и таймеры записи. Остаются события UI,
отображение UiState, форматирование и анимации. Соответствие метаданных ключам
берётся из существующего реестра, с учётом offset. Правило отключения анимаций
после применения снимка передаётся из точки сборки зависимостей во Fragment.
При повторной привязке/уничтожении представления освобождаются обработчики и анимации;
callback уничтожения регистрируется один раз за время жизни представления адаптера.

Интеграционные app-тесты соединяют настоящие Repository, UseCase, ViewModel,
кодеки и store; только BLE-очередь и запись профиля заменены собирающими callbacks.
Они проверяют обе последовательности записи пары, её пакеты, независимые таймеры
пары, границы и команды положений пальцев, чтение/входящие значения всех семи Slider
без отправок, границы ввода и отмену всех ожидающих записей при блокировке,
уходе с экрана и очистке ViewModel. Нулевое значение в кеше остаётся `{}` по правилам
существующего кодека; парсеры и формат сериализации не менялись.

На физическом V3/INDY3 действия Slider и сохранение профиля пока не проверены.
Особенно нужно проверить поочерёдное изменение чувствительности двух датчиков
с сохранением второго значения; положения пальцев проверяются на STANDARD_V3.

Результат: `:app:assembleDebug` и `:app:testDebugUnitTest` с фильтром
`com.bailout.stickk.ubi4.*` — `BUILD SUCCESSFUL`. 64 теста в 12 классах,
без ошибок и пропусков, включая 8 новых интеграционных сценариев.
Timestamp отчётов: `2026-09-09T11:56:57`;
лог — `/tmp/ubi4-all-sliders-20260909.log`.
Shared, общие парсеры, iOS, UBI4, XML/Compose и конфигурация Gradle не изменены.
APK не устанавливался, команды физическому устройству не отправлялись.

## Связь Fragment с ViewModel и правила domain (2026-09-09)

`BaseWidgetsFragment` создаёт ViewModel с владельцем `this`, принимает действия
адаптера и собирает `uiState` через `viewLifecycleOwner.repeatOnLifecycle(STARTED)`.
У каждого Fragment свой экземпляр ViewModel. Подключение выполняется только
в режиме V3 на экранах, явно указавших `v3SliderParameterKeys`:

- `SensorsFragment`: чувствительность открытия и закрытия;
- `AdvancedFragment`: скорость, сила и максимальная чувствительность;
- `ServiceFragment`: закрытые положения большого пальца и указательного/среднего пальцев.

Экраны выбирают только ключи, без задания диапазонов. Остальные наследники
`BaseWidgetsFragment` и режим UBI4 новую подписку не создают. Состав виджетов
профилей и существующий `DataFactory` не менялись.

`SliderDelegateAdapterV3` получает `onAction` и `renderSliders(states)`;
в нём больше нет зависимости от ViewModel и собственных coroutine-подписок.
Он сохраняет переданное состояние для последующих привязок RecyclerView.
Границы SeekBar берутся из `SliderUiStateV3.allowedRange`; до получения состояния
управление отключено. Форматирование и анимации остаются в адаптере.

`V3SliderSettingsRules` в domain содержит ограничения семи параметров:
максимальная чувствительность `0..250`, остальные `0..100`.
Используются существующие строковые идентификаторы из общих констант KMM;
Android, парсеры и BLE не участвуют в вычислении диапазонов.
ViewModel берёт оттуда диапазоны для UiState, а `SetSliderValueUseCaseV3(key, value)`
сам проверяет значение по этим же правилам. Передать произвольный диапазон из UI
в UseCase больше нельзя. Чтение остаётся через domain-контракт репозитория.

Изменение lifecycle-поведения: при выходе представления из STARTED подписка
останавливается и `ViewDetached` отменяет ещё не отправленные записи. При возврате
`ViewAttached` восстанавливает текущие значения без повторения действий.
`onDestroyView` также отменяет подписку и освобождает адаптер. Уже поставленные
в BLE-очередь команды не отменяются. Fragment, скрытый через `hide()` без смены
состояния lifecycle, под это условие остановки не попадает.

App-тесты дополнены проверками domain-ограничений всех параметров и выбора
набора Slider для конкретной ViewModel. Проверки Fragment/RecyclerView lifecycle
на физическом V3/INDY3, сворачивания приложения и возврата остаются открытыми.

Проверка: `:app:assembleDebug` и `:app:testDebugUnitTest` с фильтром
`com.bailout.stickk.ubi4.*` — `BUILD SUCCESSFUL`. 68 тестов в 13 классах,
без ошибок и пропусков; timestamp `2026-09-09T12:30:28`.
Лог — `/tmp/ubi4-slider-fragment-domain-20260909.log`.
Shared, парсеры, iOS, XML/Compose и конфигурация Gradle не изменены.
