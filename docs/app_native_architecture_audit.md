# Аудит нативного Android-кода app — 2026-09-22

Это актуальная карта оставшейся работы, дополняющая основной план V3.
Аудит шире области реализации: просмотрены Android-участки вне
`new_electronic_by_Rodeon` и `old_electronic_by_Misha`, но рефакторинг поведения
UBI4 по-прежнему не разрешён. Общие файлы можно менять с сохранением всех версий.
AdvancedFragment исключён; DFU отложен до merge реализации коллеги.
Последующее уточнение пользователя: BLEController также исключён из изменений.
Его наличие в результатах аудита не является заданием на рефакторинг контроллера.

После U2 пользователь уточнил оставшийся объём: только Android V3 в app,
без рефакторинга KMM/shared. Действующая очередь R1–R8 и критерии завершения
обновлены в основном плане. R0 отложен; прошивочная часть C5.3 — после merge.
Прежние предложения D1/D3 не являются продолжением текущей Android-задачи.

## Объём и достоверность

Инвентаризированы 396 Kotlin/Java-файлов: 394 в main и два варианта debug/release
тестовых hooks. Проверены импорты, объявления и ссылки, точки сборки зависимостей,
маршруты Manifest/навигации и ключевые реализации с BLE/preferences/глобальными stores.
Это статический архитектурный аудит, не построчное доказательство корректности
каждого метода и не аппаратная приёмка. XML/Manifest/AIDL/Gradle проверялись как
потребители и точки входа; полный аудит неиспользуемых ресурсов ещё предстоит.
Папки исключённых реализаций не рефакторились; поиск ссылок на кандидатов учитывал
их потребителей, чтобы случайно не удалить общий контракт.

| Область | Файлов | Итог |
| --- | ---: | --- |
| versions/v3/domain | 131 | Правила, модели, контракты и UseCase выделены; это не доказательство завершённости Android-потребителей |
| versions/v3/data | 21 | Основные операции изолированы, остаются общие stores и косвенные вызовы через DI |
| versions/v3/presentation | 70 | 13 ViewModel, состояния, действия, UI helpers и совместимость с DataFactory |
| versions/v3/di | 13 | Ручная сборка выделена; некоторые callbacks всё ещё возвращают выполнение в Activity/адаптеры |
| ubi4/ui | 76 | 49 файлов fragments, 18 gripper, остальные — диалоги, график, навигация и вспомогательный UI |
| ubi4/adapters | 33 | 14 файлов widgetDelegateAdaptersV3, 12 общих widgetDelegateAdapters, 6 dialog и BleLogAdapter |
| ubi4/ble | 7 | Рабочая инфраструктура, общая для версий; не должна превращаться в ViewModel |
| ubi4/data | 12 | Android-сеть, WorkManager, training, startup; есть обратная зависимость от адаптера |
| ubi4/utility, models, contract, di, game | 15 | Смешанные UI, инфраструктура, совместимость и кандидаты на удаление |
| scan | 12 | MVP/Dagger, preferences и правила подключения в UI; не перенесён на MVVM/MVI |
| intro | 3 | Разрешения, PIN, маршрутизация и сохранение устройства ещё смешаны |
| com/motorica/gamecontrol | 1 | Parcelable-контракт IPC; сохранять совместимость с AIDL |
| debug/release testing | 2 | Тестовые hooks с разделением вариантов; не удалять как «лишние V3-файлы» |

Последняя проверка кода до аудита: 846 app-тестов и debug прошли
(`/tmp/v3-ui-cleanup-build.log`). Debug/release предыдущего шага также прошли
(`/tmp/v3-main-connection-build.log`). В этом аудите код не менялся, сборки и
тесты повторно не запускались. Проверку реального V3/INDY3 это не заменяет.

## Что уже стало лучше

- В основных V3-сценариях пользовательский ввод идёт через Action → ViewModel →
  UseCase → domain Repository → data implementation; обратно приходит UiState.
- Перенесены параметры «Спецнастроек», график/пороги/кнопки «Датчиков», служебные
  параметры, роль/PIN, имя/serial, калибровка, активный жест и группа ротации.
- Отдельно перенесены статистика, серверный профиль, поддержка, информация о
  протезе, игры и V3-данные редактора жестов. Это сценарии, а не обещание чистоты
  всех строк файлов, в которых они отображаются.
- Адаптеры основных экранов создают сами экраны; DI вынесена из Fragment.
  StateHolder больше не является вторым владельцем репозитория и UseCase.
- Main получил состояние навигации/заряда/identity; синхронизация имеет свою
  ViewModel. Очередь и низкоуровневая запись выделены в BleCommandQueue/Writer.
- Правила телеметрии и выгрузки при уходе приложения выделены; последние операции
  с сохранённым подключением V3 проходят через Main ViewModel/UseCase/Repository.
- Есть проверки границ, повторных записей, отмены и устаревших результатов.
  Последняя локальная очистка убрала 48 строк из AccountFragmentMainV3/Base.

Итог: приложение находится в середине постепенной миграции. У новых участков уже
есть понятные границы и тестируемые правила, но рядом остаются старые пути.
Число файлов/успешных тестов не переводится в процент «чистоты архитектуры».

## Главные границы и статус после переноса

### 1. Применение профилей — R1 выполнен

`SettingsProfileApplierV3` перемещён из adapters в `versions/v3/data/settingsprofiles`.
Обновление ParameterStore/ParameterProvider и построение BLE-команд сохранены;
очередь и SharedPreferences передаются через конструктор. DI-фабрика, прежний
путь Spinner и Ubi4SettingsProfileReceiver подключены к этой одной реализации.

У applier больше нет прямой зависимости от Activity. В R5.1 существующий транспорт
получил зарегистрированный executor вместо main. Чтобы не менять исключённый BLEController,
его no-argument receiver использует callback, зарегистрированный BleDependencies
до создания контроллера; callback не захватывает Activity. Это ограниченная
граница совместимости. Старый путь Spinner всё ещё требует R2.
Архитектурные проверки теперь запрещают data → adapters и обратные зависимости
receiver/applier от UI/DI. Порядок кеша/очереди и ошибки импорта покрыты тестами.

### 2. Некоторые V3-адаптеры имеют два режима выполнения

`SpinnerDelegateAdapterV3` (834 строки) и `ToggleSliderDelegateAdapterV3`
(593 строки) содержат как обработку UiState/Action, так и прямые старые пути
с stores/BLE/preferences. `SwitcherDelegateAdapterV3` (322 строки) сохраняет
прямую работу с данными; он регистрируется в Base.

Сначала построить матрицу: профиль устройства × экран × parameterKey × адаптер ×
ветка bind. Одного наличия регистрации в Base недостаточно, чтобы считать
ветку реально используемой. Одного отсутствия виджета на обычном V3 недостаточно,
чтобы удалить поддержку INDY3/общего потребителя.
Для используемых V3-веток закончить Action/UiState; недостижимые ветки удалить
вместе с collectors, scope, model-кэшами и записью параметров. Совместимость
Advanced/UBI4 сохранять; не переносить её автоматически в новую копию адаптера.

#### R2.1: карта потребителей проверена

Проверены оба `generatedHardcodeWidgets*`, преобразование DataFactory,
экранные mapper/adapter-составы, наборы ключей ViewModel и развилки onBind.
Ниже — текущий состав после генерации, а не утверждение о любом сохранённом
в базе или произвольно сформированном списке виджетов.

| Экран / display | Виджет / ключ | STANDARD_V3 | INDY3 | Владелец действий и состояния |
| --- | --- | --- | --- | --- |
| Спецнастройки / 2 | Режим работы протеза / P_KEY_HAND_CONTROL_MODE | Есть | Есть | V3SpecialSettingsViewModel; Spinner.bindSpinner |
| Спецнастройки / 2 | Действие при смене жеста / P_KEY_GESTURE_CHANGE_MODE | Есть | Нет | V3SpecialSettingsViewModel; Spinner.bindSpinner |
| Спецнастройки / 2 | Профили настроек / P_KEY_SETTINGS_PROFILE | Скрыт: SETTINGS_PROFILE_WIDGET_ENABLED=false | Есть | V3SpecialSettingsViewModel; settingsProfilesFromState=true |
| Служебные настройки / 4 | Режим работы ЕМГ / P_KEY_EMG_CONTROL_MODE | Есть | Есть | V3ServiceViewModel; Spinner.bindSpinner |
| Служебные настройки / 4 | Сторона руки / P_KEY_LEFT_RIGHT_HAND | Есть | Нет | V3ServiceViewModel; Spinner.bindSpinner |
| Служебные настройки / 4 | Роль / P_KEY_DEVICE_ROLE | Есть | Есть | V3ServiceViewModel, включая PIN; Spinner.bindSpinner |
| Спецнастройки / 2 | Переключение жестов сенсорами / P_KEY_EMG_CHANGE_GESTURE | Есть | Нет | V3SpecialSettingsViewModel; ToggleSlider.bindToggleSlider |
| Спецнастройки / 2 | Блокировка движения с ЕМГ / P_KEY_EMG_MOVEMENT_LOCK | Есть | Есть | V3SpecialSettingsViewModel; ToggleSlider.bindToggleSlider |
| Спецнастройки / 2 | Время работы экрана / P_KEY_SCREEN_TIMEOUT | Есть | Нет | V3SpecialSettingsViewModel; ToggleSlider.bindToggleSlider |
| Спецнастройки → приложение | Автоматический вход / AUTO_LOGIN | Есть | Есть | V3SpecialSettingsViewModel → AutoLoginDelegateAdapterV3 |
| Все сгенерированные дисплеи | SwitchItemV3 | Нет | Нет | В обоих генераторах нет PWCE_SWITCH_V3 |

Важные границы:

- SpecialSettingsFragment и ServiceFragment при V3 создают собственный
  CompositeDelegateAdapter; их перечисленные ключи не попадают в старую ветку
  Spinner. Sensors/SprGesture имеют свой состав без Spinner/ToggleSlider/Switcher.
- AUTO_LOGIN приходит из `DataFactory.mobileWidgets()` как общий SwitchItem,
  затем mapper делает V3SpecialSettingsWidget.Switch. Он обслуживается отдельным
  AutoLoginDelegateAdapterV3, а не SwitcherDelegateAdapterV3.
- BaseWidgetsFragment всё ещё регистрирует все три адаптера. Его наборы ключей
  Spinner/ToggleSlider пусты, settingsProfilesFromState=false. AdvancedFragment
  использует этот набор и prepareData(2), поэтому при V3-составе старые ветки
  могут обслуживать тот же display. Маршруты showAdvancedScreen и возврата из
  Help сохранены; отсутствие пункта в обычной навигации не разрешает удаление.
- Другие найденные Base-потребители: GesturesFragment читает display=0,
  SprTrainingFragment — 3; в текущих V3-каталогах искомых виджетов там нет.
  AccountFragmentMainV3/UBI4 не подают виджеты в adapterWidgets. UBI4-ветки общих
  экранов продолжают использовать прежний Base-состав и не рефакторятся.
- `DataFactory.fakeData()` содержит пробный SpinnerItemV3, но найденные вызовы
  во Fragment закомментированы. Это не действующий сценарий для новой ViewModel.
- Отсутствие SwitchItemV3 в генераторах не доказывает отсутствие произвольного
  сохранённого/общего потребителя. Удаление класса и регистрации относится к R0,
  который пользователь отложил.

Вывод: повторно переносить перечисленные рабочие обработчики не требуется.
Оставшиеся прямые зависимости Spinner/ToggleSlider относятся к совместимости
общего набора; классы целиком ещё не чистые. В частности, ToggleSlider даже при
создании читает UiState.v3WidgetsInteractionEnabled, хотя его экранная ветка
управляется UiState ViewModel. Это остаточный долг, а не новый неперенесённый виджет.
R2.2/R2.3 не объявляются полностью завершёнными: полная очистка при сохранении
исключённого Advanced требует отдельного согласования границы. R2.4 остаётся
отложенной очисткой. Не создаём копии адаптеров или новые ViewModel для обхода
этих ограничений. По результатам R2.1 выбран R3.1, список Dashboard; его выполнение описано ниже.

Проверка R2.1: 198 существующих app-тестов в 11 наборах прошли без ошибок и
пропусков: генераторы/mapper для обоих профилей и языков, Spinner/роль/PIN,
профили, ToggleSlider, AUTO_LOGIN и V3ArchitectureTest. Лог:
`/tmp/v3-adapter-routes-r2-tests.log`. Production-код и тесты в R2.1 не менялись;
обновлены только аудит и план. Сборки и аппаратные сценарии повторно не запускались.

### 3. Dashboard: список и все действия содержимого V3 подключены к ViewModel

R3.1 выполнен: DashboardSlotsFragment для V3/INDY3 создаёт V3DashboardSlotsViewModel
через DI, передаёт ViewCreated/ViewDestroyed и отображает UiState. Единственный
LoadDashboardSlotsUseCaseV3 запускает запрос, наблюдение и прежний timeout 5000 мс;
отмена подписки отменяет timeout. V3DashboardSlotsRepositoryImpl переводит текущий
общий DashboardSlotsState в domain-модель и отправляет тот же READ_AVAILABLE_SLOTS
через существующий транспорт. Domain/ViewModel не зависят от shared state/BLE/UI.

Состав слота (включая dataType, версии, размер, смещение и CRC), порядок списка,
тексты загрузки/ошибки и переход к содержимому сохранены. UI-преобразование использует
прежний shared toDashboardSlotUiItem, поэтому название и параметры навигации прежние.
Старый requestSlots во Fragment вызывается только для UBI4; его тело не менялось.
Для сохранения UBI4 это не кандидат на удаление в данном шаге.

Сохранены особенности исходного поведения: одна загрузка на создание View;
STOP/START без уничтожения View не запускает новую загрузку и не сбрасывает таймер;
при уничтожении View наблюдение/таймер отменяются; пересоздание запрашивает список
заново. Ответ после timeout заменяет ошибку, пустой список является успешным ответом.
Фильтрация ответов по плате не добавлялась: наблюдается прежний shared state,
а requestFailed по-прежнему проверяет совпадение адреса и isLoading. Это сохраняемая
семантика общего парсера, а не новая гарантия корреляции ответов с запросами.

861 app unit-тест прошёл без ошибок/пропусков, debug/release собраны.
Проверены точный пакет для адреса 8 (`00 02 01 08 49`), сброс состояния до enqueue,
данные UI, timeout/поздний ответ, уничтожение/пересоздание View и очищенная ViewModel.
Лог: `/tmp/v3-dashboard-slots-r31-build.log`.

Release установлен через `adb -s R8YX900N0JK install -r` с сохранением данных.
На Samsung SM-A065F восстановилось подключение к реальному 00001, открыты
«Датчики» и аккаунт, отображаются платы Fest H and F / Gui. При первоначальной
проверке кнопки настроек плат были скрыты текущей ролью. После включения пользователем
режима сервисного инженера список и чтение слотов проверены; результаты ниже.
В проверенном журнале после запуска нет FATAL EXCEPTION приложения.
Лог: `/tmp/v3-dashboard-slots-r31-device.log`. Эмулятор не использовался.

R3.2 выполнен: начальная загрузка содержимого и Refresh в V3/INDY3 идут через
V3DashboardSlotContentViewModel → LoadDashboardSlotContentUseCaseV3 →
V3DashboardSlotContentRepositoryImpl. ObserveDashboardSlotContentUseCaseV3 даёт
одну подписку на данные, прогресс, сообщения и текущий черновик. ViewModel управляет
состоянием экрана и временем жизни задач. Domain-контракт не зависит от shared/Android;
data адаптирует существующий DashboardSlotContentState, строит прежние BLE-пакеты,
передаёт их общему транспорту и ожидает прежний таймаут. DI собирает зависимости.

Сохранены READ_DATA при размере ≤250, READ_DATA_PART частями по 200 байт при размере
>250, порядок без дополнительных задержек и timeout `5000 + chunkCount * 1000` мс.
В частности, размер 201–250 всё ещё даёт один пакет и 7000 мс ожидания. Каждый Refresh
по-прежнему сбрасывает данные/черновик и запускает отдельный таймер: предыдущий таймер
того же View не отменяется. При уничтожении View отменяются все таймеры и подписка;
пересоздание запускает новый запрос. Поздние/пустые ответы и сборка частей, включая
прежнюю обработку частей не по порядку, сохраняют семантику общего парсера.

Fragment для V3 отображает UiState через преобразование в прежний shared Compose DTO:
все поля, editedValues, статусы и сообщения сохранены. На этапе R3.2 ввод/Send/Save/Reset ещё
работали по прежнему пути и отражались через наблюдение за shared state. Это промежуточный шаг,
а не завершённая миграция редактора содержимого. Для UBI4 сохранены прежние подписка,
requestSlotContent и sendReadDataParts; эти методы ещё имеют рабочего потребителя.
Тела чтения UBI4, записи, диалога сброса, обработчиков ввода/команд, константы и
вспомогательные функции сверены с состоянием до R3.2 и не изменены.

879 app unit-тестов прошли без ошибок/пропусков, debug/release собраны.
Новые проверки покрывают точные пакеты, размеры -1/0/1/200/201/250/251/400/401,
таймауты, части/поздние ответы, отражение черновика без дополнительных команд,
повторный Refresh, другой слот/плату и отмену при уничтожении View/очистке ViewModel.
Лог: `/tmp/v3-dashboard-content-r32-build.log`.

Release R3.2 установлен на Samsung SM-A065F (`R8YX900N0JK`) через install -r.
Проверены запуск, восстановление подключения к реальному 00001, экран «Датчики»
и аккаунт. В проверенном журнале запуска нет FATAL EXCEPTION; лог
`/tmp/v3-dashboard-content-r32-device.log`. Эмулятор не использовался.

После включения пользователем режима сервисного инженера на том же Samsung и
реальном 00001 выполнена аппаратная проверка R3.1/R3.2 (2026-09-22):

- Через кнопку настроек Fest H and F получен список из 17 слотов платы с адресом 0.
- FW_INFO (0x02): один READ_DATA, ответ 75 байт, экран показывает «Данные загружены»
  и разобранные поля прошивки. Нажатие «Обновить» даёт ровно один дополнительный
  READ_DATA; повторный ответ побайтно совпадает с первым.
- После возврата к списку открыт GESTURE_COLLECTION (0x09), размер 405 байт.
  Отправлены ровно три READ_DATA_PART: offset/size 0/200, 200/200, 400/5.
  Получены соответствующие три ответа, экран показывает «Данные загружены»,
  Size=16, Count=16, ItemSize=25, NumDrive=6 и содержимое коллекции.
- В журнале проверенных сценариев нет FATAL EXCEPTION и лишних запросов чтения.
  Выполнялись только чтение/Refresh/навигация; ввод, Send/Save/Reset не использовались.

Журнал: `/tmp/v3-dashboard-service-check.log` (проверка с 18:40:41.417 по времени телефона).
Снимки UI: `/tmp/v3-dashboard-service-slots.xml`,
`/tmp/v3-dashboard-service-content.xml`, `/tmp/v3-dashboard-service-refresh.xml`,
`/tmp/v3-dashboard-service-chunks.xml`. Таймауты/отмена и другие пограничные сценарии
проверены unit-тестами; на устройстве проверен успешный ответ этой платы V3.

R3.3 выполнен: ParameterChanged и Send в V3 идут в существующую экранную ViewModel,
затем в EditDashboardSlotContentUseCaseV3/SendDashboardSlotContentUseCaseV3 и существующий
V3DashboardSlotContentRepositoryImpl. Новых хранилищ или копии shared-парсера не добавлено:
черновик пока хранится в общем state за data-границей; экран наблюдает его через UiState.

Изменение поля синхронно применяет прежнюю shared-схему и не отправляет BLE-команд.
Сохранены кодирование чисел/массивов/строк, незавершённый ввод в editedValues и сообщение
«Есть несохраненные изменения». Send берёт актуальные байты черновика непосредственно
из data, поэтому ввод сразу перед нажатием не теряется из-за задержки наблюдения UiState.
Пустые данные дают прежнее сообщение «Нет данных для отправки»; размер ≤250 — WRITE_DATA,
больший размер — WRITE_DATA_PART по 200 байт. Используется фактический размер данных,
а не declaredSize; порядок и адрес/код из аргументов экрана сохранены. Отправка не вызывает
SAVE_DATA, не ждёт подтверждения платы и не очищает editedValues. Сообщение
«Данные отправлены» по-прежнему появляется после постановки пакетов в очередь.
Повторное явное Send и отправка частично загруженного буфера сохраняют прежнее поведение;
новых блокировок/валидаций не добавлено. После ViewDestroyed/очистки ViewModel действия
ввода/отправки игнорируются, отложенных задач записи не появилось.

Общие методы sendCurrentSlotData/sendWriteDataParts оставлены для UBI4, их тела
не изменены. На этапе R3.3 Save/Reset/ResetAll и диалог подтверждения ещё оставались прежними.
При сверке с началом R3.3 тела чтения UBI4, отправки, Save/Reset, константы и
вспомогательные функции совпали; изменились только V3-маршруты ввода/Send.

900 app unit-тестов прошли без ошибок/пропусков, debug/release собраны.
Проверены кодирование полей, незавершённый ввод, пустой/частичный черновик, Send сразу
после ввода до обновления UiState, повторная явная отправка, границы 1/200/201/250/251/400/401,
порядок и содержимое WRITE_DATA/WRITE_DATA_PART, прежние статусы, очистка через Refresh
и игнорирование действий после уничтожения View/очистки ViewModel.
Лог: `/tmp/v3-dashboard-content-r33-build.log`.

Release R3.3 установлен на Samsung SM-A065F с сохранением данных; подключение к 00001
и сервисный доступ восстановились. В FW_INFO введён `MVVM` в пустое поле FWName:
текст отобразился вместе со статусом «Есть несохраненные изменения». После «Обновить»
все видимые поля совпали с исходными, тестовый текст убран, статус «Данные загружены».
В журнале этого процесса — один запрос списка и два READ_DATA FW_INFO, оба ответа
по 75 байт побайтно совпадают. Команд WRITE/SAVE/RESET и FATAL EXCEPTION нет.
Запись на реальную плату не выполнялась; Send проверен unit-тестами через настоящий
ViewModel/UseCase/Repository с перехватом очереди BLE. Это не аппаратная проверка записи.
Лог: `/tmp/v3-dashboard-content-r33-device.log`; снимки UI:
`/tmp/v3-dashboard-r33-content.xml`, `/tmp/v3-dashboard-r33-edited.xml`,
`/tmp/v3-dashboard-r33-restored.xml`. Эмулятор не использовался.

R3.4 выполнен: Save/Reset/ResetAll подключены через ту же ViewModel, три именованных
UseCase и существующий Repository. Save отправляет только SAVE_DATA всей выбранной
плате; Reset — RESET_TO_FACTORY для выбранного dataCode; ResetAll — тот же пакет с 0xFF,
только после подтверждения. Черновик не отправляется/не очищается автоматически,
новых статусов успеха, ожидания ответа, повторного чтения или проверок значений нет.
Отдельные повторные Save/Reset по-прежнему выполняются и при ещё пустых данных.

UiState содержит идентификатор открытого подтверждения общего сброса. Обновления
данных/таймауты не теряют его; отмена не вызывает UseCase. Подтверждение принимается
один раз только для текущего запроса, старые callbacks после отмены/пересоздания
View игнорируются. ViewDestroyed закрывает V3-диалог и сбрасывает запрос.
Fragment отображает тот же Dialog по состоянию, использует прежние XML, тексты,
оформление и setCancelable(false), передаёт Confirmed/Dismissed в ViewModel.

Разрозненные V3-проверки в обработчике кнопок собраны в одну ветку: все действия
отображения идут в захваченную ViewModel; поздний V3-callback не переключается на
прямой UBI4-путь после очистки поля Fragment. Общая реализация диалога переиспользована
через callbacks, отдельного Dialog Host не добавлено. У UBI4 остаются прежние действия
и прямой вызов команды после подтверждения. Тела чтения/отправки, константы, mapper,
XML диалога и его создание сверены с началом R3.4; UI/протокол не переписывались.

908 app unit-тестов прошли без ошибок/пропусков, debug/release собраны.
Проверены точные SAVE_DATA/RESET_TO_FACTORY пакеты для разных адресов/кодов,
отсутствие отправки/очистки черновика и повторного чтения, повторные явные Save/Reset,
отмена диалога, поздние данные при открытом диалоге, устаревшее/повторное подтверждение,
уничтожение View, другой адрес после пересоздания и очищенная ViewModel.
Лог: `/tmp/v3-dashboard-content-r34-build.log`.

Release R3.4 установлен на Samsung SM-A065F (`R8YX900N0JK`) с сохранением данных.
На реальном 00001 открыты список слотов и FW_INFO. Проверены открытие прежнего диалога
«Сброс всех слотов», сохранение диалога после системного «Назад» (setCancelable(false)),
отмена, повторное открытие и повторная отмена. После этого диалог закрыт, все видимые
поля совпадают с исходными, статус «Данные загружены». В журнале текущего процесса
только READ_AVAILABLE_SLOTS и READ_DATA; WRITE/SAVE/RESET не отправлялись, нет
FATAL EXCEPTION/WindowLeaked. Сохранение и подтверждённый сброс на плате не выполнялись:
эти команды проверены тестами с перехватом BLE, а не аппаратным изменением настроек.
Лог: `/tmp/v3-dashboard-content-r34-device.log`; снимки:
`/tmp/v3-dashboard-r34-content.xml`, `/tmp/v3-dashboard-r34-dialog.xml`,
`/tmp/v3-dashboard-r34-dialog-back.xml`, `/tmp/v3-dashboard-r34-cancelled.xml`,
`/tmp/v3-dashboard-r34-reopened.xml`, `/tmp/v3-dashboard-r34-final.xml`.
Эмулятор не использовался.

Android-сценарии Dashboard подключены к MVVM + UiState/Action; общие схемы, парсер и
хранилище черновика остаются прежними за data-границей. Это не рефакторинг shared/KMM.
R4 и шаги R5.1/R5.2 завершены ниже; следующий перенос — R5.3, запрос телеметрии статистики.
Compose, ввод, подтверждения, размеры частей и порядок команд сохраняются.
Удалять заменённый код только после проверки оставшихся UBI4-потребителей.
Доступ к shared Dashboard state адаптировать через data, парсер не переписывать.

### 4. Главный экран аккаунта перенесён только частично

R4 выполнен 2026-09-23: `AccountFragmentMainV3` получает видимость настроек/обновления
плат из `V3AccountProfileUiState.areBoardServiceActionsVisible`. ViewAttached
восстанавливает роль существующим RestoreDeviceRoleUseCaseV3 до создания адаптеров;
новый ObserveServiceEngineerAccessUseCaseV3 читает StateFlow существующего
V3DeviceRoleRepository. Data возвращает read-only представление прежнего общего
флага доступа, поэтому изменения из служебных настроек остаются видны аккаунту.
Новых репозиториев и правил PIN/доступа нет; изменение роли не запрашивает профиль
повторно и не меняет ревизию его заголовка. Подписка отменяется при закрытии view
и очистке ViewModel; обновление кнопок во Fragment по-прежнему ограничено STARTED.
Удалены прямое чтение preferences, запись/наблюдение роли и числовые константы
роли во Fragment. Новый production-файл один — UseCase наблюдения доступа.

Проверены 914 app unit-тестов без ошибок/пропусков, в том числе 26 архитектурных;
debug/release собраны (`/tmp/v3-account-role-r4-build.log`). Добавлены проверки
восстановления обеих ролей до render, изменения доступа без загрузки/кеширования
профиля, отмены подписки и повторного открытия. Существующие проверки PIN и BLE
роли также проходят.

Release установлен поверх прежней сборки на физический Samsung SM-A065F
(`R8YX900N0JK`), подключение к 00001 восстановилось. В роли сервисного инженера
сравнены аккаунт до/после установки и повторный вход после возврата на «Датчики»:
те же три платы, шесть видимых кнопок «Обновить»/настроек, прежние координаты
и enabled. Роль не переключалась, действия обновления/записи не запускались.
В logcat процесса 26279 нет маркеров падения/ANR. Артефакты:
`/tmp/v3-account-role-r4-account-before.xml`, `...-account-after.xml`,
`...-reopened.xml`, `/tmp/v3-account-role-r4-device.log`.
Эмулятор не использовался; скрытие кнопок для пользователя проверено unit-тестами.

Экран всё ещё частично смешивает обязанности: Fragment наблюдает UiState.updateFlow/
FirmwareInfoState, хранит/строит список плат, загружает каталог и файлы прошивки
через YandexDiskFirmwareRepository. Полностью чистым этот Fragment не объявляется.
Список плат и firmware-кэш согласовать с новой реализацией коллеги; изменение
DFU/скачивания/диалогов прошивки остаётся после merge. AccountMainAdapterUBI4,
BootloaderAdapterUBI4 и CardAdapter имеют живых потребителей — удалять их нельзя.

### 5. Main, Base и соединение всё ещё имеют смешанные обязанности

Main сохраняет публичные preferences-методы, BLE/DFU API, static main и общую
навигацию. `BleDependencies.v3CommandTransport` с R5.1 получает зарегистрированный
BleCommandExecutor без обращения к `MainActivityUBI4.main`. Реализация самого
executor остаётся в Activity и продолжает использовать прежнюю очередь/механизм записи.
Base сохраняет общий набор адаптеров, диалоги и BLE-методы старых потребителей.

#### R5.1: карта оставшихся обращений и регистрация транспорта — 2026-09-23

| Потребитель | Для чего используется Main/Base | Решение |
| --- | --- | --- |
| BleDependencies.v3CommandTransport | Поиск executor перед каждой командой через main | Заменено явной регистрацией существующего BleCommandExecutor при initializeSession и снятием в onDestroy |
| V3SensorsViewModelFactory | observeSyncProgress, getBLEController().refreshWidgetsV3BySwipe | R5.2: заменено зарегистрированной операцией обновления в BleDependencies; данные/фабрика не ищут Activity |
| V3AccountStatisticsViewModelFactory | mainOrNull → getBLEController().requestTelemetryDataV3 | R5.3: перенести получение существующего сервиса в DI подключения; отсутствие сервиса сохраняет прежний no-op |
| createAccountProfileLocalRepository | mDeviceName, locate, mDeviceAddress, mDeviceType, driverVersionS | R5.3: отделить данные от Activity с прежними nullable/default-значениями; не заменять их автоматически на ConnectionState, это изменит поведение |
| Base.bindV3SliderSettings | Прямая очередь Main для старого slider binder | Единственный override набора ключей — AdvancedFragment; исключён пользователем, оставляем |
| Base.refreshWidgetsList и общий набор адаптеров/диалогов | V3 fallback Advanced и UBI4/обучение/жесты | Основные V3 «Датчики» уже отправляют Action своей ViewModel. Общие пути сохраняются, R0 и полная R2 отложены |
| SpecialSettingsFragment → main.getBoolean/saveBoolean | Ветка при отсутствии V3 ViewModel | Это UBI4 fallback; V3 получает настройку из своего состояния, общий путь не переносим |
| GesturesTwoSectionDelegateAdapterV3 → main | Ресурсы, Android context, создание GestureCollectionFactory | UI-зависимость для следующей проверки размещения R8; не новая бизнес-операция репозитория |
| Main/Base → навигация, Toast, lifecycle, диалоги | Платформенные обязанности UI | Допустимы в UI; перенос в domain ради сокращения Activity не нужен |

Выполненная правка R5.1 затрагивает два production-файла: BleDependencies и одну
строку освобождения зависимости в MainActivityUBI4. Новый рабочий класс не создан.
Сам executor хранится слабой ссылкой; transport разрешает текущий executor
при каждом enqueue. Снятие регистрации проверяет идентичность владельца, поэтому
завершение старой Activity не отключает уже новую. После закрытия активной сессии
нет возврата к старому executor; ошибка отсутствующей зависимости по-прежнему
передаётся вызывающему коду без повтора команды и без раннего callback.
Пакет, UUID, тип WRITE, поток вызова и завершение оставлены V3CommandTransport
и прежней очереди. Общие BleEnvironment/manager/parser-регистрации сохранены.
Сам Activity executor не перенесён, полный R5/Main/Base ещё не завершён.

Проверены 919 app-тестов без ошибок/пропусков, включая 27 архитектурных,
4 новых проверки регистрации/замены/закрытия executor и 5 прежних проверок
пакетов/callback транспорта. Debug/release собраны;
лог `/tmp/v3-main-dependencies-r51-build.log`. Общие UBI4-ветки, парсеры,
BLEController, Base/Advanced и shared/iOS в этом шаге не менялись.

На физическом Samsung SM-A065F (`R8YX900N0JK`) release установлен с сохранением
данных. После холодного запуска восстановилось подключение к 00001 и завершилась
синхронизация. В роли сервисного инженера открыты аккаунт → настройки Fest →
список слотов → FW_INFO. В logcat процесса 27927 подтверждены два TX READ_DATA
и два ответа по 75 байт (при открытии и кнопкой «Обновить»); экран показывает
«Данные загружены». Падений/ANR/ошибок отсутствующего executor нет.
Запись, сброс и движение не запускались. Пересоздание Activity с перекрывающимися
владельцами проверено на уровне регистрации unit-тестами, не аппаратно;
физические INDY3/UBI4 на этом шаге не проверялись. Эмулятор не использовался.
Артефакты: `/tmp/v3-main-dependencies-r51-device.log`,
`/tmp/v3-main-dependencies-r51-slots.xml`, `...-content.xml`, `...-refreshed.xml`.

#### R5.2: обновление «Датчиков» без singleton Activity — 2026-09-23

Из V3SensorsViewModelFactory удалены MainActivityUBI4 и два callback, которые
искали main при обновлении. Фабрика передаёт V3SensorsCommandsRepositoryImpl
одну операцию BleDependencies.refreshSensors. В репозитории сохранены проверки
устройства/версии/повторного запуска и выставление fullInitInProgress; обращения
к показу/подписке диалога из него удалены.

MainActivityUBI4 регистрирует обновление только для V3 после создания BLEController
и до подключения устройства. Существующий BleDependencies соединяет callback
observeSyncProgress текущей Activity с refreshWidgetsV3BySwipe текущего контроллера.
Порядок сохранён: флаг → подписка на состояние синхронизации → контроллер.
Показ прежнего диалога по-прежнему идёт через V3SyncViewModel/UiState в Activity.
Регистрация хранит callback/контроллер до закрытия или замены сессии, затем очищается;
долгоживущий репозиторий захватывает только функцию DI, не старую Activity.
Снятие регистрации старым владельцем не затрагивает новую; запоздалая регистрация
не текущего владельца отклоняется. Ошибки callback проходят без retry/сброса флагов,
как и прежде. Новых production-файлов/UseCase/Repository не добавлено.

Граница совместимости остаётся явной: существующий observeSyncProgress и внутренние
обращения BLEController к main не переписывались. Это завершение R5.2, не полный
рефакторинг Activity/контроллера. ViewModel/Action/UiState «Датчиков», UI, протокол,
общий Base, Advanced, UBI4 и shared/iOS сохранены.

Проверены 924 app-теста без ошибок/пропусков, включая 28 архитектурных;
debug/release собраны (`/tmp/v3-sensors-refresh-r52-build.log`). В BleDependenciesTest
добавлены проверки реальной цепочки UseCase → Repository → DI: порядок флага,
подписки и вызова контроллера, блокировка повторного запуска, замена текущей сессии,
запоздалый unbind/bind и ошибка подписки до запроса BLE. Прежние проверки сценариев
«Датчиков» и синхронизации также проходят.

Release установлен на физический Samsung SM-A065F (`R8YX900N0JK`) с сохранением
данных. После холодного запуска подключён реальный 00001, синхронизация завершена.
Два свайпа в свободном правом поле «Датчиков» дали два новых цикла: в logcat процесса
29889 количество observeSyncProgress и requestDeviceData выросло с 1 до 3.
Лог подтверждает показ/закрытие SyncProgressDialog; после каждого цикла виден
экран датчиков. Пороги 171/109 и чувствительность 41/62 остались прежними.
Падений/ANR/ошибок регистрации нет, кнопки движения не нажимались.
Пересоздание владельца проверено unit-тестами; физические INDY3/UBI4 не проверялись.
Эмулятор не использовался. Артефакты: `/tmp/v3-sensors-refresh-r52-device.log`,
`/tmp/v3-sensors-refresh-r52-ready.xml`, `...-refreshing.xml`, `...-second-swipe.xml`.

Удалять методы только после переноса последнего V3-потребителя и проверки других
версий. Остальные зависимости подключать к текущей сессии,
не меняя порядок очереди/callback и не вводя второй BLE-controller.
Навигация, lifecycle, разрешения, показ диалогов и анимации могут оставаться в UI.
BLEController оставляем целиком по прямому указанию пользователя. Улучшения
его потребителей допустимы только с существующим API и прежним поведением;
потребность менять контроллер означает отложенный шаг. DFU отдельно после merge,
новое ограничение на BLEController автоматически этим merge не снимается.

`V3MainRepository` уже объединяет connection, identity, widgets, battery и upload.
Не расширять его всеми новыми обязанностями. Пересмотреть связность при следующем
переносе, переиспользовать существующие контракты. `getVisibleDisplays` с DataFactory
— UI-совместимость под именем Repository, а не предметное бизнес-правило.
Не менять generatedHardcodeWidgets ради исправления названия/расположения этой границы.

### 6. Общие входные экраны не охвачены экранной миграцией V3

`ScanActivity` (875 строк), ScanPresenter/Interactor, SplashScreen, StartActivity
и PinActivity ещё не образуют MVVM/MVI-цепочку. В UI есть preferences, автоподключение,
выбор версии/маршрута и подготовка устройства. Уже есть Dagger в старом scan-коде;
утверждение «во всём приложении нет DI» неверно.

Это общие входы V3/UBI4/исключённых реализаций. Выделять V3-правила выбора и
автоподключения малыми шагами; Android permissions, Intent и ActivityResult
остаются на платформенной стороне. Не удалять старые Presenter/Interactor, пока
через них работают остальные устройства. Не менять WDApplication/ApplicationGraph
из исключённой папки. Полная переделка общей scan-архитектуры сейчас вне объёма V3.

### 7. Дополнительные экраны и фоновые потребители

- AchievementsFragment читает AchievementsState, дату через Activity и вызывает
  BLE напрямую. Но в AccountFragmentMainV3 сейчас `showAchievementsItem = false`.
  Сначала подтвердить доступные маршруты; не включать скрытую кнопку ради миграции.
  Для доступного V3-сценария — ViewModel/UseCase/data; карточки, цвета и анимации остаются UI.
- BleLogFragment сам читает/сохраняет фильтр и наблюдает BleLogStore.
  Для V3 перенести данные/фильтр за ViewModel; позиция прокрутки остаётся UI.
- Help: преимущественно статический UI, не требует пустой ViewModel на каждую
  инструкцию. В SensorsFragmentHelpUBI4 есть preferences, в Help/AdvancedSettingsHelp
  выбор контента по версии; перенести только чтение доступности, сохраняя маршруты.
- SettingsProfileUploadWorker/Scheduler — инфраструктура Android; не делать
  ViewModel для Worker. Общие network-клиенты/repository допустимы в data.
- GameControlBridgeService — IPC/foreground service, не экран. Сохранить Binder,
  AIDL, Parcelable и частоту публикации; зависимости на глобальные сигналы
  изолировать data-источником при работе с этим участком, без экранной ViewModel.
- ControllerBleStatusConnection смешивает broadcast и анимацию: источник
  connection state можно отделить от отображения. BorderAnimator, BottomNavigation,
  chart mapping, recycler binding и GL lifecycle сами по себе не нарушения.

## Что удалить, а что сначала проверить

| Кандидат | Доказательство сейчас | Условие удаления |
| --- | --- | --- |
| `ubi4/adapters/widgetDelegateAdapters/GesturesDelegateAdapter.kt` | Не найдено активного создания; одноимённые log tags другого адаптера не являются использованием | Перепроверить импорты, динамические ссылки и все версии; удалить файл и относящиеся к нему baseline-записи, сохранить общие ресурсы |
| `scan/view/MyAdapter.java` | Найдены объявление/конструктор и lint baseline, потребители не найдены | Проверить ресурсы/рефлексию/варианты сборки, затем удалить |
| `ubi4/utility/InitAllOptionsUBI4.kt` | Не найдены потребители класса/его holder в исходниках | Проверить межмодульный API; удалять Android-класс, не общий AllOptionsV3 автоматически |
| `ubi4/contract/OnChatClickListener.kt` | Не найдены реализации/потребители | Удалить после проверки динамических контрактов |
| `ubi4/ui/gripper/v3model/V3ModelTestLauncher.kt` | Не найдены вызовы, временный флаг false | Удалить неиспользуемый launcher; отдельно оценить debug-размещение тестовой Activity, зарегистрированной в Manifest |
| Старые ветки Spinner/ToggleSlider/Switcher | Прямой путь присутствует, полная достижимость ещё не доказана | Матрица потребителей, затем удалить только заменённое/недостижимое |
| `SettingsProfileApplierV3` | R1 выполнен: одна реализация в data/settingsprofiles, потребители подключены через DI | Старого файла в adapters больше нет; функциональность сохранена |
| BLE-методы, prefs-getters, static main | Имеют потребителей | Удалять только после устранения ссылок нужной версии; совместимость оставить |

Поиск одного имени — недостаточное доказательство: ScanComponent используется
через сгенерированный DaggerScanComponent, StartActivity зарегистрирована и
запускается из ScanActivity. Однофайловые DTO тоже могут возвращаться с выведенным
типом. Их не включаем в автоматическое удаление.

## Что сохраняем осознанно

`generatedHardcodeWidgets` / `generatedHardcodeWidgetsINDY3`, DataFactory и
DataFactoryV3*WidgetsSource остаются по решению пользователя. Это ограниченная
граница совместимости, не повод переносить UI Items и Android в domain.
Shared/iOS API и парсеры не меняем. Папки UBI4 не равны мёртвому коду:
там есть общие адаптеры и Fragment. Advanced, SprTraining/MotionTraining и
прочие UBI4-сценарии не входят в миграцию V3.

Сохраняем renderer/GLSurfaceView, XML/Compose, UI-model/mapper, ViewHolder и
диалоговые Host, когда они только отображают и передают события. Их не нужно
переносить в domain или дробить на UseCase. Длинный renderer не равен god object
бизнес-логики. 3D-редактор ещё требует аппаратной приёмки, несмотря на перенос
его V3-имени/позиций/задержек/записи в ViewModel.

## Правила завершения каждого следующего шага

1. Зафиксировать старый маршрут и его потребителей: V3, INDY3, UBI4, shared/iOS.
2. Перенести одну обязанность в существующие слои; новый класс нужен только при
   самостоятельной ответственности. Простые связанные операции допустимо группировать.
3. Переключить реальный UI/фоновый вход и удалить заменённые ветки/подписки/поля
   в том же шаге. Временное сохранение объяснить конкретным живым потребителем.
4. Проверить не только imports внутри versions/v3, но и реальные DI-callbacks,
   adapters, shared Fragment и входящие события. Не маскировать обход новым Host.
5. Проверить поведение: render не пишет; повторный bind не повторяет команду;
   после STOP/смены устройства не приходят устаревшие результаты; сохранены
   исходные тайминги, значения по умолчанию, лимиты и порядок BLE.
6. Отчитаться о числе добавленных/удалённых рабочих строк отдельно от тестов и
   комментариев, оставшихся исключениях и фактически выполненных проверках.

Рост domain (131 файл / 1582 строки) сам по себе не ошибка: здесь много небольших
моделей/контрактов. Но нельзя продолжать добавлять обёртки без удаления прежнего
пути. V3SpecialSettingsViewModel (633 строки) также требует проверки дублирования
UI-преобразований, чтобы сложность не переехала целиком из Fragment в ViewModel.
Сначала удаляем дубли; не вводим универсальный MVI-фреймворк или новые модули.

Приоритеты и конкретный следующий шаг — в актуальной очереди основного плана.

## Разбор расположения UI и целевая организация

В `app/.../ubi4/ui` 76 файлов: 49 в fragments, 18 в gripper, 4 в dialog,
по одному в main/bottom/widgets/gestures/dashboard. В `versions/v3/presentation`
ещё 70 файлов; Android-адаптеры находятся отдельно в `ubi4/adapters`.
Это части одного presentation-слоя, исторически разложенные по разным принципам:
тип Android-компонента (`fragments`), имя экрана (`mainFragmentV3`) и новая
группировка по сценарию (`presentation/accountprofile`). Различие пакетов не
означает отсутствия связи: Fragment создаёт экранную ViewModel и наблюдает её state.

### Один принцип размещения

Все классы одного V3-only экрана постепенно собираются в уже существующем
`versions/v3/presentation/<сценарий>`. Presentation включает UI и ViewModel;
Fragment в нём допустим. Не создаём ещё один корень `ui/v3` и не перемещаем
все 70 новых файлов обратно. Общий для версий UI остаётся в `ubi4/ui`.
Сохраняем имена классов, domain/data/di и ресурсы. Это целевое размещение,
а не утверждение, что перенос уже выполнен.

Пример первого небольшого переноса:

```text
ubi4/
  ui/
    main/MainActivityUBI4.kt                # общая Activity
    fragments/SensorsFragment.kt           # общий экран V3/UBI4
    widgets/GestureUsageChartView.kt        # общий View из XML
  versions/v3/
    presentation/accountstatistics/
      AccountFragmentStatisticsV3.kt       # перенести сюда существующий файл
      V3AccountStatisticsChartMapper.kt    # перенести сюда существующий файл
      V3AccountStatisticsViewModel.kt      # уже здесь
      V3AccountStatisticsUiState.kt        # уже здесь
      V3AccountStatisticsAction.kt         # уже здесь
    domain/accountstatistics/              # без изменений
    data/accountstatistics/                # без изменений
    di/V3AccountStatisticsViewModelFactory.kt
```

### Реальные владельцы файлов

| Файлы / группа | Что подтверждено по коду | Размещение и порядок |
| --- | --- | --- |
| AccountFragmentStatisticsV3 + ChartMapper | Отдельный V3-маршрут, уже используют ViewModel/UiState | Первые два файла для перемещения в presentation/accountstatistics; без изменения имён и поведения |
| AccountFragmentMainV3 | Профиль и роль через ViewModel; остаются платы/firmware/global state | Цель presentation/accountprofile после очистки ответственности; перенос package не делает старую логику чистой |
| Папка mainFragmentUBI4 | AccountFragmentMainUBI4 относится к UBI4, но AccountMainAdapter/Item/Listener и BootloaderAdapter/Card/Item/FirmwareAssets используются также V3 | Не считать всю папку UBI4-only; общие компоненты можно отдельно сгруппировать в ui/fragments/account/components после проверки импортов, Fragment UBI4 сохранить |
| Sensors, Service, SpecialSettings, SprGesture | Явные V3/UBI4-ветки, общий XML/адаптерный fallback | Оставить в ui/fragments; V3 ViewModel/State остаются в своих feature-пакетах; не копировать Fragment |
| MainActivityUBI4, BaseWidgetsFragment | Общие точки подключения и lifecycle | Оставить общими, сокращать только разрешённые обязанности |
| CustomerService / ProsthesisInformation с UBI4 в имени | Общие экраны с отдельным V3 binding | Не переносить в exclusive V3; Information дополнительно внедряется через Dagger-граф из исключённой папки |
| AccountGamesFragment | Общий вход, отдельные V3 и прежняя ветки | Оставить общим; V3 state и правила уже в presentation/games |
| DashboardSlots / DashboardSlotContent | Открываются из AccountFragmentMainUBI4 и AccountFragmentMainV3 | Общий UI; переносить V3-обязанности по R3, целиком пакет в V3 не переносить |
| Achievements | Единственный найденный override перехода — в аккаунте V3, где кнопка скрыта; UBI4-аккаунт его не реализует | Пока сохранить пакет/доступность; после проверки реального маршрута и очистки данных возможен feature presentation/achievements; не объявлять общим только по имени navigator-метода |
| with_encoders_v3 | Activity выбирает useV3GestureProtocol, preview используется и UBI4-адаптерами | Оставить общей графикой на текущем месте; не переносить всю папку по суффиксу |
| v3model + with_encoders | Графическая инфраструктура и взаимные потребители | Не смешивать пакетную уборку с GL lifecycle и аппаратной проверкой |
| Help, BleLog, dialog, bottom, widgets, gestures | Общий UI, есть ссылки из XML и других сценариев | Оставить общими; отдельную логику данных убирать по R-этапам, статические инструкции не снабжать пустой ViewModel |
| Advanced, SprTraining, MotionTraining | Явно исключённые ранее участки | Не перемещать и не чистить ради единообразного дерева |

Показательные подтверждения общих потребителей: оба AccountFragmentMain вызывают
showDashboardSlotsScreen; редактор использует EXTRA_USE_V3_GESTURE_PROTOCOL;
GesturesOpticDelegateAdapter и GesturesTwoSectionDelegateAdapterV3 используют
общий CollectionGesturePreviewController/GLSurfaceView. Имя V3 не равно владению V3.

Адаптер одного экрана можно впоследствии поместить в его feature, если он действительно
используется только там. Общий Slider/Spinner/ToggleSlider не копируем в каждый
feature; текущие пакеты сохраняем до проверки потребителей и очистки R2.
UI Host/mapper не становятся domain-классами при наведении порядка.

Для общих компонентов тоже предпочитаем группировку по назначению, а не длинные
папки с типом класса в названии. Например, общие карточки аккаунта —
`ui/fragments/account/components`; общий Dashboard Fragment и его Android-preview —
один `ui/fragments/dashboard`. Это последующие небольшие перемещения после проверки
потребителей, без переноса shared Compose-кода в app. Пакеты общего Information,
Advanced и связанной графики пока сохраняются из-за описанных ограничений.
Не заводить бесконтрольный `common`: общий компонент обязан иметь конкретных
потребителей и предметное место, например account/components или widgets.

### Проверки перед перемещением

- Для начала достаточно U1 — двух файлов статистики. Старые файлы перемещаются,
  не копируются; новые архитектурные слои не добавляются. После переноса старая папка исчезает,
  если пуста. Поведенческий рефакторинг и переименование не смешиваются с этим diff.
- V3ArchitectureTest сейчас запрещает импорт DI всем файлам presentation.
  Fragment должен иметь право получить свою ViewModelFactory. Уточнить правило
  только для конкретной UI-точки сборки; ViewModel/StateHolder по-прежнему не
  зависят от DI, UI не вызывает Repository/UseCase и не читает data/BLE/preferences.
  Обновить проверку статистики на новый путь вместо удаления проверки.
- Обновлять imports, Manifest/XML/полные имена классов, тестовые пути и Java-доступ
  при каждом реальном перемещении. Проверить сохранение/восстановление Fragment
  и sourceFragmentClass, если затрагивается такой маршрут.
- Общий GestureUsageChartView используется в двух XML — он не входит в U1.
  Редактор зарегистрирован в Manifest; GL-классы имеют package-private связи.
  Это причины оставить их вне первого перемещения, а не объявлять неиспользуемыми.
- AccountFragmentProsthesisInformationUBI4 включён в ApplicationGraph исключённой
  папки. Его package нельзя менять без нарушения текущих границ или лишнего shim;
  сохраняем исходное расположение.
- На этом шаге выполнены только анализ и уточнение плана. Kotlin/Java/XML-код
  приложения не перемещался; BLEController не менялся; сборки не запускались.

### Выполнение U1 после согласования

Два UI-файла статистики и тест mapper перемещены в существующие пакеты
`versions/v3/presentation/accountstatistics` в main/test. Старые папки удалены;
MainActivity использует новый импорт, архитектурный тест проверяет новый путь.
Исключение DI ограничено точной парой Fragment → его ViewModelFactory;
остальные ограничения сохранены. Количество production-файлов не увеличилось:
теперь в `ubi4/ui` 74, в `versions/v3/presentation` 72 файла.

Для стека, сохранённого до обновления APK, старое полное имя статистики
преобразуется в новое через небольшой wrapper FragmentFactory в существующем
файле Fragment. Activity устанавливает его до super.onCreate; другие экраны
по-прежнему создаёт прежняя фабрика. Этот контракт покрыт отдельным unit-тестом.
Сами обработчики/подписки экрана, подписи графика, ресурсы и BLEController не менялись.
Проверки U1: 847 app unit-тестов прошли без ошибок и пропусков,
debug и release собраны успешно (`:app:testDebugUnitTest :app:assembleDebug
:app:assembleRelease --offline`).
Полное аппаратное восстановление Activity и открытие/возврат не проверялись.

### Выполнение U2: только перемещение адаптеров

Пользователь отложил удаление неиспользуемого кода и выбрал сначала упорядочить
адаптеры. Все 12 V3 UI-адаптеров и PowerSpinnerSelection перемещены из
`adapters/widgetDelegateAdaptersV3` в пакеты `versions/v3/presentation` по назначению;
точная карта приведена в разделе U2 основного плана. Девять существующих тестовых
файлов также перемещены. Новых production-классов нет, тела реализаций не изменены.

На момент U2 в старой папке оставался только SettingsProfileApplierV3: он не
отображает UI, а применяет данные. В следующем R1 он перемещён в data/settingsprofiles.
У Spinner/ToggleSlider/Switcher сохраняются описанные выше старые пути; U2
меняет расположение, но не объявляет эти пути соответствующими MVVM/MVI.
Архитектурные проверки учитывают точные существующие зависимости перенесённых
адаптеров и сохраняют запреты для остального presentation.

Актуальные количества после U2: `ubi4/adapters` — 20 production-файлов,
`versions/v3/presentation` — 85, `ubi4/ui` — 74. Числа и пути в начале аудита
описывают исходное состояние до U1/U2.
Проверены 847 app unit-тестов, debug и release; всё успешно.
Лог: `/tmp/v3-adapters-u2-build.log`. Production-изменения ограничены package/import,
суммарно −9 строк импортов; новых рабочих классов нет. Аппаратные сценарии
на этом шаге не запускались.

### Выполнение R1: применение профилей

Перемещена существующая реализация applier, без нового production-класса поверх неё.
В DI добавлена функция сборки, callback передан репозиторию и Spinner; старый вход
серверного импорта сохранён через регистрацию перед созданием BLEController.
Сохранены пакеты, преобразования жестов/групп, порядок обновления кеша и команды,
AUTO_LOGIN через прежний preferences-ключ/apply(), уведомление импорта в finally.
Основной план описывает оставшиеся границы receiver/транспорта. Следовавшая за R1
проверка R2.1 описана выше; R3.1–R3.4, R4 и шаги R5.1/R5.2 выполнены;
текущий следующий перенос — R5.3, запрос телеметрии для статистики.
Проверено: 854 app unit-теста без ошибок/пропусков, debug и release собраны.
Лог: `/tmp/v3-profiles-r1-build.log`. Применение профилей на телефоне не проверялось.
