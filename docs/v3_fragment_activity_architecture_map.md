# C4 — оставшиеся обязанности Fragment и Activity для V3

Актуальная транспортная структура после упрощения C4.20–C4.23: один
V3CommandTransport ставит пакеты в прежнюю очередь; BleCommandWriter выполняет
синхронную запись UBI4 и writeAsync для V3 с переданным lifecycleScope/IO.
V3CommandExecutor и V3TransportDependencies удалены; сборка объединена в
BleDependencies. Исторические описания ниже отражают промежуточные шаги.
Из тестов удалена копия исходного launch-блока, пять сценариев проверяют writer.
Проверено: 758 app-тестов, debug/release; Samsung + 00001, синхронизация и
четыре экрана без новых падений, содержимое XML совпало. Запись параметров,
движение, прошивка и физический UBI4 не проверялись. Рабочий код: −12 строк,
тестовый: −10 строк; удалены два рабочих файла (относительно начала упрощения).

Дата: 2026-09-18. Область — V3 (STANDARD_V3 и INDY3). Общие файлы
сохраняют UBI4-пути. SprTrainingFragment и MotionTrainingFragment относятся
к UBI4 по уточнению пользователя: обучение не переносим и не удаляем.

Актуальное ограничение пользователя: AdvancedFragment и его Slider binder, lifecycle,
маршруты и сборка зависимостей исключены из дальнейшего рефакторинга. C4.11 отменён.
Исторические предложения переноса Advanced ниже более не действуют.

## Карта и порядок небольших шагов

| Участок | Текущее состояние | Следующее действие |
| --- | --- | --- |
| Создание зависимостей «Датчиков» | Перенесено в существующую V3SensorsViewModelFactory (C4.2 ниже). Fragment подключает готовую ViewModel; транспорт и callbacks синхронизации собраны в фабрике (C4.14/C4.18). | Сохранять сборку в di. |
| Создание зависимостей «Спецнастроек» | Перенесено в существующую V3SpecialSettingsViewModelFactory (C4.3 ниже). Один экземпляр репозитория параметров сохранён для Slider/ToggleSlider/Spinner. | Сохранять сборку в di. |
| Создание зависимостей «Служебных настроек» | Перенесено в существующую V3ServiceViewModelFactory (C4.4 ниже). Общий репозиторий параметров используется Slider, Spinner и ролью. | Сохранять сборку в di. |
| Создание зависимостей «Жестов» | Перенесено в существующую V3GesturesViewModelFactory (C4.5 ниже). Собственный V3-адаптер остаётся в SprGestureFragment. | Сохранять сборку в di. |
| Адаптеры и render/callback-методы Base | V3 «Датчики», «Жесты», «Спецнастройки» и Service владеют своими наборами адаптеров. Общий набор остаётся для UBI4 и Advanced. | C4.9 проверил оставшуюся связку Slider/Advanced. Сборка репозитория перенесена в di (C4.10). C4.11 отменён: оставшаяся связка Advanced сохраняется. |
| AdvancedFragment и общий Slider binder | Единственный потребитель v3SliderParameterKeys — AdvancedFragment. showAdvancedScreen вызывается только запасным возвратом из Help, если источником уже был Advanced. Текущая навигация не даёт самостоятельного входа; удержание accountBtn переключает секретный пункт. | Проверка C4.9 подтверждает единственного потребителя binder. Сборка репозитория перенесена в di (C4.10). Дальнейшие изменения экрана, binder и маршрутов исключены пользователем. |
| Навигация MainActivityUBI4 | Activity выбирает V3/UBI4-экраны аккаунта, управляет стеком, верхней/нижней панелью и паузой графика при переходах. | Отдельно выделять V3-координацию; не менять общие правила переходов, анимаций и сохранения view. |
| Очередь, подключение и синхронизация | Activity владеет очередью и отправкой через BLEController, прогрессом синхронизации. Репозитории V3 подключены callbacks к прежнему транспорту. | Позднее в C4 выделить V3-контракты, сохраняя порядок, обработку завершения/отмены и общий протокол. Перенос di не означает завершения этого участка. |

DataFactoryV3*WidgetsSource остаются ограниченной границей совместимости до D2.
Прошивка и внутренности 3D-редактора — C5. Изменять эти области ради очистки Base
на текущем шаге не требуется. Полный аппаратный сценарий 3D остаётся незакрытым C1.

## C4.2 — сборка зависимостей «Датчиков»

В V3SensorsViewModelFactory добавлен конструктор для существующих callbacks:
передача пакета в очередь, показ синхронизации, обновление виджетов. Он создаёт
прежние репозитории параметров, графика, команд и сессии, а также источник состава
виджетов. Конструктор с domain-контрактами сохранён для поведенческих тестов.

Из Base удалены createV3SensorsPlotRepository/createV3SensorsCommandsRepository
и их импорты: единственный потребитель был SensorsFragment. Общий
createV3DeviceSettingsRepository после C4.4 нужен только Slider binder для Advanced.
SensorsFragment больше не создаёт репозитории или DataFactoryV3SensorsWidgetsSource.

Сами callbacks дословно сохраняют прежние обращения к MainActivityUBI4.main,
SERIALPORTCHAR_UUID и WRITE. Они не захватывают Fragment/Activity; обращение к
текущей Activity по-прежнему происходит при вызове. Создание и lifecycle ViewModel,
все UseCase, data-реализации, UiState/Action и render остаются прежними.
Работа UBI4 в SensorsFragment и общая композиция адаптеров Base не изменены.
Новых production-классов нет; в трёх production-файлах +24/−17 строк.

Проверки: 735 app-тестов, включая 17 архитектурных, без ошибок и пропусков;
debug/release собраны. Проверка границ запрещает возвращать создание репозиториев
в SensorsFragment и зависимости data.sensors в Base. Лог: /tmp/v3-sensors-di-build.log.
Итоговый release установлен на Samsung R8YX900N0JK с сохранением данных.
Подключение к реальному 00001 восстановилось; проверен переход «Датчики» →
«Спецнастройки» → «Датчики». У 29 элементов экрана совпали текст, координаты и
доступность до/после установки и повторного входа; пороги 171/109, слайдеры 41/62
сохранены. Crash-журнал не изменился. Команды движения, изменение параметров и
принудительное обновление виджетов на устройстве не запускались.
Физические UBI4/INDY3 не проверялись; сохранность их правил подтверждается
неизменёнными ветками/реализациями и существующими поведенческими тестами.

## C4.3 — сборка зависимостей «Спецнастроек»

V3SpecialSettingsViewModelFactory.create(context, enqueuePacket) собирает прежние
репозитории параметров, профилей, настроек приложения, сессии и источник виджетов.
Slider, ToggleSlider и Spinner используют один экземпляр V3DeviceSettingsRepositoryImpl,
как прежде. Сохранены SettingsProfileApplierV3::apply, applicationContext и файл
APP_PREFERENCES с MODE_PRIVATE. Фабрика не сохраняет переданный Context.

Из SpecialSettingsFragment удалены создание репозиториев, чтение preferences для
их сборки и соответствующие импорты. Он передаёт фабрике прежний callback очереди
через MainActivityUBI4.main с SERIALPORTCHAR_UUID/WRITE. UI, lifecycle, ViewModel,
UseCase, data-реализации и ветка UBI4 не изменены; сравнение с копией до шага это
подтвердило. Старый конструктор фабрики с контрактами сохранён для тестов.
Новых production-классов нет; в двух production-файлах +30/−15 строк.

735 app-тестов, включая 17 архитектурных, прошли; debug/release собраны.
Существующая проверка сборки зависимостей расширена на SpecialSettingsFragment:
запрещены V3 data/Repository/UseCase, создание источника состава и доступ к preferences.
Лог: /tmp/v3-special-di-build.log. Итоговый release установлен на Samsung
R8YX900N0JK с сохранением данных; реальное подключение 00001 восстановилось.
Для 57 элементов «Настроек протеза» и 10 элементов «Мобильных настроек» совпали
текст, координаты, доступность и состояние до/после установки и повторного входа.
Выбранный раздел сохранился при переходе через «Датчики»; исходный раздел протеза
восстановлен, телефон оставлен на «Датчиках». Crash-журнал не изменился.
Параметры, автоподключение и профили на телефоне не менялись. Профили скрыты на
обычном V3: их поведение проверено тестами, физические INDY3/UBI4 не проверялись.
Сборка зависимостей Service завершена в C4.4 ниже.

## C4.4 — сборка зависимостей «Служебных настроек»

V3ServiceViewModelFactory.create собирает репозитории параметров, роли, данных
устройства, калибровки и сессии, а также прежний источник виджетов. Один экземпляр
репозитория параметров используется Slider, Spinner и репозиторием роли. Для роли
сохранён context.getSharedPreferences(APP_PREFERENCES, MODE_PRIVATE); фабрика
не хранит Context. Конструктор с domain-контрактами остаётся для тестов.

ServiceFragment передаёт прежние callbacks чтения серийного номера, имени,
имени из Intent, немедленного обновления имени и очереди. Они обращаются к
MainActivityUBI4.main при вызове, не захватывая Fragment или конкретную Activity.
Callback завершения записи передаётся репозиторию данных устройства без замены;
для параметров и калибровки фабрика передаёт прежний пустой callback.

Из Fragment удалены создание репозиториев/источника виджетов и связанные импорты.
Сравнение с копией до шага подтвердило сохранность остального кода Fragment,
включая UBI4, lifecycle и render, а также прежней сборки UseCase/ViewModel.
Сами data/domain/presentation-реализации, UI и протокол не менялись.
Новых production-классов нет. Общий метод Base сохранён для Advanced.

735 app-тестов, включая 17 архитектурных, прошли; debug/release собраны.
Существующая проверка сборки зависимостей расширена на ServiceFragment и запрещает
возвращать V3 data/Repository/UseCase, создание источника виджетов и preferences в UI.
Лог: /tmp/v3-service-di-build.log. В двух production-файлах +43/−18 строк.

Итоговый release установлен на Samsung R8YX900N0JK с сохранением данных,
подключение к реальному 00001 восстановилось. На служебном экране у 37 элементов
совпали текст, координаты, доступность и состояние до/после установки и при
повторном входе через «Датчики». Crash-журнал не изменился. Вкладка Service была
временно показана штатным удержанием accountBtn и после проверки снова скрыта;
телефон оставлен на исходном экране «Датчики». Параметры, роль, имя и серийный номер
не менялись, калибровка не запускалась. Физические UBI4/INDY3 не проверялись;
правила репозиториев и обработки действий проверены существующими тестами.
Сборка зависимостей «Жестов» завершена в C4.5 ниже.

## C4.5 — сборка зависимостей «Жестов»

V3GesturesViewModelFactory.create(context, enqueuePacket) создаёт прежние
V3GesturesRepositoryImpl, V3AppSettingsRepositoryImpl, DataFactoryV3GesturesWidgetsSource
и V3DeviceSessionRepositoryImpl. Сохранены порядок создания, applicationContext,
APP_PREFERENCES/MODE_PRIVATE и прежний callback очереди через MainActivityUBI4.main
с SERIALPORTCHAR_UUID/WRITE. Фабрика не сохраняет переданный Context.

Из SprGestureFragment удалены создание этих зависимостей и связанные импорты.
Сравнение с копией до шага подтвердило сохранность остального кода Fragment,
включая UBI4, адаптеры, lifecycle и render, а также прежней сборки UseCase/ViewModel.
Конструктор фабрики с контрактами сохранён для поведенческих тестов.
Правила жестов, группы ротации, пользовательских имён, разделов и раскрытия
коллекции не менялись. Новых production-классов нет.

735 app-тестов, включая 17 архитектурных, прошли; debug/release собраны.
Проверка сборки зависимостей расширена на SprGestureFragment. Лог:
/tmp/v3-gestures-di-build.log. В двух production-файлах +20/−16 строк.

Итоговый release установлен на Samsung R8YX900N0JK с сохранением данных,
подключение к реальному 00001 восстановилось. У 23 элементов экрана «Жесты»
совпали текст, координаты, доступность и состояние до/после установки и при
повторном входе через «Датчики». Активным остался «Кулак 2»; пять имён и порядок
группы ротации сохранены. Crash-журнал не изменился; телефон возвращён на «Датчики».
Жесты и группу не меняли, 3D-редактор не открывали. Физические UBI4/INDY3
не проверялись; их правила и общие зависимости сохранены в коде и покрыты
существующими поведенческими тестами.

Создание V3-адаптеров «Датчиков» перенесено в C4.6 ниже.

## C4.6 — адаптеры «Датчиков» принадлежат экрану

SensorsFragment создаёт собственные PlotDelegateAdapterV3, SensorsButtonsDelegateAdapterV3,
SliderDelegateAdapterV3 и CompositeDelegateAdapter для V3. Эти три типа покрывают
весь состав V3SensorsWidgetMapper для STANDARD_V3/INDY3. Относительный порядок
адаптеров сохранён: Plot, Buttons, Slider; порядок виджетов по-прежнему задаёт UiState.
Ветка UBI4 получает прежний super.adapterWidgets и использует прежний bindUbi4Widgets.

Экран непосредственно отображает state.plot/buttons/sliders и передаёт действия
в свою ViewModel. Промежуточные render/action-методы Plot и SensorsButtons из Base
удалены вместе с их полями и зависимостями от состояния «Датчиков». Общий Slider
и его методы пока нужны «Спецнастройкам», Service и Advanced.

Общий набор Base сохранён полностью и в прежнем порядке. DataFactory всё ещё
поддерживает PlotItemV3/ButtonsItemV3, поэтому поддержка этих типов в общем наборе
не удалялась: там остались адаптеры с прежними пустыми обработчиками для экранов,
не имеющих V3SensorsViewModel. Рабочие V3-адаптеры «Датчиков» теперь создаёт только
SensorsFragment. При выборе V3 общий набор Base для SensorsFragment не создаётся.

Все три экранных адаптера регистрируют очистку через registerDelegateCleanup;
releaseDelegateResources, порядок onDestroyView и отключение RecyclerView сохранены.
Повторное использование view holders, регистрация очистки и обработка действий
внутри адаптеров не менялись. Логика ViewModel, domain/data и состав виджетов прежние.
Сравнение с копией до шага подтвердило неизменность UBI4-binding, teardown,
V3-binding и общего набора Base после подстановки двух прежних адаптеров.
Новых production-классов нет.

736 app-тестов, включая 18 архитектурных, прошли; debug/release собраны.
Новая проверка запрещает возвращать состояние и action/render-методы «Датчиков»
в Base. Прошли существующие проверки отключения/recycle/destroy адаптеров,
отсутствия записей при render, а также ViewModel и состава обеих моделей V3.
Лог: /tmp/v3-sensors-adapters-build.log. В двух production-файлах +43/−43 строки.

Итоговый release установлен на Samsung R8YX900N0JK с сохранением данных;
реальное подключение 00001 восстановилось. У 29 элементов «Датчиков» совпали
текст, координаты, доступность и состояние до/после установки, возврата из
«Спецнастроек» и возврата из «Жестов». Crash-журнал не изменился. Кнопки движения,
изменение параметров и вход в 3D-редактор не выполнялись. Физические UBI4/INDY3
не проверялись; общий путь UBI4 проверен сравнением кода, состав обеих моделей
V3 и поведение адаптеров — тестами. Телефон оставлен на «Датчиках».
Перенос адаптеров «Спецнастроек» описан в C4.7 ниже.


## C4.7 — адаптеры «Спецнастроек» принадлежат экрану

2026-09-21. SpecialSettingsFragment создаёт собственный V3-набор из AutoLogin,
Spinner, ToggleSlider и Slider в прежнем относительном порядке. Эти типы покрывают
весь состав V3SpecialSettingsWidgetMapper для STANDARD_V3/INDY3. Экран передаёт
действия своей ViewModel и отображает её состояние непосредственно в адаптерах;
порядок render, обновление списка, диалог имени профиля и lifecycle сохранены.

Из Base удалены поля AutoLogin/ToggleSlider и заменённые методы действий/отображения
этих виджетов и профилей. Общий набор Base сохранён со всеми прежними типами,
порядком, настройками и нейтральными обработчиками для остальных потребителей.
Общие Spinner/Slider остаются нужны Service и Advanced. UBI4 получает
super.adapterWidgets; прежние overrides ключей и settingsProfilesFromState сохранены,
чтобы не переключить общий адаптер на другие пути обработки.

Экранный Spinner передаёт выбор, создание и переименование профиля ViewModel.
Callback V3SettingsProfilesUpdates нужен только прежнему пути профилей и остаётся
в общем наборе Base; при settingsProfilesFromState этот путь не вызывается.
В UI не добавлены зависимости от data. Все четыре адаптера регистрируют очистку
через registerDelegateCleanup; код самих адаптеров и releaseDelegateResources прежний.
Сравнение с копией до шага подтвердило сохранность общего набора, UBI4-render,
подключения ViewModel, выбора раздела, диалогов и teardown.
Новых production-классов нет; в двух production-файлах +67/−75 строк.

737 app-тестов, включая 19 архитектурных, прошли; debug/release собраны.
Новая проверка запрещает возвращать координацию состояния/действий «Спецнастроек»
в Base. Проверка привязки Spinner дополнена запретом вызова старого уведомления
об изменении профилей при обработке экранных действий. Прошли существующие
проверки состава STANDARD_V3/INDY3, render без записей и очистки обработчиков.
Лог: /tmp/v3-special-adapters-build.log.

Итоговый release установлен на Samsung R8YX900N0JK с сохранением данных;
реальное подключение 00001 восстановилось. У 57 элементов настроек протеза
и 10 элементов мобильных настроек совпали текст, координаты, доступность и
состояние до/после установки и при повторном входе через «Датчики».
Выбранный мобильный раздел сохранился при переходе; после проверки восстановлен
раздел протеза, телефон возвращён на исходные «Датчики» (29 элементов совпали).
Crash-журнал не изменился. Параметры, автоподключение и профили не менялись.
Профили скрыты на обычном V3; физические INDY3/UBI4 не проверялись.
Их состав и поведение покрыты существующими тестами; общая ветка UBI4 дополнительно
проверена сравнением кода. Перенос адаптеров Service описан в C4.8 ниже.


## C4.8 — адаптеры Service принадлежат экрану

2026-09-21. ServiceFragment создаёт собственный V3-набор: калибровка, кнопка
журнала BLE, Spinner, текстовые поля, Slider. Сохранён прежний относительный
порядок; набор покрывает V3ServiceWidgetMapper для STANDARD_V3/INDY3.
Действия идут в прежнюю экранную ViewModel, render обращается к локальным
адаптерам. Обработка индекса роли, PIN, обратной связи, анимаций и навигация
в журнал BLE не менялись. Новых production-классов нет: +72/−72 строки.

Из Base удалены поля калибровки, Spinner и текстовых полей вместе с заменёнными
state/action-методами и зависимостями от service/spinner presentation.
Общий набор сохранён со всеми типами и настройками: три адаптера встроены
в композицию с прежними нейтральными обработчиками для остальных потребителей.
Service сохраняет override ключей Spinner и super.adapterWidgets для UBI4.
Общее уведомление профилей остаётся в Base; экран Service не содержит профилей
и не нуждается в этом callback. Общий Slider и его binder пока нужны Advanced.

Регистрация очистки идёт через registerDelegateCleanup. Сравнение с копией
до шага подтвердило сохранность общего набора, lifecycle, UBI4-binding,
обработки роли и render после подстановки прямых вызовов адаптеров.
ViewModel, UseCase, репозитории, адаптеры, UI и BLE-протокол не редактировались.

738 app-тестов, включая 20 архитектурных, прошли; debug/release собраны.
Новая проверка запрещает возвращать Service state/action-методы и зависимости
в Base. Прошли проверки состава обеих моделей V3, роли/PIN, текстовых полей,
калибровки, слайдеров, Spinner и очистки обработчиков.
Лог: /tmp/v3-service-adapters-build.log.

Итоговый release установлен на Samsung R8YX900N0JK с сохранением данных;
подключение к реальному 00001 восстановилось. Для 37 элементов Service совпали
текст, координаты, доступность и состояние до/после установки и при повторном
входе через «Датчики». Crash-журнал не изменился. Телефон возвращён на «Датчики»;
временно показанная служебная вкладка снова скрыта. Параметры, роль, имя и серийный
номер не менялись, калибровка не запускалась. Физические INDY3/UBI4 не проверялись;
общий путь UBI4 проверен сравнением кода, состав и обработчики — app-тестами.
Следующий небольшой шаг C4.9 — проверка оставшейся связки Slider/Advanced,
её потребителей и сборки зависимостей без удаления общих маршрутов.


## C4.9 — проверка оставшейся связки Slider/Advanced

2026-09-21. Проверены ссылки в app/shared, ресурсы меню, навигация и тесты.
Единственный production-потребитель v3SliderParameterKeys — AdvancedFragment:
скорость, сила и максимальная чувствительность датчиков. Остальные основные
V3-экраны используют свои адаптеры/ViewModel. V3SliderSettingsViewModelFactory
вызывается только из Base; createV3DeviceSettingsRepository нужен только ей.
V3SliderSettingsViewModel/Action также используются поведенческими тестами,
включая проверку взаимодействия двух экранов в SpecialSettings.

Обычного входа в Advanced в текущем коде не найдено. BottomNavigationController
ведёт page_4 в SpecialSettings, page_secret в Service. Удержание accountBtn
переключает видимость секретного пункта. Единственный вызов showAdvancedScreen
находится в HelpFragmentUBI4: при пустом back stack и sourceFragmentClass,
равном имени AdvancedFragment. Источник Help берётся из текущего activeFragment.
Это запасной возврат, а не самостоятельный вход. Такой результат статического
поиска не доказывает невозможность восстановления ранее сохранённого Fragment;
общие класс, контракт навигации и маршрут не удаляем.

Текущая цепочка: Advanced задаёт ключи → Base подключает ViewModel → ViewModel
вызывает UseCase → domain-интерфейс репозитория → data-реализация. Нарушение
границы сборки остаётся в Base: он создаёт V3DeviceSettingsRepositoryImpl.
Фабрика уже собирает три UseCase, но получает готовый репозиторий из UI.

Следующий ограниченный шаг C4.10: существующая V3SliderSettingsViewModelFactory
создаёт прежний репозиторий по enqueuePacket callback и набору ключей; Base
передаёт прежнюю очередь SERIALPORTCHAR_UUID/WRITE и удаляет метод создания/data-import.
Сохранить конструктор с domain-контрактом, ViewModelProvider owner, ключи,
repeatOnLifecycle(STARTED), ViewAttached/ViewDetached, отмену job и порядок очистки.
Связь binder с Advanced и общий набор адаптеров в этом шаге не менять.
После переноса — архитектурные/поведенческие app-тесты и сборка. Проверка обычных
экранов на Samsung не считается проверкой недоступного через меню Advanced.

В C4.9 изменены только документы; production-код и тесты не редактировались.
Сборка и аппаратные сценарии повторно не запускались. Последняя проверка кода —
C4.8: 738 app-тестов и debug/release. Ранее предложенный перенос binder в Advanced впоследствии отменён пользователем;
экран и его подключение исключены из дальнейших изменений.


## C4.10 — сборка репозитория общего Slider в di

2026-09-21. В существующую V3SliderSettingsViewModelFactory добавлен конструктор
с enqueuePacket и sliderParameterKeys, создающий прежний V3DeviceSettingsRepositoryImpl.
Конструктор с domain-контрактом сохранён. Из Base удалены создание репозитория
и его data-import; callback очереди перенесён дословно в вызов фабрики.
SERIALPORTCHAR_UUID, WRITE и пустой callback завершения прежние.

Сравнение с копией до шага подтвердило, что остальной Base не менялся:
ViewModelProvider owner, ключи, STARTED-подписка, ViewAttached/ViewDetached,
отмена job, render, адаптеры и очистка сохранены. Создание UseCase/ViewModel
в фабрике тоже прежнее. Классы Advanced, его маршруты и UBI4-путь не редактировались.
Новых классов нет. Архитектурная проверка расширена: Base не может импортировать
V3 settings data, Repository/UseCase или возвращать методы createV3*Repository.

738 app-тестов, включая 20 архитектурных, прошли; debug/release собраны.
Лог: /tmp/v3-slider-di-build.log. На телефон сборка не устанавливалась:
штатный вход в Advanced не найден, проверка других экранов не подтверждает
его работу. Это ограничение проверки сохранено; аппаратный тест Advanced
не заявляется. Ранее предложенный C4.11 отменён пользователем. Advanced и его binder больше не переносим.


## C4.12 — подключение BLE «Спецнастроек» в di

В V3SpecialSettingsViewModelFactory добавлен create(context), делегирующий прежней
фабрике с callback. В него перенесено прежнее обращение к MainActivityUBI4.main,
SERIALPORTCHAR_UUID, WRITE и пустому callback завершения. Текущая Activity
по-прежнему определяется при отправке; Fragment/Activity не захватываются.
Из SpecialSettingsFragment удалены BLE-imports и сборка callback.

Это перенос подключения инфраструктуры в composition root, а не переработка
очереди: общий транспорт Activity, протокол, порядок команд и репозитории прежние.
Глобальная Activity остаётся ограничением существующей инфраструктуры.
Фабрика с внедряемым callback и конструктор с domain-контрактами сохранены.
Advanced, Base и Slider factory не редактируются. UBI4-binding и lifecycle
SpecialSettings сохранены. Архитектурный тест запрещает возвращать BLE-сборку
в SpecialSettingsFragment. Новых production-классов нет.

739 app-тестов, включая 21 архитектурный, прошли; debug/release собраны.
Лог: /tmp/v3-special-transport-build.log. Следующий шаг C4.13 — аналогичный
перенос сборки BLE-callback «Жестов» в существующую фабрику di.

Release установлен на Samsung R8YX900N0JK с сохранением данных; подключение
к реальному 00001 восстановилось. У 57 элементов настроек протеза совпали
текст, координаты, доступность и состояние после установки и повторного входа
через «Датчики». Crash-журнал не изменился; телефон возвращён на «Датчики».
Параметры и профили не менялись; отправка команд записи на протез не проверялась.
Физические UBI4/INDY3 не проверялись; ветка UBI4 сохранена сравнением кода.


## C4.13 — подключение BLE «Жестов» V3 в di

V3GesturesViewModelFactory.create(context) подключает прежний callback очереди
через существующую фабрику с enqueuePacket. Обращение к MainActivityUBI4.main,
SERIALPORTCHAR_UUID, WRITE и пустой callback завершения сохранены дословно;
текущая Activity определяется при отправке, Fragment/Activity не захватываются.
Из SprGestureFragment удалены сборка V3-callback и импорт SERIALPORTCHAR_UUID.

Общий UBI4-вызов через transmitter(), BLECommands, MAIN_CHANNEL_CHARACTERISTIC
и WRITE сохранён. Сравнение с копией до шага подтвердило неизменность остального
Fragment (включая render, lifecycle и диалоги) и прежней сборки фабрики.
Новых production-классов нет. Advanced, Base, очередь, репозитории и ViewModel
не редактировались. Глобальная Activity остаётся ограничением инфраструктуры;
перенос callback в di не означает завершения отделения BLE-транспорта.
Архитектурная проверка запрещает возвращать сборку BLE в bindV3Gestures.

740 app-тестов, включая 22 архитектурных, прошли; debug/release собраны.
Лог: /tmp/v3-gestures-transport-build.log. Следующий шаг C4.14 — аналогичный
перенос сборки BLE-callback «Датчиков» с сохранением остальных callbacks.

Release установлен на Samsung R8YX900N0JK с сохранением данных; подключение
к реальному 00001 восстановилось. У 23 элементов «Жестов» совпали текст,
координаты, доступность и состояние после установки и повторного входа через
«Датчики». Crash-журнал не изменился, телефон возвращён на «Датчики».
Жесты и группу ротации не меняли, 3D-редактор не открывали; команды движения
на физическом устройстве не проверялись. Физические UBI4/INDY3 не проверялись;
общий UBI4-путь сохранён сравнением кода, поведение V3 — существующими тестами.


## C4.14 — подключение BLE «Датчиков» V3 в di

V3SensorsViewModelFactory.create(showSyncProgress, refreshWidgets) подключает
прежний BLE-callback через существующий конструктор. Обращение к текущей
MainActivityUBI4.main, SERIALPORTCHAR_UUID, WRITE и пустой callback завершения
сохранены. Fragment/Activity не захватываются. Из SensorsFragment удалены
сборка enqueuePacket и два BLE-imports.

Callbacks observeSyncProgress и refreshWidgetsV3BySwipe остаются на прежних
местах и передаются без обёрток. Сравнение с копией до шага подтвердило,
что остальной Fragment и существующие конструктор/create фабрики не менялись:
UBI4-binding, render графика, lifecycle, порядок обновления и очистка прежние.
Advanced, Base, очередь, парсеры, репозитории и ViewModel не редактировались.
Расширена существующая архитектурная проверка отсутствия BLE-сборки в UI.
Новых production-классов нет. Глобальная Activity и общая синхронизация остаются
границами существующей инфраструктуры, этот шаг не перерабатывает транспорт.

740 app-тестов, включая 22 архитектурных, прошли; debug/release собраны.
Лог: /tmp/v3-sensors-transport-build.log. Следующий шаг C4.15 — подключение
BLE для Service в di с сохранением callback завершения записи onSent.

Release установлен на Samsung R8YX900N0JK с сохранением данных; подключение
к реальному 00001 восстановилось. У 29 элементов «Датчиков» совпали текст,
координаты, доступность и состояние после установки и возврата из «Спецнастроек».
Crash-журнал не изменился. Телефон оставлен на исходных «Датчиках».
Пороги/параметры, кнопки движения и ручное обновление не использовались;
физические UBI4/INDY3 не проверялись. Их общие пути сохранены сравнением кода,
состав и поведение V3 проверены существующими app-тестами.


## C4.15 — подключение BLE Service V3 в di

В V3ServiceViewModelFactory.create прежний enqueuePacket стал callback по
умолчанию; возможность передать свою реализацию сохранена. Из ServiceFragment
удалены его сборка и BLE-imports. Пакет, SERIALPORTCHAR_UUID, WRITE и исходный
onSent передаются текущей MainActivityUBI4.main без замены, вызова или обёртки
onSent. Fragment/Activity не захватываются.

Callbacks имени, серийного номера, имени из Intent и немедленного обновления
имени остаются прежними. Тело фабрики не менялось: для обычных параметров и
калибровки используется прежний пустой callback; device-info получает onSent.
Сравнение с копией до шага подтвердило неизменность остального Fragment,
включая UBI4, lifecycle, render и диалоги. Advanced, Base, очередь, парсеры,
репозитории и ViewModel не редактировались. Новых классов нет.
Расширена архитектурная проверка отсутствия BLE-сборки в UI на Service.
Глобальная Activity остаётся ограничением существующей инфраструктуры;
перенос её подключения в di не означает полного отделения транспорта.

740 app-тестов, включая 22 архитектурных, прошли; debug/release собраны.
В набор входят проверки завершения записи имени/серийного номера, повторного
callback и смены устройства до завершения. Лог: /tmp/v3-service-transport-build.log.
Следующий шаг C4.16 — проверка оставшихся обращений основных V3-экранов к Activity
(синхронизация/обновление Sensors, callbacks данных Service, навигация).
Advanced и его binder исключены из этой проверки и дальнейшего переноса.

Release установлен на Samsung R8YX900N0JK с сохранением данных; реальное
подключение 00001 восстановилось. У 37 элементов Service совпали текст,
координаты, доступность и состояние после установки и повторного входа через
«Датчики». Crash-журнал не изменился; служебная вкладка снова скрыта,
телефон возвращён на «Датчики». Параметры, имя, серийный номер и роль не менялись,
калибровка не запускалась. Физическая запись с onSent не проверялась;
передача callback сохранена сравнением кода, его поведение покрыто app-тестами.
Физические UBI4/INDY3 не проверялись; UBI4-путь сохранён сравнением кода.


## C4.16 — оставшиеся обращения четырёх основных V3-экранов к Activity

Проверка статическая; Advanced, его binder и сборка исключены. Изменены только
документы, код/тесты/телефон не менялись. Последняя проверка кода — C4.15:
740 app-тестов и debug/release. Отсутствие BLE-callback в Fragment не означает
отсутствия инфраструктурных зависимостей: четыре фабрики пока подключаются
к глобальной текущей Activity. Это остаётся границей дальнейшей работы C4.

| Экран | Оставшиеся обращения | Решение |
| --- | --- | --- |
| Service | currentSerial, deviceName, intentDeviceName, applyDeviceName при сборке фабрики | Выполнено в C4.17: прежние callbacks подключены в существующей фабрике di. |
| Sensors | observeSyncProgress и getBLEController().refreshWidgetsV3BySwipe при сборке фабрики | Выполнено в C4.18: подключение перенесено в di, реализация и порядок синхронизации сохранены. |
| Sensors и SprGesture | refreshBottomNavVisibility при обновлении виджетов | Это общая UI-координация с Activity. Сохранить места и порядок вызовов; не помещать в domain/data ради устранения импорта. |
| Service | navigator().showBleLogScreen() | Обычная UI-навигация. Сохранить маршрут. |
| SprGesture V3 | startActivity редактора по запросу из UiState, затем GestureSettingsOpened | Сохранить порядок запуска/подтверждения и extras; внутренности редактора относятся к C5. |
| SpecialSettings | main.getBoolean/saveBoolean | Это ветка без V3 ViewModel, используемая UBI4. V3 читает/сохраняет выбранный раздел через своё состояние и UseCase. Не переносить UBI4. |
| SprGesture UBI4 | transmitter().bleCommandWithQueue, showToast в общем диалоге, обновление панели | Сохранить общий путь; V3 использует собственный адаптер и диалоги состояния. |

Ограничения следующего C4.17: callbacks должны обращаться к текущей Activity
при вызове, не сохранять значения в момент создания фабрики и не захватывать
Fragment/Activity. Приоритет источников имени в репозитории сохранить:
currentSerial → deviceName → connectedName → intentDeviceName с прежними
проверками пустых строк. applyDeviceName остаётся немедленным обновлением после
постановки команды в очередь; не переносить его в onSent. Обработка завершения
записи, read-back серийного номера и защита от смены устройства остаются прежними.
Существующие внедряемые callbacks и domain-конструктор фабрики сохранить.

Для последующего Sensors сохранить refreshSensors: fullInitInProgress=true →
showSyncProgress → refreshWidgets. BLEController запускает initRequestsV3 в
Activity lifecycleScope и затем сбрасывает fullInitInProgress. Перенос подключения
не должен добавлять coroutine/job, менять поток или заменять диалог синхронизации
новым UI. Архитектурные/поведенческие проверки нужны при изменении кода, а не
повторно для этой карты. Новые Host/interface-классы для текущих переносов не нужны.


## C4.17 — callbacks данных устройства Service в di

Четыре прежних callback — currentSerial, deviceName, intentDeviceName и
applyDeviceName — подключены как значения по умолчанию в существующей
V3ServiceViewModelFactory.create. Их можно по-прежнему внедрять явно.
ServiceFragment передаёт только Context; импорты MainActivityUBI4 и
EXTRAS_DEVICE_NAME и настройка callbacks из Fragment удалены.

Activity по-прежнему читается при вызове каждого callback: снимка значений
при создании фабрики и захвата Fragment/Activity нет. Тело фабрики, enqueuePacket
и передача onSent дословно сохранены. Репозиторий, приоритет источников имени
и немедленное обновление после постановки команды в очередь не редактировались.
Сравнение с копией до шага подтвердило неизменность остальных частей Fragment,
включая UBI4, render, диалоги и lifecycle. Новых классов нет. Advanced и Base
не затронуты. Архитектурная проверка запрещает возвращать обращения к Activity
для данных устройства в ServiceFragment. Глобальная Activity остаётся
инфраструктурной зависимостью di; этот перенос не заменяет её общую реализацию.

740 app-тестов, включая 22 архитектурных, прошли; debug/release собраны.
Лог: /tmp/v3-service-info-di-build.log. Следующий шаг C4.18 — подключение
callbacks синхронизации и обновления Sensors в существующей фабрике di.

Release установлен на Samsung R8YX900N0JK с сохранением данных; подключение
к реальному 00001 восстановилось. У 37 элементов Service совпали текст,
координаты, доступность и состояние после установки и повторного входа через
«Датчики». Crash-журнал не изменился; служебная вкладка снова скрыта,
телефон возвращён на «Датчики». Имя, серийный номер, роль и параметры не менялись,
калибровка не запускалась. Физическая запись данных не проверялась;
callback и порядок обновления сохранены сравнением кода и app-тестами.
Физические UBI4/INDY3 не проверялись; общий UBI4-путь сохранён сравнением кода.


## C4.18 — завершение сборки зависимостей четырёх основных V3-экранов

V3SensorsViewModelFactory.create теперь по умолчанию подключает прежние
observeSyncProgress и getBLEController().refreshWidgetsV3BySwipe. Возможность
внедрить callbacks сохранена. SensorsFragment получает готовую фабрику.
Activity определяется при вызове callbacks, а не при создании фабрики;
захвата Fragment/Activity, нового coroutine/job или изменения потока нет.

Сравнение с копией до шага подтвердило, что остальной SensorsFragment и фабрика
не менялись: UBI4, render, lifecycle, очередь и очистка прежние. Порядок
fullInitInProgress=true → диалог → обновление остаётся в неизменённом репозитории;
BLEController по-прежнему отвечает за initRequestsV3 и завершение.
Существующий тест проверяет порядок диалог/обновление и блокировку повторного
запуска. Архитектурная проверка расширена запретом сборки синхронизации в Sensors.
Новых классов нет. Advanced и Base не затронуты.

Итоговая проверка bindV3Sensors/bindV3SpecialSettings/bindV3Service/bindV3Gestures
подтвердила отсутствие сборки Activity transport/storage callbacks в четырёх
основных экранах. В UI остаются подключение ViewModel, отображение UiState,
передача Action и UI-навигация/координация панели. Общие UBI4-пути сохранены.
Этот участок сборки зависимостей завершён; повторные переносы отдельных callback
больше не планируются. Полный C4 не закрыт: di по-прежнему подключается к
глобальной Activity, где остаются очередь, подключение и синхронизация.
Следующий целостный участок — граница V3-транспорта с общей Activity (C4.19):
сначала карта вызовов, владения очередью, lifecycle и завершения/отмены.

740 app-тестов, включая 22 архитектурных, прошли; debug/release собраны.
Лог: /tmp/v3-sensors-sync-di-build.log.

Release установлен на Samsung R8YX900N0JK с сохранением данных; подключение
к реальному 00001 и стартовая синхронизация завершились. У 29 элементов
«Датчиков» совпали текст, координаты, доступность и состояние после установки
и возврата из «Спецнастроек». Crash-журнал не изменился, телефон оставлен
на «Датчиках». Параметры и кнопки движения не использовались; отдельный ручной
refresh на телефоне не проверялся. Его порядок/повторный запуск покрыты
app-тестами, подключение callback сохранено сравнением кода. Физические
UBI4/INDY3 не проверялись; UBI4-binding сохранён сравнением кода.


## C4.19 — граница V3-транспорта и жизненный цикл общей очереди

Проверены MainActivityUBI4, BLEController, BluetoothLeService, BlockingQueueUbi4,
BleCommandExecutor/BleEnvironment и четыре V3-фабрики. Только анализ и документы:
код, парсеры, iOS, UBI4, Advanced и телефон не менялись. Тесты повторно не запускались;
последняя проверка C4.18 — 740 app-тестов и debug/release.

### Фактическая цепочка и ограничения

- Четыре фабрики передают callback репозиториям; отправка идёт через текущую
  MainActivityUBI4.main с SERIALPORTCHAR_UUID/WRITE. Service сохраняет исходный
  onSent, остальные сценарии передают пустое завершение. Emulator hook выполняется
  в Activity до постановки в очередь; новый адаптер должен проходить через него.
- Activity владеет BlockingQueueUbi4, remainingTasks, worker и writeLock.
  startQueue вызывается при создании Activity. Очередь извлекает задачи по порядку,
  разрешая следующую по allowNext от парсеров либо автоматически через 300 мс.
  Оба парсера пользуются той же очередью: переносить/подменять её только для V3 нельзя.
- У V3 Runnable запускает lifecycleScope.launch(Dispatchers.IO), у UBI4 writeData
  вызывается синхронно. Worker уменьшает remainingTasks после task.run: для V3
  это НЕ гарантирует завершения BLE-записи. Порядок извлечения из очереди не следует
  выдавать за доказательство порядка выполнения IO-coroutines.
- writeData вызывает BLEController.bleCommand и ждёт canSendFlag через wait.
  При успешном GATT write сервис сообщает WRITE; BLEController выставляет флаг
  и notifyAll текущей Activity. Парсерный allowNext и GATT write — разные события.
- onSent вызывается после возврата writeData. При dispatched=false writeData
  возвращается без отправки, но прежний код всё равно вызывает callback.
  Это не receipt/ACK протеза и не новый контракт успешной записи. При исключении
  до возврата callback не вызывается. Эти особенности не исправлять попутно.
- onStop worker не останавливает; onDestroy выключает флаг worker, прерывает поток,
  вызывает cleanup BLEController и очищает mainRef, если это та же Activity.
  Отправки V3 принадлежат lifecycleScope, но блокирующее wait само по себе не
  гарантирует кооперативную отмену coroutine. При disconnect контроллер закрывает
  GATT/планирует reconnect; единой гарантии очистки ожидающих задач и завершений
  в просмотренной цепочке нет. Не добавлять её в рамках простого переноса.
- DFU ожидает remainingTasks и имеет отдельные прямые пути записи. Их семантику,
  задержки, счётчик, locks и передачу команд нельзя менять ради четырёх экранов.

### Выбранная граница следующего связного переноса C4.20

Создать один Android data-адаптер V3CommandTransport с операцией enqueue(packet,
onSent) поверх существующего BleCommandExecutor. Получать executor через provider
при КАЖДОМ вызове. Подключение provider текущей MainActivityUBI4 остаётся в di;
адаптер не знает Fragment/Activity. Использовать один общий di-binding для четырёх
фабрик, удалив их повторяющиеся UUID/WRITE/bleCommandWithQueue callbacks за один шаг.
Domain-контракты репозиториев, внедряемые callbacks для тестов и сборку остальных
зависимостей сохранить. Не вводить новый domain-интерфейс с GATT-параметрами.

Адаптер только передаёт тот же пакет и исходный callback, ровно один раз, с
SERIALPORTCHAR_UUID/WRITE. Без launch, очереди, копирования пакета, ретраев,
проглатывания ошибки отсутствующей Activity, раннего callback или смены потока.
Провайдер нужен для сохранения выбора текущей Activity при отправке и отсутствия
её захвата на весь срок ViewModel. BleEnvironment не подставлять вместо main:
его executor отдельно регистрируется и в рассмотренном объекте нет unregister,
поэтому такая замена могла бы изменить поведение после уничтожения Activity.

Проверки C4.20: fake executor проверяет идентичность пакета/callback, UUID/WRITE,
порядок вызовов, отсутствие раннего завершения, выбор нового executor при смене
provider и проброс исключений. Архитектурная проверка запрещает прямой BLE-dispatch
в четырёх фабриках после переноса (прочие callbacks Activity пока остаются).
Один общий запуск app-тестов/сборок и проверка навигации на Samsung 00001.
Команды движения и запись параметров для такой проверки не требуются.
Это выделение входа V3 в существующий транспорт; владение общей очередью остаётся
в Activity до отдельного решения по выявленным lifecycle-ограничениям.


## C4.20 — единый вход в транспорт четырёх основных V3-экранов

Добавлен data/transport/V3CommandTransport: enqueue передаёт тот же ByteArray,
SERIALPORTCHAR_UUID, WRITE и исходный onSent существующему BleCommandExecutor.
Executor запрашивается у provider при каждом вызове. Транспорт не знает Activity,
не создаёт очереди/coroutines и не перехватывает исключения. Callback по умолчанию
пустой, как раньше у параметров/жестов; callback Service передаётся без обёртки.

V3TransportDependencies в di содержит один stateless transport с provider
текущей MainActivityUBI4.main. Он не сохраняет Activity; создание binding не
запрашивает executor. Четыре фабрики подключены к нему одним переносом, их
прямые UUID/WRITE/bleCommandWithQueue callbacks удалены. Спецнастройки и Жесты
больше не импортируют MainActivityUBI4; Sensors и Service ещё используют её
для других прежних callbacks. Внедряемые зависимости и domain-конструкторы
фабрик сохранены. Сравнение с копией до шага подтвердило неизменность их
остальной сборки. Fragment, Activity, общая очередь, парсеры, репозитории,
ViewModel, Advanced, UBI4 и iOS не редактировались.

Пять тестов транспорта проверяют тот же пакет и callback, UUID/WRITE,
синхронную передачу на текущем потоке без раннего завершения, порядок enqueue,
смену executor после создания transport, пустой callback и проброс ошибок
provider/executor без повторов. Архитектурная проверка закрепляет общий binding
четырёх фабрик и отсутствие UI/di/Android-зависимостей в data transport.
Передача исключений и завершения наследует существующий executor: onSent
не превращён в ACK устройства, счётчик/worker/writeLock и emulator hook прежние.

Владение очередью и исполнение команд всё ещё находятся в Activity. Этот шаг
закрывает единый вход в транспорт для четырёх экранов, но не весь C4.
Следующий участок C4.21 — выделение выполнения V3-команд из Activity с сохранением
существующих lifecycleScope/IO, writeData, порядка завершения и UBI4-ветки;
характеризационные проверки должны предшествовать изменению этого механизма.

Проверка C4.20 завершена: 746 app unit-тестов, включая 23 архитектурных,
без ошибок и пропусков; assembleDebug/assembleRelease прошли. Release установлен
на Samsung R8YX900N0JK, подключённый к 00001. Проверены переходы Датчики →
Жесты → Спецнастройки → Служебные → Датчики. Сравнение XML до/после показало
совпадение содержимого экранов (26/20/54/33 элемента без нижней навигации).
Отличие навигации связано с временно открытой служебной вкладкой; она снова
скрыта после проверки. Crash-buffer до/после идентичен. Записи параметров,
движение и калибровка не выполнялись; аппаратные UBI4/INDY3 не проверялись.

## C4.21 — выполнение задачи V3 после извлечения из очереди

V3CommandExecutor в data/transport принимает scope, функцию writeData и dispatcher
(IO по умолчанию). Он запускает прежнюю последовательность writeData → onSent
в переданном scope. V3TransportDependencies собирает его для конкретной Activity;
Activity лениво передаёт собственный lifecycleScope и ::writeData. Экземпляр
не является singleton и не переживает владельца через глобальный binding.
В getBleCommandWithQueue V3-ветка вызывает execute; прежний launch-блок удалён.
Выбор версии по-прежнему происходит при выполнении Runnable, а не при enqueue.

До подключения выполнены пять одинаковых сценариев для исходного launch-блока
и извлечённого исполнителя: асинхронный запуск/аргументы/порядок callback,
отмена до запуска, отмена во время синхронной записи, исключения записи и callback.
Сохраняется отсутствие дополнительной проверки отмены между writeData и callback.
Исключения обрабатываются исходным scope, повторов и собственной очереди нет.

Это перенос запуска V3-записи и завершения, а не всей BLE-инфраструктуры.
Общий writeData, writeLock/canSendFlag, worker, remainingTasks, onDestroy и DFU
остаются в Activity без изменений. UBI4 выполняется синхронно как прежде.
Проверка полного текста Activity относительно копии до шага подтверждает,
что изменились только импорт, binding и V3 launch-блок. Advanced и его binding,
UI, парсеры, KMM и iOS не редактировались.

Следующий участок C4.22 — определить безопасную границу оставшегося общего
writeData и уведомлений writeLock. Учитывать UBI4 и DFU; не дублировать механизм
блокировки ради V3 и не менять семантику отправки или отмены.

Проверки C4.21: 751 app-тест без ошибок/пропусков, debug/release собраны.
На Samsung R8YX900N0JK после установки release синхронизация с 00001 завершилась.
Переходы Датчики → Жесты → Спецнастройки → Служебные → Датчики выполнены.
Содержимое экранов совпало с исходными XML: 26/20/54/33 элемента без нижней
навигации. Служебная вкладка снова скрыта. Crash-buffer не изменился.
Записи параметров, команды движения/калибровки не проверялись.
Физические UBI4/INDY3 не проверялись; сохранение общей ветки проверено по diff.

## C4.22 — граница общей записи и уведомлений

Проверены все ссылки writeLock/canSendFlag в app/shared, отправка BLEController,
callback BluetoothLeService, инициализация/уничтожение Activity и прямые DFU-пути.

| Участок | Текущее поведение, которое нужно сохранить |
| --- | --- |
| Activity.setStaticVariables | Под writeLock устанавливает false и вызывает notifyAll; это не завершение записи. |
| Activity.writeData | Под тем же монитором ставит false, вызывает bleCommand; при отказе ставит true и возвращает, иначе ждёт в while. |
| BLEController WRITE callback | Сначала завершает pendingFirmwareControlWrite, затем получает mainOrNull; при его отсутствии выходит. Под монитором текущей Activity ставит true и уведомляет всех. |
| BluetoothLeService | Отправляет WRITE callback при GATT_SUCCESS; callback не содержит идентификатор ожидающей команды. |
| V3/UBI4 | Используют один writeData; V3 запускает его через lifecycleScope/IO, UBI4 — в worker. |
| DFU | Старый переход использует очередь; прямые control-записи имеют собственный deferred, но получают тот же WRITE callback. Reset с ожидаемым disconnect и writeWithoutResponse обходят очередь. |
| onDestroy | Прерывает worker и очищает BLEController; отдельного завершения writeLock нет. |

canSendFlag хранится в общем FlagState, а монитор принадлежит Activity. Перенос
флага в новый экземпляр изменил бы поведение при смене Activity. Callback сейчас
находит текущую mainOrNull, а не Activity, начавшую отправку; это также сохраняем.
READ/NOTIFY проходят через тот же bleCommand, поэтому нельзя сузить принимаемые
аргументы до WRITE только потому, что четыре V3-фабрики отправляют WRITE.
Парсерный allowNext очереди и GATT WRITE — разные события; объединять их нельзя.

Решение для C4.23: один BleCommandWriter в общей Android-папке ubi4/ble, а не
V3-копия writeData. Он получает функцию BLE-dispatch и владеет одним монитором;
write/reset/onWriteCompleted переносят существующие три synchronized-блока.
Существующий FlagState.canSendFlag и сообщения логов сохраняются. DI собирает
writer на Activity; Activity делегирует запись/сброс/уведомление. BLEController
по-прежнему завершает DFU deferred до mainOrNull и вызывает уведомление через
текущую Activity, но перестаёт напрямую изменять её монитор и флаг. Публичный
writeLock удаляется после проверки всех ссылок. Один и тот же writer используется
обеими ветками; UBI4-правила, worker, очередь, парсеры и DFU-алгоритмы не переносятся.

Перенос выполнить одним связанным изменением, без временного второго монитора.
Перед подключением проверить: отказ dispatch без ожидания; успешную запись с
ожиданием сигнала; сигнал во время dispatch; пробуждение без true; reset; исключение
и прерывание ожидания; прежний порядок V3/UBI4 и DFU callback. Не вводить таймаут,
повтор, дополнительную отмену, привязку callback к пакету или новый ACK-контракт.
После подключения — app-тесты/debug/release и навигация/синхронизация Samsung 00001.
Физическая навигация V3 не заменяет проверки прошивки и аппаратного UBI4.

C4.22 — аудит и решение по границе: production-код не изменялся, сборка и
аппаратная проверка повторно не запускались. C4.23 — реализация этого решения.

## C4.23 — общий BleCommandWriter подключён

Из Activity удалены writeData, публичный writeLock и synchronized-блок сброса.
Из BLEController удалён прямой доступ к монитору Activity и canSendFlag.
BleCommandWriter в ubi4/ble содержит прежние write/reset/onWriteCompleted,
один приватный монитор и прежний общий FlagState.canSendFlag. Текст write
сравнен с исходным: изменён только вызов внедрённого dispatch и убран комментарий.
Сообщения логов, цикл ожидания, аргументы, отказ и исключения сохранены.

BleDependencies в общей di создаёт writer для Activity. Dispatch задан лямбдой:
инициализация writer в setStaticVariables не требует ещё не созданного
mBLEController; контроллер читается при отправке. V3CommandExecutor получает
writer::write, UBI4 вызывает тот же writer синхронно. Activity делегирует reset
и уведомление через onBleWriteCompleted. BLEController сохраняет порядок:
pendingFirmwareControlWrite.complete → mainOrNull → уведомление текущей Activity.
Никакого singleton writer, второго монитора, нового scope или таймаута не добавлено.

Семь тестов до подключения проверили отказ без ожидания и точные аргументы;
ожидание успешной записи; reset/пробуждение без завершения; сигнал во время
самого dispatch; сброс прошлого завершения перед новой записью; nullable/READ/
NOTIFY; проброс исключения без повторов; прерывание ожидания с освобождением
монитора и сохранение общего флага. Изменения Activity/BLEController просмотрены
относительно копии до шага: очередь, worker, DFU-методы и onDestroy неизменны.
Advanced, парсеры, shared/KMM, iOS и UI не редактировались.

Следующий участок C4.24 — владение очередью, worker и remainingTasks в Activity:
выделять их связанно, сохраняя объект BlockingQueueUbi4 для существующих
потребителей, момент уменьшения счётчика и прежнюю остановку worker. Это не
повод менять завершение V3-записи, правила DFU или семантику отмены.

Проверки C4.23: 758 app-тестов без ошибок/пропусков; debug/release собраны.
Release установлен на Samsung R8YX900N0JK, подключение 00001 завершено.
Переходы Датчики → Жесты → Спецнастройки → Служебные → Датчики прошли;
содержимое XML совпало до/после (26/20/54/33 элемента без нижней навигации).
Служебная вкладка снова скрыта, crash-buffer не изменился. Команды движения,
калибровки, запись параметров и прошивка не запускались. Физические UBI4/INDY3
не проверялись; общие участки проверены тестами writer и сравнением кода.

## C4.24 — очередь, worker и счётчик вынесены вместе

Один BleCommandQueue владеет прежним BlockingQueueUbi4, AtomicInteger и worker.
Activity создаёт его на свой lifecycle без новой фабрики/интерфейса, делегирует
start/stop/enqueue и чтение pendingCount. Исходный startQueue и поля удалены.
Тело worker сравнено с копией до шага и совпало дословно (кроме имени метода).
Сохранены put → increment, decrement после Runnable.run, отсутствие decrement
при ошибке/прерывании, проверки флага/interrupt и имя потока. stop не очищает
очередь/счётчик и не добавляет join. Это перенос, не исправление прежних гонок.

Парсеры получают тот же объект очереди через getQueueUBI4; shared и allowNext
не менялись. DFU читает тот же AtomicInteger через pendingCount; ожидания, таймауты
и команды не изменены. V3/UBI4 выбор и выполнение Runnable сохранены в Activity.
UI, Advanced, BLEController, парсеры и KMM/iOS не редактировались.

Пять тестов до подключения проверили передачу packet/task по ссылке и порядок
put/increment, отказ put, порядок выполнения и повторный start, уменьшение
счётчика после возврата, продолжение после ошибки, остановку во время get и
прерывание выполняющейся задачи без очистки очереди/счётчика.
Далее C4.25: сверить оставшиеся V3-обязанности Activity (синхронизация/навигация)
с картой C4; не выносить естественные Android lifecycle/UI обязанности ради числа строк.

Проверки C4.24: 763 app-теста без ошибок/пропусков, debug/release собраны.
Release установлен на Samsung R8YX900N0JK с реальным 00001. Синхронизация
завершилась, переходы по четырём экранам выполнены. XML содержимого до/после
совпал, crash-buffer не изменился; служебная вкладка снова скрыта. Запись
параметров, движение, калибровка, прошивка и физический UBI4/INDY3 не проверялись.

## C4.25 — остаток синхронизации и навигации

Прослежены Activity → SyncProgressDialog и BottomNavigationController, вызовы
observeSyncProgress из Sensors DI/Base, события BLEController/парсера V3.

- Activity запускает первоначальный loader до создания BottomNavigationController;
  setChromeVisible(false) безопасен за счёт короткого замыкания условия. Этот порядок
  и немедленный show сохраняются, чтобы не вернуть мигание/обращение к lateinit.
- SyncProgressDialog сам подписывается на четыре общих потока UiState и содержит
  решения о показе/закрытии и восстановлении панелей. Это оставшаяся смешанная
  ответственность UI/глобального состояния; C4 ещё нельзя считать завершённым.
- startup имеет приоритет над fullInit; завершение current>=total закрывает диалог
  только после startup-ветки. При total=0 fullInit остаётся в ожидании. В рамках
  одного показа процент не уменьшается (maxOf текущего и нового), show сбрасывает его.
- widgetsLoadingFlow — отдельное событие закрытия, даже если флаги ещё true.
  Не заменять merge на combine без сохранения этого поведения. Новый прогресс
  или флаг может снова открыть диалог. Повторный observe отменяет старый watchJob,
  а STARTED-подписка восстанавливается после background с текущими значениями.
- syncShownOnce ограничивает ensureSyncDialogShown, но не прямой observeSyncProgress;
  ручное обновление в Sensors использует прямой вызов. Не объединять эти методы
  механически. Маршрут Advanced и общий binder остаются исключёнными.
- FragmentManager, back stack, hide/add/replace, анимации, IME и видимость панелей
  остаются UI-обязанностями. Новый Navigator/Host ради уменьшения Activity не нужен.
- computeVisibleDisplays пока обращается к DataFactory; это известная граница D2.
  BottomNavigationVisibilityPolicy уже отделяет выбор доступной вкладки. Её нельзя
  заменять захардкоженным V3-набором, пока состав зависит от существующего источника.
- BLEController/парсеры остаются источниками протокольной синхронизации; менять
  их генерацию запросов, критерии готовности и UBI4-инициализацию в этом шаге нельзя.

Следующий связанный шаг C4.26: состояние отображения синхронизации V3 через
ViewModel/UiState и UseCase/Repository для прежних событий. Диалог отображает
состояние; Activity связывает lifecycle и немедленный startup-показ. Путь UBI4
и его подписки сохраняются. Без дополнительного Controller/Host/StateHolder.
Сначала закрепить перечисленные переходы, повторное подключение наблюдения,
возврат из background и событие ready при ещё активных флагах, затем подключить
V3 и удалить его прежнюю обработку из UI. Один перенос всего сценария, не серия
пустых обёрток. Внешний UI, панели, анимации и парсеры сохраняются.

C4.25 завершён как сверка по плану: production-код не менялся, повторная сборка
и аппаратные тесты не запускались. Изменены только карта, план и статус ревизии.

## C4.26 — состояние отображения синхронизации V3

V3SyncRepositoryImpl читает прежние четыре потока UiState, сохраняя ready
отдельным событием и снимая startup/fullInit в момент исходного события.
ObserveSyncUseCaseV3 связывает domain-контракт с V3SyncViewModel. ViewModel
преобразует события в UiState: startup приоритетен, неизвестный total держит
загрузку, процент не уменьшается до reset/нового показа, ready закрывает независимо
от флагов, следующий прогресс может открыть снова. ViewCreated сбрасывает состояние
для нового экземпляра Activity; STOP отменяет источник, сохраняя видимое состояние.

Activity сохраняет немедленный startup show и прежнюю установку startup-флага.
Для V3 она подписывает renderer в STARTED, для UBI4 вызывает прежний обработчик.
Один lifecycle-наблюдатель состояния живёт в Activity; повторный observeSyncProgress
заменяет только подписку ViewModel на источник, исключая конкурирующие finally.
SyncProgressDialog.renderV3 не читает глобальные потоки, а применяет состояние.
chromeVisible=null означает отсутствие запроса менять панели; renderer сохраняет
прежний chromeHidden и меняет панели только при переходе. Это важно для навигации.

Старый observeSyncProgress диалога сохранён дословно для UBI4; V3 его больше
не вызывает. Общий код нельзя удалить до отдельного рефакторинга UBI4, который
исключён пользователем. XML/анимации, парсеры, протокольная инициализация,
Advanced и KMM/iOS не редактировались. Шесть production-файлов — контракт/события,
UseCase, data, ViewModel, UiState/Action и DI; дополнительных Host/Controller нет.

До UI-подключения прошли семь тестов: startup/fullInit, неизвестный total и
границы процента, монотонность/reset, ready и повторное открытие, warm ready без
изменения панелей, STOP/repeated attach, новая Activity/очистка VM и повторное
чтение текущих данных источника. Затем выполнены все app-тесты и сборки.
C4 остаётся открытым до итоговой сверки оставшихся обязанностей Activity/Base;
инициация протокола в BLEController и состав DataFactory не объявляются перенесёнными.

Проверки C4.26 завершены: 770 app-тестов без ошибок/пропусков, debug/release
собраны. На Samsung R8YX900N0JK с реальным 00001 приложение после установки
дошло до рабочего экрана. Четыре экрана совпали с XML до установки; служебная
вкладка снова скрыта. Проверены HOME → недавние приложения → карточка Моторика старт.
Первый кадр возврата содержал нулевые пороги/заблокированные контролы; повторный
снимок после восстановления данных полностью совпал с исходным. Причина/время
этого промежуточного состояния не сравнивались со старой сборкой. Crash-buffer
не изменился. Движение, запись параметров, прошивка, физические INDY3/UBI4
и возврат из фона во время незавершённой аппаратной синхронизации не проверялись.


## C4.27 — итоговая сверка Activity/Base (2026-09-21)

Проверены текущие тела методов Activity/Base, четыре экранных выбора адаптеров,
маршрут обновления Sensors и Android-потребители DFU. Это проверка границ по коду,
а не новое подтверждение поведения на устройстве.

| Ответственность | Решение / незавершённая работа |
| --- | --- |
| FragmentManager, back stack, панели, IME, анимации, Dialog | Оставить в Android UI; отдельный Navigator/Host ради сокращения строк не нужен. |
| Отображение синхронизации V3 | Уже ViewModel/UiState; Activity связывает lifecycle и немедленный startup-показ. Инициация протокола этим не перенесена. |
| initAllVariables/setStaticVariables, reconnect | Смешаны Intent/lifecycle и глобальная сессия/регистрация BLE. D2: отделить V3-данные, сохраняя порядок регистрации и подключения; парсеры не менять. |
| observeBattery, currentSerial, applyDeviceNameImmediately | Шапка ещё читает глобальные данные и меняет имя сессии. D2: данные через V3-контракты/состояние; цвета и Android View остаются UI. |
| TelemetryCoordinator, enqueueAppCloseUploadIfNeeded | Сборка и правила запуска ещё в Activity. D2: сохранить connection/STOP/DESTROY-триггеры, защиту повторного enqueue и отмену в onResume. |
| computeVisibleDisplays, updateFlow | Пока зависят от DataFactory; D2 вместе с V3-составом виджетов. Не заменять список вкладок константой. |
| preferences, WidgetRepoProvider | Общие API имеют старых потребителей; D2 переносит V3-потребление, не удаляет API UBI4. |
| firmwareJumpToBootloader и dfu* | C5: самостоятельный сценарий; Android shared-транспорты используют BleCommandExecutor через BleEnvironment. Queue drain, deferred и reset/disconnect различаются — не объединять механически. |
| Base: адаптеры и Slider binder | Четыре основных экрана выбирают собственные V3-адаптеры; super остаётся UBI4. Непустой v3SliderParameterKeys в Base-пути задаёт Advanced, исключённый пользователем. |
| Base: refreshWidgetsList | Sensors V3 отправляет RefreshRequested во ViewModel; UBI4 вызывает прежний метод. Общую fallback-ветку не удалять ради сокращения Base. |
| Base: lifecycle, диалоги, имена | Cleanup/rebind остаются UI; SprGesture V3 пропускает старое чтение имён. Другие наследники ещё вызывают базовое чтение preferences при onResume — это остаток D2, не полностью очищенный Base. |

C4.27 завершён без правок production-кода и тестов. C4 целиком не закрыт:
оставшиеся зависимости явно закреплены в D2/C5. Порядок C5 → D → E сохраняется;
после D2 нужна повторная сверка C4. Следующий небольшой шаг — C5.1, карта
редактора V3 до переноса его внутренней логики. Общий рендер и UBI4 сохраняются.

Новые сборки и аппаратные проверки не запускались: изменена только документация.
Результаты 770 тестов и Samsung выше относятся к C4.26.

## D2.1 — вкладки и заряд главного экрана V3 (2026-09-21)

По решению пользователя зависимости Activity/Base очищаются перед D1.
Введена V3MainViewModel с V3MainUiState для доступных display и процента
батареи. Activity передаёт ViewCreated/NavigationRefreshRequested/ViewDestroyed,
отображает состояние и сохраняет Android-маршруты, IME и анимации панелей.
V3SyncViewModel остаётся владельцем самостоятельного состояния диалога
синхронизации; смешивать эти состояния в этом шаге не потребовалось.

GetMainVisibleDisplaysUseCaseV3 и ObserveMainUpdatesUseCaseV3 обращаются к
V3MainRepository; реализация читает UiState.updateFlow и batteryPercentFlow.
DataFactory.prepareData остаётся в data как временная совместимость: наличие
метаданных display само по себе не гарантирует отображаемый виджет, поэтому
прежняя фильтрация 0..4 сохранена. Domain получает только Set<Int>/Int и
события, без List<Any>, Android Views или структур виджетов. Полное удаление
зависимости V3 от DataFactory относится к D2.5, а не объявлено завершённым.

Первое вычисление вкладок и публичный refreshBottomNavVisibility остаются
синхронными. navigationRevision позволяет повторно применить UI-правила
скрытой вкладки/IME при неизменном наборе display; батарея эту ревизию не
меняет и не запускает повторный расчёт/переключение вкладок. Проценты
не ограничиваются дополнительно; цвета сохранены: <20 красный, 20..40
жёлтый, >40 зелёный. Подписки живут до DESTROY, включая STOP, как прежние
подписки Activity; пересоздание отменяет старую подписку, очистка VM
не позволяет её запустить заново.

UBI4 по-прежнему использует прежние DataFactory/потоки в Activity; вынесено
только общее отображение батареи в renderBattery. Методы инициализации,
имени/serial, DFU, синхронизации и управления видимостью панелей сохранены.
MainActivity пока не стала меньше: добавлено связывание нового UiState,
а UBI4-реализация остаётся необходимой. BaseWidgetsFragment в этом шаге
не изменён. Следующий D2.2 — его ненужное чтение имён на V3-экранах;
Sensors V3 уже имеет RefreshRequested, Advanced и общие fallback-пути
не следует переносить как новую ответственность V3.

Проверки: 815 app-тестов, 0 ошибок/падений/пропусков, debug/release собраны
(`/tmp/v3-main-state-build.log`). Добавлены проверки фильтрации неизвестных
виджетов и диапазона display, replay/повторных событий, независимости заряда
от навигации, неизменяемого снимка и отмены/пересоздания подписок.

Release установлен на Samsung R8YX900N0JK с V3 `00001`. Синхронизация,
«Жесты» → «Спецнастройки» → фон/возврат → «Датчики» → «Жесты» прошли.
Индикатор батареи отображается; XML «Спецнастроек» до/после фона совпал,
исходный экран «Жесты» восстановлен с тем же XML. Crash-buffer не изменился.
Пороговые цвета проверены по сохранённому коду, разряд устройства до порогов
не выполнялся. Физические INDY3/UBI4, iOS и переходы с клавиатурой в этом
шаге не проверялись. Shared, парсеры и прошивка не изменены.
Артефакты: `/tmp/v3-main-state-crash-before.log`,
`/tmp/v3-main-state-crash-final.log`, `/tmp/v3-main-state-sensors.png`,
`/tmp/v3-account-main-state-final.xml`.

## D2.2 — ненужное чтение имён жестов на V3-экранах

Базовый gestureNameList используется только общим GesturesOpticDelegateAdapter.
V3-адаптеры SensorsFragment, ServiceFragment и SpecialSettingsFragment его
не используют; SprGestureFragment уже пропускает базовую загрузку для V3.
В этих трёх экранах добавлено такое же переопределение loadGestureNameList:
super вызывается только для UBI4. Тем самым при onCreate/onResume V3 больше
не читаются MAC и 14 имён из preferences через navigator.

BaseWidgetsFragment, AdvancedFragment и общие lifecycle-вызовы не изменены.
notifyDataSetChanged при onResume сохранён; загрузка/обновление имён «Жестов»
из ViewModel сохраняется. Для UBI4 базовый метод вызывается как прежде,
Advanced по-прежнему наследует его. Новых классов и тестов для трёх простых
условий не добавлено; это удаление ненужного чтения, не новый Repository.

Проверка: 815 app-тестов, включая архитектурные, без ошибок/падений/пропусков;
debug/release собраны (`/tmp/v3-base-names-build.log`). На Samsung R8YX900N0JK
с 00001 проверены Датчики, Спецнастройки, Служебные настройки и фон/возврат
в Service. В логе нового процесса нет строк базового loadGestureNameList.
XML Service до/после фона совпадает; служебная вкладка снова скрыта,
исходный экран Жесты восстановлен с тем же XML. Crash-buffer не изменился.
Параметры устройства не редактировались; физические UBI4/INDY3 не проверялись.
Артефакты: `/tmp/v3-base-names-phone.log`,
`/tmp/v3-base-names-crash-before.log`, `/tmp/v3-base-names-crash-final.log`.

Далее D2.3: сначала имя/serial главного экрана, затем startup/session.
DataFactory и прочие общие зависимости остаются по плану; Base не объявлен
полностью очищенным. DFU по-прежнему отложен до merge реализации коллеги.

### D2.3.1 — имя и serial шапки V3

V3MainViewModel владеет V3MainDeviceIdentity в UiState. Подключение и
существующие callbacks Activity передают Action; UpdateMainDeviceIdentityUseCaseV3
обращается к V3MainRepository. Доступ к ConnectionState и форматированию
DeviceNameBridgeV3 находится в data. mDeviceName/getCurrentSerial для V3
читают состояние синхронно; аккаунт, Service и телеметрия сохраняют свои
прежние getters и порядок fallback. Activity отображает displayName из состояния.

Сохранены три разных пути: подключение задаёт serial, оставляя deviceName null;
переименование обновляет полное имя соединения и оба значения, игнорируя blank;
ответ DeviceInfo обновляет оба значения и SettingsProfileManager, но не имя
соединения. Подключение/переименование не обновляют serial профилей. Пробелы
и префиксы в полном имени сохраняются; отображение использует прежний bridge.
Общий CPU/UUID-фильтр и формирование serial в callback не перерабатывались.

820 app-тестов (включая архитектурные) прошли, debug/release собраны:
`/tmp/v3-main-identity-build.log`. Проверены инициализация до привязки UI,
три пути обновления, пустое имя, отсутствие повторных записей при обновлении
экрана, смена устройства и запрет действий после очистки ViewModel.

На Samsung R8YX900N0JK установлена release-сборка и проверено подключение
к 00001, имя в шапке на Датчиках/Спецнастройках/Жестах и фон/возврат.
XML Спецнастроек до/после фона совпал; crash-buffer не изменился, в логе
нового процесса нет FATAL EXCEPTION. Имя/serial реального устройства не
редактировались: сценарии записи проверены unit-тестами. Артефакты:
`/tmp/v3-main-identity-phone.log`, `/tmp/v3-main-identity-crash-final.log`.

Это ещё не полное отделение сессии: startup и DI callbacks Service через
Activity остаются до следующих шагов D2.3.2; телеметрия — D2.4, DataFactory — D2.5.
UBI4 сохраняет свои поля и прежние операции. Shared/iOS, парсеры, Advanced и DFU
в этом шаге не изменялись; физическая проверка UBI4/INDY3 остаётся открытой.

### D2.3.2 — регистрация BLE и удаление неиспользуемого блока

setStaticVariables удалён из MainActivityUBI4; его последовательность перенесена
в существующий BleDependencies.registerSession: новый flow подтверждения чанков,
reset writer, executor менеджера, оба парсера и BleEnvironment.register.
Вызов остаётся на прежнем месте initAllVariables, до DeviceConnected,
сохранения MAC, WidgetRepoProvider.setCurrentMac и запуска BLEController.
Используются те же экземпляры manager/writer/executor и тот же lifecycleScope.
Это сборка зависимостей в di; новые UseCase/Repository для конструкторов не нужны.

Удалены два ручных BluetoothLeService() и приватные bluetoothLeService /
mServiceConnection в Activity. Проверка всех ссылок показала: callback Activity
никогда не передавался bindService и не читался; эти экземпляры сервиса не
подключались и не использовались для команд. Конструирование создавало только
локальные объекты/Handler, без подключения или запланированных задач.
Настоящий ServiceConnection, bind/unbind и обработка GATT находятся в
BLEController и не изменялись. Общая регистрация UBI4 сохранена без новых
ветвлений; реализация обоих парсеров и код shared/iOS/DFU не менялись.

Относительно начала этого шага MainActivityUBI4: 1249 → 1201 строк (−48);
BleDependencies: 12 → 37 (+25); рабочий код суммарно −23, новых классов нет.
Остаются контекст запуска и связи Service с Activity (D2.3.3), телеметрия (D2.4)
и DataFactory (D2.5); это не завершение рефакторинга всей Activity.

Проверка D2.3.2: 820 app-тестов, включая архитектурные и BLE-тесты, без
падений/ошибок/пропусков; debug/release собраны (`/tmp/v3-ble-registration-build.log`).
На Samsung R8YX900N0JK с 00001 проверены новое подключение (GATT_SUCCESS),
завершение синхронизации до экрана Датчиков, Спецнастройки, фон/возврат и Жесты.
XML Спецнастроек до/после фона совпал; crash-buffer не изменился, FATAL EXCEPTION
в новом процессе нет (`/tmp/v3-ble-registration-phone.log`). Параметры не
редактировались, команды движения/калибровки не запускались. Физические
UBI4/INDY3 не проверялись; для общей сборки зависимостей проверены сохранность
обоих парсеров, аргументов и последовательности вызовов относительно исходника.

### D2.3.3 — контекст запуска

Из Activity удалён разбор name/address/type/profile из Intent. Общий Android
data-адаптер DeviceConnectionInitializer сохраняет этот алгоритм без изменения
правил и вызывается из BleDependencies.initializeSession до регистрации парсеров.
Он находится вне versions/v3, поскольку выбирает V3/UBI4 ещё до создания
экранной ViewModel. Это адаптация параметров Android-запуска к существующему
общему контексту, а не новый сценарий пользовательского редактирования.
V3-операции экранов по-прежнему идут через ViewModel → UseCase → Repository.

Сохранены исходные ключи, пустые значения при отсутствии extras, отсутствие
нормализации полного имени/адреса, приоритет валидного явного профиля (включая
NOT_V3) над именем и прежний fallback при неизвестном enum. Предварительная
проверка deviceType тоже сохранена: последующий bridge, как раньше, задаёт
итоговый профиль по явному значению или имени. Классификатор не продублирован.
onNewIntent, восстановление имени/адреса при onResume, сохранение последнего MAC,
WidgetRepoProvider и порядок подключения не менялись. Парсеры, shared/iOS,
Advanced и DFU не изменялись. Activity: 1201 → 1184 строк.

Открытая часть D2.3.3 — callbacks Service к Activity. Полного удаления глобального
ConnectionState этот шаг не заявляет; общий контекст остаётся для старых потребителей.

Проверка: 824 app-теста без падений/ошибок/пропусков, debug/release собраны
(`/tmp/v3-connection-context-build.log`). Новые проверки фиксируют выбор
STANDARD_V3/INDY3/NOT_V3, приоритет extras, неверный enum, различие type/name,
смену контекста и пустые extras. На Samsung R8YX900N0JK с 00001 подтверждены
GATT_SUCCESS, завершение синхронизации и шапка 00001; XML Датчиков совпал
до/после фона, исходный экран Жесты восстановлен. Crash-buffer не изменился,
в логе нового процесса нет FATAL EXCEPTION (`/tmp/v3-connection-context-phone.log`).
Параметры устройства не редактировались. UBI4/INDY3 проверены тестами выбора
контекста, без физической проверки этих устройств.

### D2.3.3 — Service без обратных вызовов в Activity

V3DeviceInfoRepositoryImpl и V3MainRepositoryImpl используют один
V3DeviceIdentityStore. Он содержит текущее имя/serial и fallback из Intent,
без Activity/Context. DI выдаёт экземпляр по главному ViewModelStore:
пересоздание Activity сохраняет источник, независимые главные экраны получают
разные экземпляры. WeakHashMap не удерживает завершённые scope и Activity.
Модель переименована в V3DeviceIdentity и находится в domain/device.

После enqueue имя обновляется в этом источнике; главная ViewModel получает
изменения через ObserveMainUpdatesUseCaseV3 и отображает их в UiState.
Поток identity наблюдается напрямую, без промежуточного merge/channel;
main-immediate сохраняет обновление шапки при синхронной отправке с UI-потока.
Наблюдение живёт до ViewDestroyed как прежние подписки Activity и возобновляет
последнее состояние при ViewCreated. Serial из DeviceInfo по-прежнему обновляет
SettingsProfileManager, переименование — нет.

Из фабрики Service удалены currentSerial/deviceName/intentDeviceName/applyDeviceName
callbacks и импорт MainActivityUBI4. Из Activity удалён applyDeviceNameImmediately
(проверены все Kotlin/Java-потребители, оставшихся вызовов нет), из Main Action
удалён DeviceNameChanged. Существующие getters аккаунта/телеметрии продолжают
читать Main UiState. onNewIntent обновляет только fallback имени, не текущую
идентификацию или подключение; соответствующая прежняя семантика сохранена.
Пакеты, ACK/achievement, read-after-write serial и проверки адреса/профиля не менялись.

MainActivityUBI4: 1184 → 1170 строк. Добавлен один data-источник вместо цепочки
Service Repository → Activity → Main ViewModel. Следующий этап D2.4 — телеметрия;
UBI4, shared/iOS, Advanced, парсеры и DFU в этом шаге не перерабатывались.

Проверка: 827 app-тестов без ошибок/падений/пропусков, debug/release собраны
(`/tmp/v3-service-identity-build.log`). Сквозной тест соединяет реальные
Service/Main repositories, Store и Main ViewModel: enqueue предшествует
локальному имени, UiState меняется до ACK, команда одна, serial профилей
не изменяется. Проверены blank, повторная подписка, очистка ViewModel,
изоляция scope и обновление fallback Intent. Прежние проверки пакетов,
read-after-write и поздних ACK сохранены.

Samsung R8YX900N0JK / 00001: GATT_SUCCESS, синхронизация завершилась, поле имени
в Service подставило 00001 и сохранило его после фона; шапка тоже 00001.
Записать не нажималось. Служебная вкладка снова скрыта, исходный экран Жесты
восстановлен. Crash-buffer не изменился, FATAL EXCEPTION в новом процессе нет
(`/tmp/v3-service-identity-phone.log`). Физические UBI4/INDY3 не проверялись.

### D2.4.1 — зависимости телеметрии

Activity вызывает BleDependencies.bindTelemetry до initBLEStructure/connect.
DI создаёт прежний TelemetryCoordinator с тем же lifecycleScope/preferences,
подключает запрос BLE и showToast, регистрирует тот же onConnectedListener:
проверка UiState.isInterfaceV3Activated при событии, sendTelemetry(false).
BLEController.cleanup по-прежнему удаляет listener; новых подписок нет.

Coordinator читает fallback в data: текущий serial, имя, connectedDeviceName,
preferences CONNECTED_DEVICE с прежним значением по умолчанию "null".
Первые два значения поступают из уже существующего V3DeviceIdentityStore
главного scope. Список формируется только когда Sender запросит его после
ожидания BLE-ответа; пробелы/null/порядок не нормализуются дополнительно.
Shared Sender, выбор UUID, payload и протокол не изменены. Правила 24 часов,
sendInProgress, запись timestamp после успеха, ошибки/отмена и Toast сохранены.

Из Activity удалены поле coordinator, fallback callback и устаревший
закомментированный обработчик ручной отправки. Activity: 1170 → 1146 строк;
во всех трёх рабочих файлах суммарно +1 строка, новых рабочих классов нет.
Это перенос сборки и источников данных; правило 24 часов пока остаётся в
Coordinator. Следом D2.4.2 (выгрузка при уходе), затем D2.4.3 (правила телеметрии).

Проверка: 831 app-тест без ошибок/падений/пропусков, debug/release собраны
(`/tmp/v3-telemetry-wiring-build.log`). Новые тесты с подменённым Sender проверяют
живые данные после ожидания ответа, порядок/null/пробелы fallback, интервал,
повторную отправку, сохранение timestamp только после успеха, ошибки и отмену
с возможностью повтора. Сеть и реальное устройство в unit-тестах не использовались.
2026-09-22 пользователь самостоятельно переустановил приложение и подключил
Samsung R8YX900N0JK. Выполнена проверка чтением: экран Датчики, устройство 00001,
в текущем процессе PID 12601 есть GATT_SUCCESS, нет FATAL EXCEPTION/ANR;
crash-buffer совпадает с сохранённым до проверки. В 13:08:31 по времени телефона
TelemetryV3 сообщает пропуск отправки: предыдущая была менее 24 часов назад.
Логи: /tmp/v3-telemetry-wiring-phone.log. Отправка на сервер не проверена.
SHA-256 установленного APK не совпадает с локальными проверенными debug/release;
это не доказывает устаревший код, но не позволяет подтвердить аппаратную проверку
именно этих сборок. Установка и запуск агентом не выполнялись; эмулятор не
использовался. Полная проверка этого шага на V3, а также UBI4/INDY3 остаётся открытой.

### D2.4.2 — планирование выгрузки профилей при уходе в фон

V3: onResume → ViewResumed → Main ViewModel → ScheduleProfileUploadUseCaseV3
сбрасывает guard и отменяет app-close задачу через V3MainRepositoryImpl.
onStop и прежний fallback onDestroy передают ViewStopped с текущим фактом
BLE-соединения, переходом к сканированию и исходным locate. Activity остаётся
владельцем BLEController; Android/BLE-типы не поступают во ViewModel/domain.
UseCase проверяет условия, ставит guard до обращения к scheduler, выбирает
"en" только для пустого/пробельного языка. ViewCreated сбрасывает guard нового
экземпляра Activity при сохранённой ViewModel. ViewDestroyed отменяет наблюдения,
но не блокирует последующий fallback планирования; очищенная ViewModel игнорирует Actions.

Репозиторий хранит только applicationContext и вызывает существующий scheduler.
WorkManager: прежние 3 секунды, REPLACE, ограничение сети, serial из
SettingsProfileManager и общий connection-guard. Worker, сетевой payload и
UBI4-ветка не изменены. Удалённая из V3 ветки диагностика отсутствия соединения
больше не печатается; само условие пропуска сохранено в UseCase.
Новый рабочий класс один; Activity 1146 → 1154 строки из-за разделения путей V3/UBI4.

837 app-тестов без ошибок/пропусков; debug/release собраны
(/tmp/v3-profile-upload-build.log). Проверены stop/destroy, возврат, пересоздание,
нет соединения/переход к сканированию, язык, исключение scheduler, отсутствие
записей при render/после clear и applicationContext в data. Scheduler подменён
в unit-тесте: реальная отправка не выполнялась. Новая сборка на телефоне не
устанавливалась и не проверялась; прежняя проверка телефона относится к D2.4.1.

### D2.4.3 — правила автоматической телеметрии

BLE callback → существующий TelemetryCoordinator → SendTelemetryUseCaseV3 →
V3TelemetryRepositoryImpl → неизменённый Ubi4TelemetrySender. Это автоматическая
операция соединения, без нового экранного состояния/Action или отдельной ViewModel.
Coordinator сохраняет прежний Activity lifecycleScope и вывод логов/Toast;
данные устройства, preferences и правила из него удалены (86 → 42 строки).
UseCase и repository собираются в BleDependencies на каждый listener, как раньше
создавался coordinator. Callback, проверка V3, cleanup listener и scope не изменены.

Domain решает: при активной отправке — пропуск; положительный timestamp моложе
24 часов — пропуск; 0/отрицательный timestamp и ровно 24 часа разрешают отправку.
Время успеха читается после завершения отправки. Guard освобождается в finally,
включая ошибку и отмену; отмена не превращается в пользовательскую ошибку.
Repository сохраняет timestamp в прежнем ключе через apply(), поздно читает
fallback в прежнем порядке и переводит три специальных исключения Sender в
domain-ошибки. DTO Sender не выходит из data; результат содержит только grips.
Общий Sender сохраняет выбор UUID, ожидание BLE и сетевой payload. iOS/shared
не изменены. Автоматический вызов по-прежнему не показывает Toast.

843 app-теста без ошибок/пропусков, debug/release собраны
(/tmp/v3-telemetry-domain-build.log). Проверены граница интервала и будущее время,
параллельный запрос, timestamp завершения, ошибки отправки/сохранения, отмена
ожидания при завершении scope и прежние тексты сообщений. Сохранены проверки
fallback после BLE и режима без Toast. Сетевой Sender в тестах подменён;
новая сборка не устанавливалась на телефон, реальная отправка не проверена.

### Решение пользователя о формировании виджетов

Замена источников DataFactory на Android-каталоги отменена. Пользователь откатил
два последних изменения: отдельный каталог «Спецнастроек» и начатый единый каталог.
Состав экранов по-прежнему задают generatedHardcodeWidgets/generatedHardcodeWidgetsINDY3
с display/widgetPosition. DataFactory и DataFactoryV3*WidgetsSource сохраняются;
внедрённые ранее UiState/Action и цепочки UseCase/Repository не отменяются.

### D2.3.4 — сохранённое подключение V3

MainActivity передаёт операции в Main ViewModel → ManageMainConnectionUseCaseV3 →
V3MainRepositoryImpl. Последний MAC сохраняется после подготовки identity;
MAC хранилища устанавливается отдельно в прежней точке onCreate, до UI/BLE.
Восстановление имени/адреса остаётся под проверкой наличия BLE service в onResume,
перед reconnect; сброс последнего MAC выполняется до перехода на ScanActivity.
UiState не запускает повторные записи. Сохраняются исходные строки, ключи,
apply(), fallback «NOT SET!» и строковый маркер «null». Восстановление не меняет
identity, серийный номер профиля или MAC хранилища. Диагностический count Room
на V3 удалён; UBI4-путь оставлен. Формирование виджетов, shared и iOS не менялись.
846 app-тестов без ошибок/пропусков, debug/release собраны
(/tmp/v3-main-connection-build.log). Проверены исходные значения preferences,
сброс без удаления сохранённого подключения, отсутствие повторных операций при
обновлении UiState и запрет действий после очистки ViewModel. Новая сборка
на телефоне не проверялась; аппаратная приёмка остаётся открытой.
